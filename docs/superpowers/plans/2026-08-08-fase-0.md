# Fase 0 — Plan de implementación

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Dejar la base de TallerPro en condiciones de producción: esquema administrado con migraciones, correlativo de orden atómico, estado de orden derivado de sus trabajos, y adjuntos fuera del disco efímero.

**Architecture:** Cuatro cortes secuenciales sobre el backend NestJS, cada uno desplegable por separado. La lógica de estados vive en un módulo de funciones puras (`estado-orden.ts`) para poder probarla sin base de datos; la derivación se aplica dentro de una transacción que bloquea la fila de la orden. El almacenamiento de archivos se abstrae detrás de una interfaz con dos implementaciones (disco y S3) elegidas por variable de entorno.

**Tech Stack:** NestJS 11, TypeORM 1.1.0, PostgreSQL 16, Jest 30 + ts-jest, Angular 20 (signals), `@aws-sdk/client-s3` + `@aws-sdk/s3-request-presigner`.

**Spec:** `docs/superpowers/specs/2026-08-08-fase-0-design.md`

## Global Constraints

- Rama de trabajo: `fase-0`. No commitear a `master`.
- Mensajes de commit en español, formato conventional commits en minúscula (`feat:`, `fix:`, `docs:`, `chore:`, `test:`), igual que el historial existente.
- Todo commit termina con el trailer `Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>`.
- Tests unitarios del backend: viven junto al código en `backend/src/**/*.spec.ts` (`rootDir: src`, `testRegex: .*\.spec\.ts$`). Se corren con `npm test` desde `backend/`.
- Tests e2e del backend: viven en `backend/test/*.e2e-spec.ts`. Se corren con `npm run test:e2e` desde `backend/`. **Requieren PostgreSQL levantado** (`docker compose up -d` desde la raíz).
- La base local es desechable: se puede borrar y recrear en cualquier momento.
- `noImplicitAny: false` y `strictNullChecks: true` en `backend/tsconfig.json`. El código existente usa `as any` para las referencias por id en TypeORM (`{ id } as any`); mantener ese patrón.
- Nombres de dominio en español (`orden`, `trabajo`, `adjunto`, `clave`), igual que el resto del proyecto.
- El límite de subida es de **5 MB** y las extensiones permitidas son **jpg, jpeg, png, pdf**. No cambian en esta fase.
- La vigencia de las URL firmadas de S3 es de **300 segundos** (5 minutos).
- Formato del correlativo: `ORD-` + número con `padStart(6, '0')`.

---

## Estructura de archivos

**Se crean**

| Archivo | Responsabilidad |
|---|---|
| `backend/src/data-source.ts` | `DataSource` para el CLI de TypeORM. Solo lo consume el CLI |
| `backend/src/migrations/*.ts` | Migraciones generadas y escritas a mano |
| `backend/src/ordenes/estado-orden.ts` | Funciones puras de derivación y transición de estado |
| `backend/src/ordenes/estado-orden.spec.ts` | Tests unitarios de lo anterior |
| `backend/src/almacenamiento/almacenamiento.interface.ts` | Contrato e inyección `ALMACENAMIENTO` |
| `backend/src/almacenamiento/almacenamiento-disco.service.ts` | Driver de disco (desarrollo) |
| `backend/src/almacenamiento/almacenamiento-disco.spec.ts` | Tests del driver de disco |
| `backend/src/almacenamiento/almacenamiento-s3.service.ts` | Driver S3 (producción) |
| `backend/src/almacenamiento/almacenamiento.module.ts` | Factory que elige el driver |
| `backend/test/flujo-orden.e2e-spec.ts` | e2e del ciclo completo de una orden |

**Se modifican**

| Archivo | Cambio |
|---|---|
| `backend/src/app.module.ts` | `synchronize: false`, migraciones, `ServeStaticModule` condicional, `AlmacenamientoModule` |
| `backend/package.json` | Scripts de migración, `dotenv`, SDK de S3 |
| `backend/tsconfig.json` | Override de `ts-node` a `commonjs` |
| `backend/src/ordenes/ordenes.service.ts` | Correlativo por secuencia, `entregar`, `cancelar`, borrado sin huérfanos |
| `backend/src/ordenes/ordenes.controller.ts` | Endpoints `entregar` y `cancelar` |
| `backend/src/ordenes/dto/actualizar-orden.dto.ts` | Sale `estado` |
| `backend/src/trabajos/trabajos.service.ts` | Transacción con bloqueo + derivación |
| `backend/src/trabajos/trabajos.module.ts` | Importa `AdjuntosModule` |
| `backend/src/trabajos/dto/actualizar-trabajo.dto.ts` | Sale `orden_id` |
| `backend/src/adjuntos/*` | Pasa a usar la interfaz de almacenamiento |
| `backend/src/adjuntos/entities/adjunto.entity.ts` | `ruta` → `clave`, se va `nombre_archivo` |
| `frontend/src/app/...` | Botones de estado, adjuntos por `url` |
| `.env.example`, `render.yaml`, `README.md` | Variables y documentación |

**Se eliminan**

| Archivo | Motivo |
|---|---|
| `backend/test/app.e2e-spec.ts` | Espera un `GET /` con `"Hello World!"` que no existe |
| `frontend/src/app/app.spec.ts` | Busca un `<h1>` con `"Hello, frontend"` que ya no está |

---

# CORTE 1 — Migraciones

### Task 1: Configurar el DataSource y los scripts de migración

**Files:**
- Create: `backend/src/data-source.ts`
- Modify: `backend/package.json` (scripts y devDependencies)
- Modify: `backend/tsconfig.json` (override de ts-node)

**Interfaces:**
- Consumes: nada.
- Produces: `backend/src/data-source.ts` con `export default new DataSource(...)`, consumido por el CLI mediante `-d src/data-source.ts`. Los scripts `migration:generate|run|revert|show`.

- [ ] **Step 1: Instalar dotenv**

`data-source.ts` corre fuera de Nest, así que necesita cargar el `.env` por su cuenta. Hoy `dotenv` llega solo como dependencia transitiva de `@nestjs/config`; declararlo evita que un cambio ajeno lo rompa.

```bash
cd backend && npm install --save-dev dotenv
```

- [ ] **Step 2: Añadir el override de ts-node en `backend/tsconfig.json`**

El proyecto compila con `"module": "nodenext"`. El CLI de TypeORM ejecuta el `DataSource` a través de ts-node, que con `nodenext` falla al resolver los `import` del archivo. La solución estándar es forzar `commonjs` solo para ts-node, sin tocar la compilación de Nest.

Añadir como propiedad de primer nivel del JSON, hermana de `compilerOptions`:

```json
  "ts-node": {
    "compilerOptions": {
      "module": "commonjs",
      "moduleResolution": "node"
    }
  }
```

- [ ] **Step 3: Crear `backend/src/data-source.ts`**

Replica la misma bifurcación que ya usa `app.module.ts`: si hay `DATABASE_URL` (producción) la usa con SSL; si no, arma la conexión desde las variables sueltas de Docker. La diferencia es que aquí las entidades van por glob, porque `autoLoadEntities` depende del contenedor de Nest y el CLI no lo tiene.

```ts
import 'reflect-metadata';
import { config } from 'dotenv';
import { DataSource, DataSourceOptions } from 'typeorm';

config();

const rutas = {
  entities: ['src/**/*.entity.ts'],
  migrations: ['src/migrations/*.ts'],
};

const url = process.env.DATABASE_URL;

const opciones: DataSourceOptions = url
  ? {
      type: 'postgres',
      url,
      ssl: { rejectUnauthorized: false },
      ...rutas,
    }
  : {
      type: 'postgres',
      host: process.env.DB_HOST,
      port: Number(process.env.DB_PORT),
      username: process.env.DB_USER,
      password: process.env.DB_PASSWORD,
      database: process.env.DB_NAME,
      ...rutas,
    };

export default new DataSource(opciones);
```

- [ ] **Step 4: Añadir los scripts a `backend/package.json`**

Dentro de `"scripts"`, junto a los que ya existen:

```json
    "typeorm": "typeorm-ts-node-commonjs -d src/data-source.ts",
    "migration:generate": "npm run typeorm -- migration:generate",
    "migration:run": "npm run typeorm -- migration:run",
    "migration:revert": "npm run typeorm -- migration:revert",
    "migration:show": "npm run typeorm -- migration:show"
```

- [ ] **Step 5: Verificar que el CLI conecta**

Con PostgreSQL levantado (`docker compose up -d` desde la raíz):

```bash
cd backend && npm run migration:show
```

Esperado: conecta y no lista ninguna migración (todavía no hay). Si aparece un error de conexión, revisar el `.env`; si aparece `Unknown file extension ".ts"` o un error de módulos ESM, el override de ts-node del Step 2 no quedó bien aplicado.

- [ ] **Step 6: Commit**

```bash
git add backend/src/data-source.ts backend/package.json backend/package-lock.json backend/tsconfig.json
git commit -m "chore: configurar el DataSource y los scripts de migración"
```

---

### Task 2: Generar la migración inicial y apagar synchronize

**Files:**
- Create: `backend/src/migrations/<timestamp>-EsquemaInicial.ts` (generada por el CLI)
- Modify: `backend/src/app.module.ts:24-52` (el `useFactory` de TypeORM)

**Interfaces:**
- Consumes: los scripts y el `DataSource` de la Task 1.
- Produces: la tabla `migrations` en la base y el esquema completo creado por migración. A partir de aquí, todo cambio de esquema es una migración.

- [ ] **Step 1: Vaciar la base local**

La migración debe generarse contra una base vacía, si no el CLI la compara con el esquema que ya creó `synchronize` y genera un archivo vacío.

```bash
docker compose down -v && docker compose up -d
```

Esperado: el volumen `postgres_data` se recrea desde cero. Esperar unos segundos a que PostgreSQL acepte conexiones.

- [ ] **Step 2: Generar la migración inicial**

```bash
cd backend && npm run migration:generate -- src/migrations/EsquemaInicial
```

Esperado: crea `backend/src/migrations/<timestamp>-EsquemaInicial.ts` con los `CREATE TABLE` de las siete tablas.

- [ ] **Step 3: Revisar la migración generada**

Abrir el archivo y confirmar que están las siete tablas —`usuarios`, `roles`, `usuario_roles`, `ordenes`, `trabajos`, `comentarios`, `adjuntos`— con sus claves foráneas, los `ON DELETE CASCADE` de `trabajos`, `comentarios` y `adjuntos`, y los índices únicos de `usuarios.username`, `usuarios.email`, `roles.nombre` y `ordenes.numero_orden`. Contrastar con `docs/modelo-er.md`, que documenta el esquema esperado.

- [ ] **Step 4: Apagar synchronize en `backend/src/app.module.ts`**

En **las dos ramas** del `useFactory` (la de `DATABASE_URL` y la de variables sueltas), reemplazar `synchronize: true` por estas tres líneas:

```ts
            synchronize: false,
            migrations: [__dirname + '/migrations/*.js'],
            migrationsRun: true,
```

El glob apunta a `.js` y funciona igual en desarrollo y en producción porque `nest start` también ejecuta desde `dist/`. `migrationsRun: true` aplica lo pendiente al arrancar, así Render no necesita un paso extra de build.

- [ ] **Step 5: Verificar el arranque contra una base vacía**

```bash
docker compose down -v && docker compose up -d
cd backend && npm run start:dev
```

Esperado en el log: TypeORM ejecuta `EsquemaInicial`, y después `SeedService` imprime `Roles iniciales creados` y `Usuarios de prueba creados (contraseña: 123456)`. El seed corre después porque las migraciones se aplican al inicializar el `DataSource`, antes de los `onModuleInit`.

- [ ] **Step 6: Verificar que se puede iniciar sesión**

```bash
curl -s -X POST http://localhost:3001/auth/login \
  -H 'Content-Type: application/json' \
  -d '{"email":"admin@taller.com","password":"123456"}'
```

Esperado: JSON con `access_token` y el usuario con rol `Administrador`.

- [ ] **Step 7: Confirmar que no queda synchronize**

```bash
grep -rn "synchronize: true" backend/src
```

Esperado: sin resultados.

- [ ] **Step 8: Commit**

```bash
git add backend/src/migrations backend/src/app.module.ts
git commit -m "feat: administrar el esquema con migraciones en vez de synchronize"
```

---

### Task 2b: Claves foráneas obligatorias y limpieza del corte 1

Tarea añadida durante la ejecución, a partir de tres hallazgos de la revisión de
las Tasks 1 y 2. Aprobada por el usuario el 2026-08-08.

**Files:**
- Create: `backend/src/migrations/<timestamp>-FksObligatorias.ts` (generada por el CLI)
- Modify: `backend/src/ordenes/entities/orden.entity.ts`
- Modify: `backend/src/trabajos/entities/trabajo.entity.ts`
- Modify: `backend/src/comentarios/entities/comentario.entity.ts`
- Modify: `backend/src/adjuntos/entities/adjunto.entity.ts`
- Modify: `backend/src/data-source.ts` (silenciar dotenv)
- Modify: `backend/eslint.config.mjs:9` (ignorar migraciones generadas)

**Interfaces:**
- Consumes: la infraestructura de migraciones de las Tasks 1 y 2.
- Produces: nada nuevo. Endurece el esquema existente.

- [ ] **Step 1: Silenciar la línea promocional de dotenv**

`dotenv@17` imprime una línea de propaganda en stdout en cada `config()`, que
ensucia la salida de todo comando `migration:*`. En `backend/src/data-source.ts`:

```ts
config({ quiet: true });
```

- [ ] **Step 2: Ignorar las migraciones generadas en eslint**

El CLI de TypeORM genera los archivos con comillas dobles, indentación de cuatro
espacios y sin comas finales, y no vale la pena reformatearlos a mano en cada
migración futura. En `backend/eslint.config.mjs`, línea 9:

```js
    ignores: ['eslint.config.mjs', 'src/migrations/**'],
```

- [ ] **Step 3: Marcar como obligatorias las relaciones que lo son**

Ningún `@ManyToOne` del proyecto pasa `{ nullable: false }`, así que TypeORM creó
todas sus columnas como nullable. La consecuencia real es que la base acepta una
orden sin autor o un comentario sin usuario, cosa que el modelo documentado en
`docs/modelo-er.md` prohíbe. Añadir `{ nullable: false }` a estas siete
relaciones:

| Entidad | Relación | Columna |
|---|---|---|
| `Orden` | `creado_por` | `creado_por` |
| `Trabajo` | `orden` | `orden_id` |
| `Trabajo` | `creado_por` | `creado_por` |
| `Comentario` | `trabajo` | `trabajo_id` |
| `Comentario` | `usuario` | `usuario_id` |
| `Adjunto` | `trabajo` | `trabajo_id` |
| `Adjunto` | `subido_por` | `subido_por` |

**`Trabajo.asignado_a` NO entra en la lista** y conserva su `{ nullable: true }`
explícito: un trabajo puede existir sin mecánico asignado, y ésa es justamente la
columna `PENDIENTE` del Kanban.

Cuidado con la sintaxis cuando la relación ya tiene opciones: `@ManyToOne(() =>
Orden, (orden) => orden.trabajos, { onDelete: 'CASCADE', nullable: false })`.

- [ ] **Step 4: Generar la migración**

```bash
cd backend && npm run migration:generate -- src/migrations/FksObligatorias
```

Esperado: una migración con siete `ALTER TABLE ... ALTER COLUMN ... SET NOT NULL`.
Revisarla antes de seguir: si aparece algún `DROP`/`CREATE` de tabla, o toca
`trabajos.asignado_a`, algo salió mal en el Step 3.

- [ ] **Step 5: Aplicar la migración**

```bash
cd backend && npm run migration:run
```

Esperado: `FksObligatorias` aplicada. Si falla con "column contains null values",
hay filas viejas sin autor: la base es desechable, así que
`docker compose down -v && docker compose up -d`, arrancar la API para recrear el
esquema y el seed, y volver a intentar.

- [ ] **Step 6: Verificar que la restricción existe de verdad**

No basta con que la migración corra. Comprobar que la base rechaza el dato malo:

```bash
docker compose exec -T postgres psql -U postgres -d db_taller -c \
  "INSERT INTO comentarios (id, contenido, trabajo_id, usuario_id) VALUES (gen_random_uuid(), 'x', NULL, NULL);"
```

Esperado: error `null value in column "trabajo_id" ... violates not-null constraint`.
Antes de esta tarea, ese INSERT habría pasado.

- [ ] **Step 7: Verificar que la aplicación sigue funcionando**

Arrancar la API y recorrer el flujo que escribe en las siete columnas: iniciar
sesión como `jefe@taller.com`, crear una orden, crear un trabajo en ella y dejar un
comentario. Los tres deben responder `201`. Es la comprobación de que ningún
servicio dependía de poder dejar una FK en `NULL`.

- [ ] **Step 8: Commit**

```bash
git add backend/src backend/eslint.config.mjs
git commit -m "fix: exigir las claves foráneas obligatorias en el esquema"
```

---

# CORTE 2 — Correlativo de orden

### Task 3: Correlativo por secuencia de PostgreSQL

**Files:**
- Create: `backend/src/migrations/<timestamp>-CrearSecuenciaNumeroOrden.ts` (a mano)
- Create: `backend/src/ordenes/numero-orden.ts`
- Create: `backend/src/ordenes/numero-orden.spec.ts`
- Modify: `backend/src/ordenes/ordenes.service.ts:95-99` (`generarNumeroOrden`)

**Interfaces:**
- Consumes: la infraestructura de migraciones de la Task 2.
- Produces: `formatearNumeroOrden(valor: number): string` exportada desde `backend/src/ordenes/numero-orden.ts`. La secuencia `ordenes_numero_seq` en la base.

- [ ] **Step 1: Escribir el test del formateador**

Crear `backend/src/ordenes/numero-orden.spec.ts`:

```ts
import { formatearNumeroOrden } from './numero-orden';

describe('formatearNumeroOrden', () => {
  it('rellena con ceros hasta seis dígitos', () => {
    expect(formatearNumeroOrden(1)).toBe('ORD-000001');
    expect(formatearNumeroOrden(42)).toBe('ORD-000042');
  });

  it('no trunca cuando el número supera los seis dígitos', () => {
    expect(formatearNumeroOrden(1234567)).toBe('ORD-1234567');
  });

  it('cabe en los 20 caracteres de la columna', () => {
    expect(formatearNumeroOrden(999999).length).toBeLessThanOrEqual(20);
  });
});
```

- [ ] **Step 2: Correr el test para verificar que falla**

```bash
cd backend && npm test -- numero-orden
```

Esperado: FAIL, `Cannot find module './numero-orden'`.

- [ ] **Step 3: Escribir el formateador**

Crear `backend/src/ordenes/numero-orden.ts`:

```ts
export const SECUENCIA_NUMERO_ORDEN = 'ordenes_numero_seq';

export function formatearNumeroOrden(valor: number): string {
  return `ORD-${String(valor).padStart(6, '0')}`;
}
```

- [ ] **Step 4: Correr el test para verificar que pasa**

```bash
cd backend && npm test -- numero-orden
```

Esperado: PASS, 3 tests.

- [ ] **Step 5: Escribir la migración de la secuencia**

Crear `backend/src/migrations/<timestamp>-CrearSecuenciaNumeroOrden.ts`. Usar como timestamp el resultado de `date +%s000` para que quede después de `EsquemaInicial`.

El `setval` arranca la secuencia por encima del número más alto que ya exista, de modo que la migración es segura aunque se aplique sobre una base con órdenes cargadas. El tercer argumento `false` significa "el próximo `nextval` devuelve exactamente este valor".

```ts
import { MigrationInterface, QueryRunner } from 'typeorm';

export class CrearSecuenciaNumeroOrden<TIMESTAMP> implements MigrationInterface {
  public async up(queryRunner: QueryRunner): Promise<void> {
    await queryRunner.query(`CREATE SEQUENCE "ordenes_numero_seq"`);
    await queryRunner.query(`
      SELECT setval('ordenes_numero_seq', COALESCE(
        (SELECT MAX(NULLIF(regexp_replace(numero_orden, '\\D', '', 'g'), '')::bigint)
         FROM ordenes), 0
      ) + 1, false)
    `);
  }

  public async down(queryRunner: QueryRunner): Promise<void> {
    await queryRunner.query(`DROP SEQUENCE "ordenes_numero_seq"`);
  }
}
```

Reemplazar `<TIMESTAMP>` en el nombre de la clase por el mismo timestamp del archivo, siguiendo la convención de TypeORM (`CrearSecuenciaNumeroOrden1754661234000`).

- [ ] **Step 6: Cambiar `generarNumeroOrden` en `ordenes.service.ts`**

Reemplazar el método que cuenta filas:

```ts
  private async generarNumeroOrden(): Promise<string> {
    const filas: Array<{ nextval: string }> = await this.ordenRepository.manager.query(
      `SELECT nextval('${SECUENCIA_NUMERO_ORDEN}') AS nextval`,
    );
    return formatearNumeroOrden(Number(filas[0].nextval));
  }
```

Añadir el import:

```ts
import { formatearNumeroOrden, SECUENCIA_NUMERO_ORDEN } from './numero-orden';
```

`nextval` vuelve como texto porque PostgreSQL lo devuelve como `bigint`, igual que pasa con las columnas `numeric`. De ahí el `Number()`.

- [ ] **Step 7: Verificar el comportamiento contra la base**

Levantar la API (`npm run start:dev`), obtener un token de `jefe@taller.com` y crear tres órdenes seguidas con el mismo cuerpo:

```bash
TOKEN=$(curl -s -X POST http://localhost:3001/auth/login \
  -H 'Content-Type: application/json' \
  -d '{"email":"jefe@taller.com","password":"123456"}' | sed -n 's/.*"access_token":"\([^"]*\)".*/\1/p')

for i in 1 2 3; do
  curl -s -X POST http://localhost:3001/ordenes \
    -H "Authorization: Bearer $TOKEN" -H 'Content-Type: application/json' \
    -d '{"descripcion":"Cambio de aceite","fecha_ingreso":"2026-08-08","placa":"ABC-123","marca":"Toyota","modelo":"Yaris","cliente_nombre":"Juan Perez","cliente_telefono":"999888777"}' \
    | sed -n 's/.*"numero_orden":"\([^"]*\)".*/\1/p'
done
```

Esperado: `ORD-000001`, `ORD-000002`, `ORD-000003`.

- [ ] **Step 8: Verificar que eliminar no reutiliza el número**

Borrar la última orden con el token de `admin@taller.com` (`DELETE /ordenes/:id`), crear otra y comprobar que sale `ORD-000004` y no `ORD-000003`. Éste es exactamente el caso que rompía con `COUNT(*) + 1`.

- [ ] **Step 9: Commit**

```bash
git add backend/src/ordenes/numero-orden.ts backend/src/ordenes/numero-orden.spec.ts \
        backend/src/ordenes/ordenes.service.ts backend/src/migrations
git commit -m "fix: generar el correlativo de orden con una secuencia de PostgreSQL"
```

---

# CORTE 3 — Máquina de estados

### Task 4: Lógica pura de estados de orden

**Files:**
- Create: `backend/src/ordenes/estado-orden.ts`
- Create: `backend/src/ordenes/estado-orden.spec.ts`

**Interfaces:**
- Consumes: `EstadoOrden` y `EstadoTrabajo` de `backend/src/common/enums/estados.enum.ts`.
- Produces, todas exportadas desde `backend/src/ordenes/estado-orden.ts`:
  - `derivarEstado(estadosDeTrabajos: string[]): EstadoOrden`
  - `esTerminal(estado: string): boolean`
  - `puedeEntregar(estado: string): boolean`
  - `puedeCancelar(estado: string): boolean`

Los parámetros son `string` y no `EstadoOrden` porque la columna `estado` de las entidades está declarada como `varchar`, y así se evita castear en cada llamada.

- [ ] **Step 1: Escribir los tests**

Crear `backend/src/ordenes/estado-orden.spec.ts`:

```ts
import { EstadoOrden, EstadoTrabajo } from '../common/enums/estados.enum';
import { derivarEstado, esTerminal, puedeCancelar, puedeEntregar } from './estado-orden';

const { PENDIENTE, EN_PROCESO, COMPLETADO } = EstadoTrabajo;

describe('derivarEstado', () => {
  it('una orden sin trabajos queda RECIBIDA', () => {
    expect(derivarEstado([])).toBe(EstadoOrden.RECIBIDA);
  });

  it('con todos los trabajos pendientes queda RECIBIDA', () => {
    expect(derivarEstado([PENDIENTE, PENDIENTE])).toBe(EstadoOrden.RECIBIDA);
  });

  it('con algún trabajo en proceso pasa a EN_PROCESO', () => {
    expect(derivarEstado([PENDIENTE, EN_PROCESO])).toBe(EstadoOrden.EN_PROCESO);
  });

  it('con parte completada y parte sin empezar sigue EN_PROCESO', () => {
    expect(derivarEstado([PENDIENTE, COMPLETADO])).toBe(EstadoOrden.EN_PROCESO);
  });

  it('con todos los trabajos completados pasa a FINALIZADA', () => {
    expect(derivarEstado([COMPLETADO, COMPLETADO])).toBe(EstadoOrden.FINALIZADA);
  });

  it('con un único trabajo completado pasa a FINALIZADA', () => {
    expect(derivarEstado([COMPLETADO])).toBe(EstadoOrden.FINALIZADA);
  });
});

describe('esTerminal', () => {
  it('ENTREGADA y CANCELADA son terminales', () => {
    expect(esTerminal(EstadoOrden.ENTREGADA)).toBe(true);
    expect(esTerminal(EstadoOrden.CANCELADA)).toBe(true);
  });

  it('los estados de taller no son terminales', () => {
    expect(esTerminal(EstadoOrden.RECIBIDA)).toBe(false);
    expect(esTerminal(EstadoOrden.EN_PROCESO)).toBe(false);
    expect(esTerminal(EstadoOrden.FINALIZADA)).toBe(false);
  });
});

describe('puedeEntregar', () => {
  it('solo se entrega desde FINALIZADA', () => {
    expect(puedeEntregar(EstadoOrden.FINALIZADA)).toBe(true);
  });

  it('no se entrega desde ningún otro estado', () => {
    expect(puedeEntregar(EstadoOrden.RECIBIDA)).toBe(false);
    expect(puedeEntregar(EstadoOrden.EN_PROCESO)).toBe(false);
    expect(puedeEntregar(EstadoOrden.ENTREGADA)).toBe(false);
    expect(puedeEntregar(EstadoOrden.CANCELADA)).toBe(false);
  });
});

describe('puedeCancelar', () => {
  it('se cancela desde cualquier estado no terminal', () => {
    expect(puedeCancelar(EstadoOrden.RECIBIDA)).toBe(true);
    expect(puedeCancelar(EstadoOrden.EN_PROCESO)).toBe(true);
    expect(puedeCancelar(EstadoOrden.FINALIZADA)).toBe(true);
  });

  it('no se cancela lo que ya es terminal', () => {
    expect(puedeCancelar(EstadoOrden.ENTREGADA)).toBe(false);
    expect(puedeCancelar(EstadoOrden.CANCELADA)).toBe(false);
  });
});
```

- [ ] **Step 2: Correr los tests para verificar que fallan**

```bash
cd backend && npm test -- estado-orden
```

Esperado: FAIL, `Cannot find module './estado-orden'`.

- [ ] **Step 3: Escribir la implementación**

Crear `backend/src/ordenes/estado-orden.ts`:

```ts
import { EstadoOrden, EstadoTrabajo } from '../common/enums/estados.enum';

const TERMINALES: readonly string[] = [EstadoOrden.ENTREGADA, EstadoOrden.CANCELADA];

/**
 * El estado de la orden dentro del taller es función de sus trabajos.
 * Las tres reglas se evalúan en orden, así que una mezcla de pendientes
 * y completados cae en EN_PROCESO.
 */
export function derivarEstado(estadosDeTrabajos: string[]): EstadoOrden {
  if (estadosDeTrabajos.every((estado) => estado === EstadoTrabajo.PENDIENTE)) {
    return EstadoOrden.RECIBIDA;
  }
  if (estadosDeTrabajos.every((estado) => estado === EstadoTrabajo.COMPLETADO)) {
    return EstadoOrden.FINALIZADA;
  }
  return EstadoOrden.EN_PROCESO;
}

export function esTerminal(estado: string): boolean {
  return TERMINALES.includes(estado);
}

export function puedeEntregar(estado: string): boolean {
  return estado === EstadoOrden.FINALIZADA;
}

export function puedeCancelar(estado: string): boolean {
  return !esTerminal(estado);
}
```

Nota sobre el arreglo vacío: `[].every(...)` devuelve `true`, así que una orden sin trabajos entra por la primera regla y queda `RECIBIDA`, que es lo que se busca. El test lo cubre explícitamente para que nadie "arregle" ese comportamiento por accidente.

- [ ] **Step 4: Correr los tests para verificar que pasan**

```bash
cd backend && npm test -- estado-orden
```

Esperado: PASS, 12 tests.

- [ ] **Step 5: Commit**

```bash
git add backend/src/ordenes/estado-orden.ts backend/src/ordenes/estado-orden.spec.ts
git commit -m "feat: lógica de derivación y transición del estado de la orden"
```

---

### Task 5: Acciones de entrega y cancelación

**Files:**
- Modify: `backend/src/ordenes/ordenes.service.ts` (métodos nuevos)
- Modify: `backend/src/ordenes/ordenes.controller.ts` (endpoints nuevos)
- Modify: `backend/src/ordenes/dto/actualizar-orden.dto.ts` (sale `estado`)
- Modify: `backend/src/trabajos/dto/actualizar-trabajo.dto.ts` (sale `orden_id`)

**Interfaces:**
- Consumes: `puedeEntregar`, `puedeCancelar` de la Task 4.
- Produces: `OrdenesService.entregar(id: string): Promise<Orden>` y `OrdenesService.cancelar(id: string): Promise<Orden>`. Endpoints `PATCH /ordenes/:id/entregar` y `PATCH /ordenes/:id/cancelar`.

- [ ] **Step 1: Quitar `estado` de `ActualizarOrdenDto`**

El archivo completo queda así:

```ts
import { PartialType } from '@nestjs/mapped-types';
import { CrearOrdenDto } from './crear-orden.dto';

export class ActualizarOrdenDto extends PartialType(CrearOrdenDto) {}
```

Con `forbidNonWhitelisted: true` activo en el `ValidationPipe` global, mandar `estado` en el cuerpo pasa a responder `400`. Eso cierra el salto de `RECIBIDA` a `ENTREGADA` por el PATCH genérico.

- [ ] **Step 2: Quitar `orden_id` de `ActualizarTrabajoDto`**

Un trabajo que se mudara de orden dejaría mal derivadas a las dos. El archivo completo:

```ts
import { OmitType, PartialType } from '@nestjs/mapped-types';
import { CrearTrabajoDto } from './crear-trabajo.dto';

export class ActualizarTrabajoDto extends PartialType(
  OmitType(CrearTrabajoDto, ['orden_id'] as const),
) {}
```

- [ ] **Step 3: Añadir `entregar` y `cancelar` a `OrdenesService`**

Ampliar el import de `@nestjs/common` con `ConflictException`, y añadir los imports de `estado-orden` y del enum:

```ts
import { ConflictException, Injectable, NotFoundException } from '@nestjs/common';
import { EstadoOrden } from '../common/enums/estados.enum';
import { puedeCancelar, puedeEntregar } from './estado-orden';
```

Y los dos métodos, después de `actualizar`:

```ts
  async entregar(id: string) {
    const orden = await this.obtenerPorId(id);

    if (!puedeEntregar(orden.estado)) {
      throw new ConflictException(
        `No se puede entregar una orden en estado ${orden.estado}: primero deben completarse todos sus trabajos`,
      );
    }

    orden.estado = EstadoOrden.ENTREGADA;
    return await this.ordenRepository.save(orden);
  }

  async cancelar(id: string) {
    const orden = await this.obtenerPorId(id);

    if (!puedeCancelar(orden.estado)) {
      throw new ConflictException(
        `La orden ${orden.numero_orden} ya está ${orden.estado} y no se puede cancelar`,
      );
    }

    orden.estado = EstadoOrden.CANCELADA;
    return await this.ordenRepository.save(orden);
  }
```

Se usa `409 Conflict` y no `400` a propósito: el cuerpo del request es válido, lo que no admite la operación es el estado del recurso.

- [ ] **Step 4: Añadir los endpoints a `OrdenesController`**

Después del `@Patch(':id')` existente:

```ts
  @Patch(':id/entregar')
  @Roles(NombreRol.ADMINISTRADOR, NombreRol.JEFE_TALLER, NombreRol.ASESOR)
  async entregar(@Param('id') id: string) {
    return await this.ordenesService.entregar(id);
  }

  @Patch(':id/cancelar')
  @Roles(NombreRol.ADMINISTRADOR, NombreRol.JEFE_TALLER)
  async cancelar(@Param('id') id: string) {
    return await this.ordenesService.cancelar(id);
  }
```

El orden de declaración no importa: `/ordenes/abc/entregar` tiene dos segmentos y nunca coincide con `/ordenes/:id`, que tiene uno.

- [ ] **Step 5: Verificar contra la API**

Con la API levantada y un token de `jefe@taller.com`, sobre una orden recién creada (estado `RECIBIDA`):

```bash
curl -s -o /dev/null -w '%{http_code}\n' -X PATCH \
  http://localhost:3001/ordenes/$ORDEN_ID/entregar -H "Authorization: Bearer $TOKEN"
```
Esperado: `409`.

```bash
curl -s -o /dev/null -w '%{http_code}\n' -X PATCH \
  http://localhost:3001/ordenes/$ORDEN_ID -H "Authorization: Bearer $TOKEN" \
  -H 'Content-Type: application/json' -d '{"estado":"ENTREGADA"}'
```
Esperado: `400`, con el mensaje de propiedad no permitida.

```bash
curl -s -o /dev/null -w '%{http_code}\n' -X PATCH \
  http://localhost:3001/ordenes/$ORDEN_ID/cancelar -H "Authorization: Bearer $TOKEN"
```
Esperado: `200`. Repetir la misma llamada y esperar `409`.

- [ ] **Step 6: Commit**

```bash
git add backend/src/ordenes backend/src/trabajos/dto/actualizar-trabajo.dto.ts
git commit -m "feat: entregar y cancelar como acciones con transición validada"
```

---

### Task 6: Derivación transaccional del estado de la orden

**Files:**
- Modify: `backend/src/trabajos/trabajos.service.ts` (completo, es el corazón del corte)

**Interfaces:**
- Consumes: `derivarEstado` y `esTerminal` de la Task 4.
- Produces: `TrabajosService.crear`, `.actualizarEstado` y `.eliminar` pasan a ser transaccionales y a sincronizar el estado de la orden. Sus firmas públicas no cambian, así que los controladores quedan igual.

- [ ] **Step 1: Añadir el helper transaccional a `TrabajosService`**

Ampliar los imports:

```ts
import { ConflictException, ForbiddenException, Injectable, NotFoundException } from '@nestjs/common';
import { InjectRepository } from '@nestjs/typeorm';
import { DataSource, EntityManager, Repository } from 'typeorm';
import { Orden } from '../ordenes/entities/orden.entity';
import { derivarEstado, esTerminal } from '../ordenes/estado-orden';
```

Inyectar el `DataSource` en el constructor:

```ts
  constructor(
    @InjectRepository(Trabajo)
    private readonly trabajoRepository: Repository<Trabajo>,
    private readonly dataSource: DataSource,
  ) {}
```

Y añadir los dos métodos privados:

```ts
  /**
   * Envuelve una operación sobre los trabajos de una orden en una transacción
   * que bloquea la fila de la orden, y resincroniza su estado al terminar.
   *
   * El bloqueo no es decorativo: si dos mecánicos completan a la vez los dos
   * últimos trabajos, sin él cada transacción ve al otro trabajo todavía sin
   * terminar, ambas concluyen "sigue en proceso", y la orden queda EN_PROCESO
   * con todo completado.
   */
  private async conOrdenBloqueada<T>(
    ordenId: string,
    operacion: (manager: EntityManager) => Promise<T>,
  ): Promise<T> {
    return await this.dataSource.transaction(async (manager) => {
      const orden = await manager
        .createQueryBuilder(Orden, 'orden')
        .setLock('pessimistic_write')
        .where('orden.id = :id', { id: ordenId })
        .getOne();

      if (!orden) {
        throw new NotFoundException(`No existe la orden con id ${ordenId}`);
      }

      if (esTerminal(orden.estado)) {
        throw new ConflictException(
          `La orden ${orden.numero_orden} está ${orden.estado} y ya no admite cambios en sus trabajos`,
        );
      }

      const resultado = await operacion(manager);
      await this.sincronizarEstadoOrden(manager, orden);
      return resultado;
    });
  }

  private async sincronizarEstadoOrden(manager: EntityManager, orden: Orden): Promise<void> {
    const trabajos = await manager.find(Trabajo, {
      where: { orden: { id: orden.id } },
      select: { id: true, estado: true },
    });

    const derivado = derivarEstado(trabajos.map((trabajo) => trabajo.estado));

    if (derivado !== orden.estado) {
      await manager.update(Orden, { id: orden.id }, { estado: derivado });
    }
  }
```

- [ ] **Step 2: Pasar `crear` por el helper**

```ts
  async crear(dto: CrearTrabajoDto, usuarioId: string) {
    return await this.conOrdenBloqueada(dto.orden_id, async (manager) => {
      const nuevoTrabajo = manager.create(Trabajo, {
        titulo: dto.titulo,
        descripcion: dto.descripcion,
        prioridad: dto.prioridad,
        fecha_limite: dto.fecha_limite,
        orden: { id: dto.orden_id } as any,
        asignado_a: dto.asignado_a_id ? ({ id: dto.asignado_a_id } as any) : undefined,
        creado_por: { id: usuarioId } as any,
      });

      return await manager.save(nuevoTrabajo);
    });
  }
```

Crear un trabajo en una orden `FINALIZADA` la devuelve a `RECIBIDA` o `EN_PROCESO` según corresponda, que es el comportamiento correcto: apareció trabajo nuevo.

- [ ] **Step 3: Pasar `actualizarEstado` por el helper**

El trabajo se relee dentro de la transacción, ya con la orden bloqueada, para que la comprobación de asignación y la escritura ocurran sobre datos consistentes:

```ts
  async actualizarEstado(
    id: string,
    estado: EstadoTrabajo,
    usuarioId: string,
    rolesUsuario: string[],
  ) {
    const trabajo = await this.obtenerPorId(id);

    return await this.conOrdenBloqueada(trabajo.orden.id, async (manager) => {
      const actual = await manager.findOne(Trabajo, {
        where: { id },
        relations: { asignado_a: true },
      });

      if (!actual) {
        throw new NotFoundException(`No existe el trabajo con id ${id}`);
      }

      const esSupervisor =
        rolesUsuario.includes(NombreRol.ADMINISTRADOR) ||
        rolesUsuario.includes(NombreRol.JEFE_TALLER);

      if (!esSupervisor && actual.asignado_a?.id !== usuarioId) {
        throw new ForbiddenException(
          'Solo puede cambiar el estado de los trabajos asignados a usted',
        );
      }

      actual.estado = estado;
      return await manager.save(actual);
    });
  }
```

- [ ] **Step 4: Pasar `eliminar` por el helper**

```ts
  async eliminar(id: string) {
    const trabajo = await this.obtenerPorId(id);

    return await this.conOrdenBloqueada(trabajo.orden.id, async (manager) => {
      await manager.delete(Trabajo, { id });
      return { mensaje: 'Trabajo eliminado correctamente' };
    });
  }
```

Eliminar el último trabajo pendiente puede completar la orden, por eso también pasa por la sincronización.

- [ ] **Step 5: Verificar la derivación de punta a punta**

Con la API levantada, token de `jefe@taller.com` y una orden nueva:

1. Crear dos trabajos en la orden. Consultar `GET /ordenes/:id` → estado `RECIBIDA`.
2. Mover el primero a `EN_PROCESO` (`PATCH /trabajos/:id/estado`). Consultar la orden → `EN_PROCESO`.
3. Mover ambos a `COMPLETADO`. Consultar la orden → `FINALIZADA`.
4. Devolver uno a `EN_PROCESO`. Consultar la orden → `EN_PROCESO`.
5. Volver a completar ambos, entregar la orden (`PATCH /ordenes/:id/entregar` → `200`).
6. Intentar mover un trabajo → `409`, con el mensaje de que la orden ya no admite cambios.

- [ ] **Step 6: Correr toda la suite unitaria**

```bash
cd backend && npm test
```

Esperado: PASS. Si `app.e2e-spec.ts` aparece aquí es un error de configuración: los e2e viven en otro runner.

- [ ] **Step 7: Commit**

```bash
git add backend/src/trabajos/trabajos.service.ts
git commit -m "feat: derivar el estado de la orden de sus trabajos dentro de una transacción"
```

---

### Task 7: Frontend de estados de orden

**Files:**
- Modify: `frontend/src/app/core/services/orden.ts`
- Modify: `frontend/src/app/features/ordenes/formulario-orden/formulario-orden.ts`
- Modify: `frontend/src/app/features/ordenes/formulario-orden/formulario-orden.html:115-122`
- Modify: `frontend/src/app/features/ordenes/detalle-orden/detalle-orden.ts`
- Modify: `frontend/src/app/features/ordenes/detalle-orden/detalle-orden.html:13`
- Modify: `frontend/src/app/features/trabajos/tablero-kanban/tablero-kanban.ts`

**Interfaces:**
- Consumes: `PATCH /ordenes/:id/entregar` y `PATCH /ordenes/:id/cancelar` de la Task 5.
- Produces: `OrdenService.entregar(id)` y `OrdenService.cancelar(id)`, ambos `Observable<Orden>`. El output `estadoCambiado` de `TableroKanban`.

- [ ] **Step 1: Añadir los métodos al servicio**

En `frontend/src/app/core/services/orden.ts`, junto a `actualizar`:

```ts
  entregar(id: string): Observable<Orden> {
    return this.http.patch<Orden>(`${this.url}/${id}/entregar`, {});
  }

  cancelar(id: string): Observable<Orden> {
    return this.http.patch<Orden>(`${this.url}/${id}/cancelar`, {});
  }
```

- [ ] **Step 2: Sacar `estado` del formulario de orden**

En `formulario-orden.ts`:
- Quitar `estado: ['RECIBIDA'],` del `FormGroup`.
- Quitar `estado: orden.estado,` del `patchValue` de `cargarOrden`.
- Quitar la línea `datos.estado = valores.estado;` de la rama de edición de `enviar()`.
- Quitar las propiedades `estadosDisponibles` y `etiquetas`, que quedan sin uso.
- Quitar el import `import { ESTADOS_ORDEN, ETIQUETA_ESTADO_ORDEN } from '../../../core/models/estados';`.

En `formulario-orden.html`, borrar el bloque completo del selector de estado (el `<label for="estado">`, el `<select formControlName="estado">` con su `@for`, y el `<div>` de columna que los envuelve).

Si queda alguno de estos campos, la API responde `400` por `forbidNonWhitelisted`.

- [ ] **Step 3: Emitir un evento desde el Kanban al cambiar de estado**

En `tablero-kanban.ts`, añadir el output junto al que ya existe:

```ts
  readonly estadoCambiado = output<void>();
```

Y emitirlo dentro del `next` de `mover`, después de actualizar la lista local:

```ts
        this.estadoCambiado.emit();
```

- [ ] **Step 4: Añadir las acciones al detalle de orden**

En `detalle-orden.ts`, añadir los imports de `ROLES` (ya está) y los métodos:

```ts
  readonly procesando = signal<boolean>(false);

  puedeEntregar(): boolean {
    return (
      this.orden()?.estado === 'FINALIZADA' &&
      this.tokenService.tieneRol(ROLES.ADMINISTRADOR, ROLES.JEFE_TALLER, ROLES.ASESOR)
    );
  }

  puedeCancelar(): boolean {
    const estado = this.orden()?.estado;
    return (
      estado !== undefined &&
      !['ENTREGADA', 'CANCELADA'].includes(estado) &&
      this.tokenService.tieneRol(ROLES.ADMINISTRADOR, ROLES.JEFE_TALLER)
    );
  }

  entregar(): void {
    this.ejecutarAccion(this.ordenService.entregar(this.orden()!.id));
  }

  cancelar(): void {
    if (!confirm('¿Seguro que desea cancelar esta orden? No se podrá revertir.')) return;
    this.ejecutarAccion(this.ordenService.cancelar(this.orden()!.id));
  }

  private ejecutarAccion(peticion: Observable<Orden>): void {
    this.procesando.set(true);
    this.mensajeError.set(null);

    peticion.subscribe({
      next: (actualizada) => {
        this.orden.set(actualizada);
        this.procesando.set(false);
      },
      error: (error) => {
        this.procesando.set(false);
        this.mensajeError.set(error.error?.message ?? 'No se pudo completar la acción');
      },
    });
  }

  refrescarOrden(): void {
    const id = this.orden()?.id;
    if (id) this.cargarOrden(id);
  }
```

`cargarOrden` es hoy `private`: cambiarla a `private` sigue estando bien porque `refrescarOrden` la envuelve. Añadir `import { Observable } from 'rxjs';`.

- [ ] **Step 5: Añadir los botones y el enganche del Kanban en la plantilla**

En `detalle-orden.html`, junto al `<app-badge-estado>` de la línea 13:

```html
@if (puedeEntregar()) {
  <button class="btn btn-sm btn-success ms-2" [disabled]="procesando()" (click)="entregar()">
    Entregar
  </button>
}
@if (puedeCancelar()) {
  <button class="btn btn-sm btn-outline-danger ms-2" [disabled]="procesando()" (click)="cancelar()">
    Cancelar
  </button>
}
```

Y en el `<app-tablero-kanban>` de la misma plantilla, añadir el enganche al output nuevo:

```html
(estadoCambiado)="refrescarOrden()"
```

- [ ] **Step 6: Verificar en el navegador**

Levantar `npm start` en `frontend/` con la API corriendo. Entrar como `jefe@taller.com`, abrir una orden con trabajos y comprobar:
- Mover una tarjeta del Kanban actualiza el badge de estado de la orden sin recargar la página.
- Con todos los trabajos completados aparece el botón "Entregar"; antes no.
- Entrando como `mecanico@taller.com` no aparece ni "Entregar" ni "Cancelar".
- El formulario de edición de orden ya no muestra el selector de estado y guarda sin error.

- [ ] **Step 7: Commit**

```bash
git add frontend/src/app
git commit -m "feat: acciones de entrega y cancelación en el detalle de orden"
```

---

# CORTE 4 — Almacenamiento

### Task 8: Módulo de almacenamiento

**Files:**
- Create: `backend/src/almacenamiento/almacenamiento.interface.ts`
- Create: `backend/src/almacenamiento/almacenamiento-disco.service.ts`
- Create: `backend/src/almacenamiento/almacenamiento-disco.spec.ts`
- Create: `backend/src/almacenamiento/almacenamiento-s3.service.ts`
- Create: `backend/src/almacenamiento/almacenamiento.module.ts`
- Modify: `backend/package.json` (SDK de S3)
- Modify: `.env.example`

**Interfaces:**
- Consumes: nada del resto del plan.
- Produces:
  - `ALMACENAMIENTO` — token de inyección (string constante).
  - `interface ArchivoEntrante { buffer: Buffer; nombreOriginal: string; mime: string }`
  - `interface AlmacenamientoArchivos` con `guardar(archivo: ArchivoEntrante): Promise<{ clave: string }>`, `eliminar(clave: string): Promise<void>` y `obtenerUrl(clave: string): Promise<string>`.
  - `AlmacenamientoModule`, que exporta el proveedor `ALMACENAMIENTO`.

- [ ] **Step 1: Instalar el SDK de S3**

```bash
cd backend && npm install @aws-sdk/client-s3 @aws-sdk/s3-request-presigner
```

- [ ] **Step 2: Escribir la interfaz**

Crear `backend/src/almacenamiento/almacenamiento.interface.ts`:

```ts
export const ALMACENAMIENTO = 'ALMACENAMIENTO';

export interface ArchivoEntrante {
  buffer: Buffer;
  nombreOriginal: string;
  mime: string;
}

export interface AlmacenamientoArchivos {
  /** Guarda el archivo y devuelve la clave con la que se lo recupera. */
  guardar(archivo: ArchivoEntrante): Promise<{ clave: string }>;

  /** Borra el objeto. No falla si la clave ya no existe. */
  eliminar(clave: string): Promise<void>;

  /** URL con la que el navegador puede abrir el archivo. */
  obtenerUrl(clave: string): Promise<string>;
}
```

- [ ] **Step 3: Escribir los tests del driver de disco**

Crear `backend/src/almacenamiento/almacenamiento-disco.spec.ts`:

```ts
import { mkdtemp, readFile, rm } from 'fs/promises';
import { tmpdir } from 'os';
import { join } from 'path';
import { AlmacenamientoDisco } from './almacenamiento-disco.service';

describe('AlmacenamientoDisco', () => {
  let directorio: string;
  let almacenamiento: AlmacenamientoDisco;

  beforeEach(async () => {
    directorio = await mkdtemp(join(tmpdir(), 'tallerpro-'));
    almacenamiento = new AlmacenamientoDisco(directorio, 'http://localhost:3001');
  });

  afterEach(async () => {
    await rm(directorio, { recursive: true, force: true });
  });

  const archivo = {
    buffer: Buffer.from('contenido de prueba'),
    nombreOriginal: 'foto.jpg',
    mime: 'image/jpeg',
  };

  it('guarda el archivo y devuelve una clave que conserva la extensión', async () => {
    const { clave } = await almacenamiento.guardar(archivo);

    expect(clave).toMatch(/\.jpg$/);
    expect(await readFile(join(directorio, clave), 'utf8')).toBe('contenido de prueba');
  });

  it('genera una clave distinta para cada archivo', async () => {
    const primera = await almacenamiento.guardar(archivo);
    const segunda = await almacenamiento.guardar(archivo);

    expect(primera.clave).not.toBe(segunda.clave);
  });

  it('arma la URL pública a partir de la clave', async () => {
    const { clave } = await almacenamiento.guardar(archivo);

    expect(await almacenamiento.obtenerUrl(clave)).toBe(
      `http://localhost:3001/uploads/${clave}`,
    );
  });

  it('elimina el archivo del disco', async () => {
    const { clave } = await almacenamiento.guardar(archivo);

    await almacenamiento.eliminar(clave);

    await expect(readFile(join(directorio, clave))).rejects.toThrow();
  });

  it('eliminar una clave inexistente no lanza', async () => {
    await expect(almacenamiento.eliminar('no-existe.jpg')).resolves.toBeUndefined();
  });
});
```

- [ ] **Step 4: Correr los tests para verificar que fallan**

```bash
cd backend && npm test -- almacenamiento-disco
```

Esperado: FAIL, `Cannot find module './almacenamiento-disco.service'`.

- [ ] **Step 5: Escribir el driver de disco**

Crear `backend/src/almacenamiento/almacenamiento-disco.service.ts`:

```ts
import { mkdir, rm, writeFile } from 'fs/promises';
import { randomUUID } from 'crypto';
import { extname, join } from 'path';
import { AlmacenamientoArchivos, ArchivoEntrante } from './almacenamiento.interface';

/** Driver de desarrollo. En producción el disco de Render es efímero. */
export class AlmacenamientoDisco implements AlmacenamientoArchivos {
  constructor(
    private readonly directorio: string,
    private readonly urlBase: string,
  ) {}

  async guardar({ buffer, nombreOriginal }: ArchivoEntrante): Promise<{ clave: string }> {
    await mkdir(this.directorio, { recursive: true });

    const clave = `${randomUUID()}${extname(nombreOriginal)}`;
    await writeFile(join(this.directorio, clave), buffer);

    return { clave };
  }

  async eliminar(clave: string): Promise<void> {
    await rm(join(this.directorio, clave), { force: true });
  }

  async obtenerUrl(clave: string): Promise<string> {
    return `${this.urlBase}/uploads/${clave}`;
  }
}
```

- [ ] **Step 6: Correr los tests para verificar que pasan**

```bash
cd backend && npm test -- almacenamiento-disco
```

Esperado: PASS, 5 tests.

- [ ] **Step 7: Escribir el driver de S3**

Crear `backend/src/almacenamiento/almacenamiento-s3.service.ts`. No lleva test unitario: probarlo de verdad requiere un bucket, y un test contra mocks del SDK solo verificaría que el SDK se llama como se lo llama. Se verifica a mano en la Task 9.

```ts
import {
  DeleteObjectCommand,
  GetObjectCommand,
  PutObjectCommand,
  S3Client,
} from '@aws-sdk/client-s3';
import { getSignedUrl } from '@aws-sdk/s3-request-presigner';
import { randomUUID } from 'crypto';
import { extname } from 'path';
import { AlmacenamientoArchivos, ArchivoEntrante } from './almacenamiento.interface';

const VIGENCIA_URL_SEGUNDOS = 300;

export interface ConfiguracionS3 {
  endpoint: string;
  region: string;
  bucket: string;
  accessKeyId: string;
  secretAccessKey: string;
}

export class AlmacenamientoS3 implements AlmacenamientoArchivos {
  private readonly cliente: S3Client;

  constructor(private readonly config: ConfiguracionS3) {
    this.cliente = new S3Client({
      endpoint: config.endpoint,
      region: config.region,
      credentials: {
        accessKeyId: config.accessKeyId,
        secretAccessKey: config.secretAccessKey,
      },
      // R2, Supabase y MinIO exponen el bucket como parte de la ruta,
      // no como subdominio. S3 lo acepta igual.
      forcePathStyle: true,
    });
  }

  async guardar({ buffer, nombreOriginal, mime }: ArchivoEntrante): Promise<{ clave: string }> {
    const clave = `${randomUUID()}${extname(nombreOriginal)}`;

    await this.cliente.send(
      new PutObjectCommand({
        Bucket: this.config.bucket,
        Key: clave,
        Body: buffer,
        ContentType: mime,
      }),
    );

    return { clave };
  }

  async eliminar(clave: string): Promise<void> {
    await this.cliente.send(
      new DeleteObjectCommand({ Bucket: this.config.bucket, Key: clave }),
    );
  }

  async obtenerUrl(clave: string): Promise<string> {
    return await getSignedUrl(
      this.cliente,
      new GetObjectCommand({ Bucket: this.config.bucket, Key: clave }),
      { expiresIn: VIGENCIA_URL_SEGUNDOS },
    );
  }
}
```

- [ ] **Step 8: Escribir el módulo con el factory**

Crear `backend/src/almacenamiento/almacenamiento.module.ts`:

```ts
import { Global, Module } from '@nestjs/common';
import { ConfigModule, ConfigService } from '@nestjs/config';
import { join } from 'path';

import { ALMACENAMIENTO } from './almacenamiento.interface';
import { AlmacenamientoDisco } from './almacenamiento-disco.service';
import { AlmacenamientoS3 } from './almacenamiento-s3.service';

@Global()
@Module({
  imports: [ConfigModule],
  providers: [
    {
      provide: ALMACENAMIENTO,
      inject: [ConfigService],
      useFactory: (config: ConfigService) => {
        if (config.get<string>('STORAGE_DRIVER') === 's3') {
          return new AlmacenamientoS3({
            endpoint: config.get<string>('S3_ENDPOINT')!,
            region: config.get<string>('S3_REGION') ?? 'auto',
            bucket: config.get<string>('S3_BUCKET')!,
            accessKeyId: config.get<string>('S3_ACCESS_KEY_ID')!,
            secretAccessKey: config.get<string>('S3_SECRET_ACCESS_KEY')!,
          });
        }

        return new AlmacenamientoDisco(
          join(__dirname, '..', '..', 'uploads'),
          config.get<string>('API_URL') ?? 'http://localhost:3001',
        );
      },
    },
  ],
  exports: [ALMACENAMIENTO],
})
export class AlmacenamientoModule {}
```

Es `@Global()` porque lo consumen `AdjuntosModule`, `TrabajosModule` y `OrdenesModule`, y así se evita repetir el import en los tres.

La ruta del driver de disco es `join(__dirname, '..', '..', 'uploads')` porque en tiempo de ejecución `__dirname` es `dist/almacenamiento`, y el directorio `uploads/` está en la raíz de `backend/`. Es la misma que ya usa `ServeStaticModule`.

- [ ] **Step 9: Registrar el módulo y documentar las variables**

En `backend/src/app.module.ts`, añadir `AlmacenamientoModule` a los `imports`, antes de `AdjuntosModule`.

En `.env.example`, al final:

```
API_URL=http://localhost:3001
STORAGE_DRIVER=disco
S3_ENDPOINT=
S3_REGION=auto
S3_BUCKET=
S3_ACCESS_KEY_ID=
S3_SECRET_ACCESS_KEY=
```

Copiar las mismas líneas al `.env` local (que no se versiona) para que el arranque encuentre `API_URL`.

- [ ] **Step 10: Verificar que la aplicación arranca**

```bash
cd backend && npm run start:dev
```

Esperado: arranca sin errores de inyección. Todavía nadie consume `ALMACENAMIENTO`.

- [ ] **Step 11: Commit**

```bash
git add backend/src/almacenamiento backend/src/app.module.ts backend/package.json \
        backend/package-lock.json .env.example
git commit -m "feat: abstraer el almacenamiento de archivos con drivers de disco y S3"
```

---

### Task 9: Migrar los adjuntos a la interfaz de almacenamiento

**Files:**
- Create: `backend/src/migrations/<timestamp>-AdjuntosClaveDeObjeto.ts` (a mano)
- Modify: `backend/src/adjuntos/entities/adjunto.entity.ts`
- Modify: `backend/src/adjuntos/adjuntos.service.ts`
- Modify: `backend/src/adjuntos/adjuntos.controller.ts`
- Modify: `backend/src/adjuntos/adjuntos.module.ts`
- Modify: `backend/src/app.module.ts` (`ServeStaticModule` condicional)

**Interfaces:**
- Consumes: `ALMACENAMIENTO` y `AlmacenamientoArchivos` de la Task 8.
- Produces: `AdjuntosService.guardar` y `.obtenerPorTrabajo` devuelven objetos con un campo `url: string` ya resuelto. `AdjuntosService.eliminarPorTrabajos(trabajoIds: string[], manager: EntityManager): Promise<void>` para la Task 10.

- [ ] **Step 1: Escribir la migración del esquema de adjuntos**

Las dos columnas guardaban variantes de lo mismo —`nombre_archivo` el nombre generado, `ruta` la ruta en disco— y con la abstracción la única identidad que importa es la clave del objeto. `nombre_original` se queda: es lo que ve el usuario.

Crear `backend/src/migrations/<timestamp>-AdjuntosClaveDeObjeto.ts`:

```ts
import { MigrationInterface, QueryRunner } from 'typeorm';

export class AdjuntosClaveDeObjeto<TIMESTAMP> implements MigrationInterface {
  public async up(queryRunner: QueryRunner): Promise<void> {
    await queryRunner.query(`ALTER TABLE "adjuntos" RENAME COLUMN "ruta" TO "clave"`);
    await queryRunner.query(`ALTER TABLE "adjuntos" DROP COLUMN "nombre_archivo"`);
  }

  public async down(queryRunner: QueryRunner): Promise<void> {
    await queryRunner.query(
      `ALTER TABLE "adjuntos" ADD COLUMN "nombre_archivo" character varying(255) NOT NULL DEFAULT ''`,
    );
    await queryRunner.query(`ALTER TABLE "adjuntos" RENAME COLUMN "clave" TO "ruta"`);
  }
}
```

- [ ] **Step 2: Actualizar la entidad**

En `backend/src/adjuntos/entities/adjunto.entity.ts`, borrar el bloque de `nombre_archivo` y renombrar `ruta`:

```ts
  @Column({ type: 'varchar', length: 500 })
  clave!: string;
```

- [ ] **Step 3: Reescribir `AdjuntosService`**

```ts
import { Inject, Injectable, Logger, NotFoundException } from '@nestjs/common';
import { InjectRepository } from '@nestjs/typeorm';
import { EntityManager, In, Repository } from 'typeorm';

import { Adjunto } from './entities/adjunto.entity';
import { ALMACENAMIENTO, AlmacenamientoArchivos } from '../almacenamiento/almacenamiento.interface';

@Injectable()
export class AdjuntosService {
  private readonly logger = new Logger('AdjuntosService');

  constructor(
    @InjectRepository(Adjunto)
    private readonly adjuntoRepository: Repository<Adjunto>,
    @Inject(ALMACENAMIENTO)
    private readonly almacenamiento: AlmacenamientoArchivos,
  ) {}

  async guardar(archivo: Express.Multer.File, trabajoId: string, usuarioId: string) {
    const { clave } = await this.almacenamiento.guardar({
      buffer: archivo.buffer,
      nombreOriginal: archivo.originalname,
      mime: archivo.mimetype,
    });

    const nuevoAdjunto = this.adjuntoRepository.create({
      nombre_original: archivo.originalname,
      clave,
      tipo_mime: archivo.mimetype,
      tamano: archivo.size,
      trabajo: { id: trabajoId } as any,
      subido_por: { id: usuarioId } as any,
    });

    const guardado = await this.adjuntoRepository.save(nuevoAdjunto);

    return { ...guardado, url: await this.almacenamiento.obtenerUrl(clave) };
  }

  async obtenerPorTrabajo(trabajoId: string) {
    const adjuntos = await this.adjuntoRepository.find({
      where: { trabajo: { id: trabajoId } },
      relations: { subido_por: true },
      select: {
        id: true,
        nombre_original: true,
        clave: true,
        tipo_mime: true,
        tamano: true,
        created_at: true,
        subido_por: { id: true, username: true, nombres: true, apellidos: true },
      },
      order: { created_at: 'DESC' },
    });

    // Firmar es un cálculo local del SDK, sin viaje a S3, así que resolver
    // la URL de cada adjunto en el listado sale prácticamente gratis y le
    // ahorra al frontend una petición por archivo.
    return await Promise.all(
      adjuntos.map(async (adjunto) => ({
        ...adjunto,
        url: await this.almacenamiento.obtenerUrl(adjunto.clave),
      })),
    );
  }

  async eliminar(id: string) {
    const adjunto = await this.adjuntoRepository.findOne({ where: { id } });
    if (!adjunto) {
      throw new NotFoundException(`No existe el adjunto con id ${id}`);
    }

    await this.borrarDelAlmacenamiento(adjunto.clave);
    await this.adjuntoRepository.remove(adjunto);

    return { mensaje: 'Adjunto eliminado correctamente' };
  }

  /**
   * Borra del almacenamiento los adjuntos de los trabajos indicados y sus
   * filas. Lo usan los borrados en cascada de trabajos y órdenes.
   */
  async eliminarPorTrabajos(trabajoIds: string[], manager: EntityManager): Promise<void> {
    if (trabajoIds.length === 0) return;

    const adjuntos = await manager.find(Adjunto, {
      where: { trabajo: { id: In(trabajoIds) } },
      select: { id: true, clave: true },
    });

    for (const adjunto of adjuntos) {
      await this.borrarDelAlmacenamiento(adjunto.clave);
    }
  }

  /**
   * Un objeto huérfano es un problema menor que una orden que no se puede
   * eliminar, así que un fallo del almacenamiento se registra y no corta.
   */
  private async borrarDelAlmacenamiento(clave: string): Promise<void> {
    try {
      await this.almacenamiento.eliminar(clave);
    } catch (error) {
      this.logger.error(`No se pudo eliminar del almacenamiento la clave ${clave}`, error);
    }
  }
}
```

`eliminarPorTrabajos` solo borra los objetos: las filas se van solas por el `ON DELETE CASCADE` que ya tiene `adjuntos.trabajo_id`.

- [ ] **Step 4: Pasar Multer a memoryStorage**

En `backend/src/adjuntos/adjuntos.controller.ts`, cambiar el import de multer:

```ts
import { memoryStorage } from 'multer';
```

Quitar los imports de `diskStorage`, `extname` y `randomUUID`, que dejan de usarse, y reemplazar el bloque `storage` del `FileInterceptor`:

```ts
      storage: memoryStorage(),
```

El `fileFilter` y `limits` se quedan **exactamente como están**: el filtro sigue siendo el lugar correcto para validar el tipo, y el límite de 5 MB ahora además acota cuánta memoria puede ocupar un request.

- [ ] **Step 5: Exportar el servicio desde su módulo**

En `backend/src/adjuntos/adjuntos.module.ts`, añadir `AdjuntosService` al array `exports` (crear la propiedad si no existe). La Task 10 lo necesita desde `TrabajosModule` y `OrdenesModule`.

- [ ] **Step 6: Montar `ServeStaticModule` solo con el driver de disco**

En `backend/src/app.module.ts`, reemplazar la entrada fija de `ServeStaticModule` en `imports` por un spread condicional:

```ts
    ...(process.env.STORAGE_DRIVER === 's3'
      ? []
      : [
          ServeStaticModule.forRoot({
            rootPath: join(__dirname, '..', 'uploads'),
            serveRoot: '/uploads',
          }),
        ]),
```

Con el driver S3 la ruta `/uploads` deja de existir y todo acceso pasa por URL firmada. Esto cierra el hueco de que hoy cualquiera con la URL ve las fotos del vehículo de un cliente.

Se lee de `process.env` y no del `ConfigService` porque el array de `imports` se evalúa antes de que exista el contenedor de inyección. `ConfigModule.forRoot({ isGlobal: true })` ya pobló `process.env` con el `.env` en ese punto.

- [ ] **Step 7: Verificar con el driver de disco**

Arrancar la API y el frontend. Subir un JPG a un trabajo y comprobar:

```bash
curl -s http://localhost:3001/adjuntos/trabajo/$TRABAJO_ID -H "Authorization: Bearer $TOKEN"
```

Esperado: cada elemento trae `url` con la forma `http://localhost:3001/uploads/<uuid>.jpg`, y abrir esa URL en el navegador muestra el archivo.

- [ ] **Step 8: Verificar con el driver S3**

Crear un bucket de prueba en Cloudflare R2 (o el proveedor elegido), poner las credenciales en el `.env` local con `STORAGE_DRIVER=s3`, reiniciar la API y subir otro archivo. Comprobar:
- El objeto aparece en el bucket.
- La `url` del listado es una URL firmada (lleva `X-Amz-Signature`) y abre el archivo.
- `curl -o /dev/null -w '%{http_code}' http://localhost:3001/uploads/cualquier-cosa.jpg` devuelve `404`.

Volver a `STORAGE_DRIVER=disco` al terminar.

- [ ] **Step 9: Verificar que las validaciones siguen en pie**

Intentar subir un archivo `.exe` → `400` con "Solo se permiten archivos JPG, PNG o PDF". Intentar subir uno de más de 5 MB → error de límite.

- [ ] **Step 10: Commit**

```bash
git add backend/src/adjuntos backend/src/migrations backend/src/app.module.ts
git commit -m "feat: guardar los adjuntos por la interfaz de almacenamiento y firmarlos en producción"
```

---

### Task 10: Borrado en cascada sin archivos huérfanos

**Files:**
- Modify: `backend/src/trabajos/trabajos.service.ts` (método `eliminar`)
- Modify: `backend/src/trabajos/trabajos.module.ts` (importa `AdjuntosModule`)
- Modify: `backend/src/ordenes/ordenes.service.ts` (método `eliminar`)
- Modify: `backend/src/ordenes/ordenes.module.ts` (importa `AdjuntosModule`)

**Interfaces:**
- Consumes: `AdjuntosService.eliminarPorTrabajos(trabajoIds, manager)` de la Task 9; `conOrdenBloqueada` de la Task 6.
- Produces: nada nuevo. Cambia el comportamiento interno de dos métodos existentes.

- [ ] **Step 1: Limpiar el almacenamiento al eliminar un trabajo**

En `trabajos.module.ts`, añadir `AdjuntosModule` a `imports`. Inyectar el servicio en `TrabajosService`:

```ts
import { AdjuntosService } from '../adjuntos/adjuntos.service';
```

```ts
    private readonly adjuntosService: AdjuntosService,
```

Y ampliar `eliminar` para que borre los objetos antes de la fila, dentro de la transacción que ya abre `conOrdenBloqueada`:

```ts
  async eliminar(id: string) {
    const trabajo = await this.obtenerPorId(id);

    return await this.conOrdenBloqueada(trabajo.orden.id, async (manager) => {
      await this.adjuntosService.eliminarPorTrabajos([id], manager);
      await manager.delete(Trabajo, { id });
      return { mensaje: 'Trabajo eliminado correctamente' };
    });
  }
```

- [ ] **Step 2: Limpiar el almacenamiento al eliminar una orden**

`OrdenesService.eliminar` no era transaccional y pasa a serlo. En `ordenes.module.ts`, añadir `AdjuntosModule` a `imports`. En `OrdenesService`, inyectar `DataSource` y `AdjuntosService`:

```ts
import { DataSource, Repository } from 'typeorm';
import { Trabajo } from '../trabajos/entities/trabajo.entity';
import { AdjuntosService } from '../adjuntos/adjuntos.service';
```

```ts
    private readonly dataSource: DataSource,
    private readonly adjuntosService: AdjuntosService,
```

Y reescribir el método:

```ts
  async eliminar(id: string) {
    const orden = await this.obtenerPorId(id);

    await this.dataSource.transaction(async (manager) => {
      const trabajos = await manager.find(Trabajo, {
        where: { orden: { id } },
        select: { id: true },
      });

      await this.adjuntosService.eliminarPorTrabajos(
        trabajos.map((trabajo) => trabajo.id),
        manager,
      );

      await manager.delete(Orden, { id: orden.id });
    });

    return { mensaje: 'Orden eliminada correctamente' };
  }
```

Se usa `manager.delete` y no `remove` porque ya tenemos el id y no hace falta cargar la entidad de nuevo. Las filas de trabajos, comentarios y adjuntos se van solas por los `ON DELETE CASCADE`.

Eliminar una orden sigue permitido en cualquier estado, incluidos `ENTREGADA` y `CANCELADA`: la regla de "orden terminal no admite cambios" alcanza a los trabajos, no a la orden. Es la vía del administrador para dar de baja un registro completo.

- [ ] **Step 3: Verificar que no quedan huérfanos**

Con `STORAGE_DRIVER=disco`:

```bash
ls backend/uploads | wc -l
```

Crear una orden, un trabajo, subirle dos adjuntos, comprobar que el conteo subió en dos, eliminar la orden y comprobar que volvió al valor original.

Repetir eliminando solo el trabajo en vez de la orden.

- [ ] **Step 4: Verificar que no se rompió el arranque**

```bash
cd backend && npm run start:dev
```

Esperado: arranca sin errores de dependencias circulares. `AdjuntosModule` no importa a `TrabajosModule` ni a `OrdenesModule`, así que no hay ciclo.

- [ ] **Step 5: Commit**

```bash
git add backend/src/trabajos backend/src/ordenes
git commit -m "fix: borrar los archivos del almacenamiento al eliminar trabajos y órdenes"
```

---

### Task 11: Frontend de adjuntos

**Files:**
- Modify: `frontend/src/app/core/models/adjunto.model.ts`
- Modify: `frontend/src/app/core/services/adjunto.ts`
- Modify: `frontend/src/app/features/trabajos/detalle-trabajo/detalle-trabajo.ts`
- Modify: `frontend/src/app/features/trabajos/detalle-trabajo/detalle-trabajo.html:61`

**Interfaces:**
- Consumes: el campo `url` que devuelven `GET /adjuntos/trabajo/:trabajoId` y `POST /adjuntos/trabajo/:trabajoId` desde la Task 9.
- Produces: nada que consuman otras tareas.

- [ ] **Step 1: Actualizar el modelo**

En `adjunto.model.ts`, reemplazar `nombre_archivo` por `url`:

```ts
export interface Adjunto {
  id: string;
  nombre_original: string;
  url: string;
  tipo_mime: string;
  tamano: number;
  created_at: string;
  subido_por: Mecanico;
}
```

- [ ] **Step 2: Quitar el armado manual de la URL del servicio**

En `frontend/src/app/core/services/adjunto.ts`, borrar el método `obtenerUrl(nombreArchivo: string)` completo. La URL ahora viene resuelta desde la API y el frontend no debe construirla: con el driver S3 lleva una firma que el navegador no puede generar.

- [ ] **Step 3: Quitar el método del componente**

En `detalle-trabajo.ts`, borrar el método `obtenerUrl(adjunto: Adjunto): string`.

- [ ] **Step 4: Usar el campo directo en la plantilla**

En `detalle-trabajo.html`, línea 61:

```html
              <a [href]="adjunto.url" target="_blank" rel="noopener">
```

- [ ] **Step 5: Verificar en el navegador**

Con la API y el frontend levantados, abrir un trabajo con adjuntos y comprobar que:
- La lista muestra los archivos con su nombre original.
- El enlace abre el archivo en una pestaña nueva.
- Subir un archivo nuevo lo agrega a la lista y su enlace también funciona.
- Eliminar un adjunto como `jefe@taller.com` lo saca de la lista.

- [ ] **Step 6: Compilar el frontend para verificar que no quedan referencias**

```bash
cd frontend && npm run build
```

Esperado: compila sin errores. Si aparece `Property 'nombre_archivo' does not exist`, quedó una referencia sin actualizar.

- [ ] **Step 7: Commit**

```bash
git add frontend/src/app
git commit -m "feat: consumir la URL del adjunto resuelta por la API"
```

---

# CIERRE

### Task 12: Pruebas del flujo, despliegue y documentación

**Files:**
- Delete: `backend/test/app.e2e-spec.ts`
- Delete: `frontend/src/app/app.spec.ts`
- Create: `backend/test/flujo-orden.e2e-spec.ts`
- Modify: `render.yaml`
- Modify: `README.md`
- Modify: `docs/modelo-er.md`
- Modify: `docs/endpoints.http`

**Interfaces:**
- Consumes: todo lo anterior.
- Produces: nada que consuman otras tareas.

- [ ] **Step 1: Borrar los dos tests obsoletos del andamiaje**

`backend/test/app.e2e-spec.ts` espera un `GET /` que devuelva `"Hello World!"`, y este proyecto nunca tuvo un `AppController`. `frontend/src/app/app.spec.ts` busca un `<h1>` con `"Hello, frontend"` que ya no está en `app.html`. Ninguno prueba nada propio y los dos fallan.

```bash
rm backend/test/app.e2e-spec.ts frontend/src/app/app.spec.ts
```

- [ ] **Step 2: Escribir el e2e del flujo completo**

Crear `backend/test/flujo-orden.e2e-spec.ts`. Cubre el recorrido que ninguna prueba unitaria puede cubrir: que la derivación, los guards y las transiciones funcionan juntos sobre una base real.

```ts
import { INestApplication, ValidationPipe } from '@nestjs/common';
import { Test, TestingModule } from '@nestjs/testing';
import request from 'supertest';
import { App } from 'supertest/types';
import { AppModule } from './../src/app.module';

describe('Flujo de una orden (e2e)', () => {
  let app: INestApplication<App>;
  let tokenJefe: string;
  let tokenMecanico: string;
  let idMecanico: string;
  let ordenId: string;
  let trabajoId: string;

  const nuevaOrden = {
    descripcion: 'Cambio de aceite y revisión de frenos',
    fecha_ingreso: '2026-08-08',
    placa: 'E2E-001',
    marca: 'Toyota',
    modelo: 'Yaris',
    cliente_nombre: 'Cliente de prueba',
    cliente_telefono: '999888777',
  };

  const iniciarSesion = async (email: string) => {
    const respuesta = await request(app.getHttpServer())
      .post('/auth/login')
      .send({ email, password: '123456' })
      .expect(201);
    return respuesta.body;
  };

  beforeAll(async () => {
    const moduleFixture: TestingModule = await Test.createTestingModule({
      imports: [AppModule],
    }).compile();

    app = moduleFixture.createNestApplication();
    app.useGlobalPipes(
      new ValidationPipe({ whitelist: true, forbidNonWhitelisted: true, transform: true }),
    );
    await app.init();

    tokenJefe = (await iniciarSesion('jefe@taller.com')).access_token;

    const sesionMecanico = await iniciarSesion('mecanico@taller.com');
    tokenMecanico = sesionMecanico.access_token;
    idMecanico = sesionMecanico.usuario.id;
  });

  afterAll(async () => {
    if (ordenId) {
      const admin = await iniciarSesion('admin@taller.com');
      await request(app.getHttpServer())
        .delete(`/ordenes/${ordenId}`)
        .set('Authorization', `Bearer ${admin.access_token}`);
    }
    await app.close();
  });

  it('crea la orden con un correlativo con formato', async () => {
    const respuesta = await request(app.getHttpServer())
      .post('/ordenes')
      .set('Authorization', `Bearer ${tokenJefe}`)
      .send(nuevaOrden)
      .expect(201);

    ordenId = respuesta.body.id;
    expect(respuesta.body.numero_orden).toMatch(/^ORD-\d{6,}$/);
    expect(respuesta.body.estado).toBe('RECIBIDA');
  });

  it('rechaza mandar el estado por el PATCH genérico', async () => {
    await request(app.getHttpServer())
      .patch(`/ordenes/${ordenId}`)
      .set('Authorization', `Bearer ${tokenJefe}`)
      .send({ estado: 'ENTREGADA' })
      .expect(400);
  });

  it('no entrega una orden que no está finalizada', async () => {
    await request(app.getHttpServer())
      .patch(`/ordenes/${ordenId}/entregar`)
      .set('Authorization', `Bearer ${tokenJefe}`)
      .expect(409);
  });

  it('la orden sigue RECIBIDA con un trabajo pendiente', async () => {
    const creado = await request(app.getHttpServer())
      .post('/trabajos')
      .set('Authorization', `Bearer ${tokenJefe}`)
      .send({
        titulo: 'Cambiar pastillas de freno',
        orden_id: ordenId,
        asignado_a_id: idMecanico,
      })
      .expect(201);

    trabajoId = creado.body.id;

    const orden = await request(app.getHttpServer())
      .get(`/ordenes/${ordenId}`)
      .set('Authorization', `Bearer ${tokenJefe}`)
      .expect(200);

    expect(orden.body.estado).toBe('RECIBIDA');
  });

  it('completar el trabajo deja la orden FINALIZADA', async () => {
    await request(app.getHttpServer())
      .patch(`/trabajos/${trabajoId}/estado`)
      .set('Authorization', `Bearer ${tokenMecanico}`)
      .send({ estado: 'COMPLETADO' })
      .expect(200);

    const orden = await request(app.getHttpServer())
      .get(`/ordenes/${ordenId}`)
      .set('Authorization', `Bearer ${tokenJefe}`)
      .expect(200);

    expect(orden.body.estado).toBe('FINALIZADA');
  });

  it('reabrir el trabajo devuelve la orden a EN_PROCESO', async () => {
    await request(app.getHttpServer())
      .patch(`/trabajos/${trabajoId}/estado`)
      .set('Authorization', `Bearer ${tokenMecanico}`)
      .send({ estado: 'EN_PROCESO' })
      .expect(200);

    const orden = await request(app.getHttpServer())
      .get(`/ordenes/${ordenId}`)
      .set('Authorization', `Bearer ${tokenJefe}`)
      .expect(200);

    expect(orden.body.estado).toBe('EN_PROCESO');
  });

  it('un mecánico ajeno no puede mover el trabajo', async () => {
    const sufijo = Date.now();
    await request(app.getHttpServer())
      .post('/auth/registro')
      .send({
        username: `ajeno${sufijo}`,
        email: `ajeno${sufijo}@taller.com`,
        password: '123456',
        nombres: 'Mecánico',
        apellidos: 'Ajeno',
      })
      .expect(201);

    const ajeno = await iniciarSesion(`ajeno${sufijo}@taller.com`);

    await request(app.getHttpServer())
      .patch(`/trabajos/${trabajoId}/estado`)
      .set('Authorization', `Bearer ${ajeno.access_token}`)
      .send({ estado: 'PENDIENTE' })
      .expect(403);
  });

  it('entrega la orden una vez completada y la cierra a cambios', async () => {
    await request(app.getHttpServer())
      .patch(`/trabajos/${trabajoId}/estado`)
      .set('Authorization', `Bearer ${tokenMecanico}`)
      .send({ estado: 'COMPLETADO' })
      .expect(200);

    await request(app.getHttpServer())
      .patch(`/ordenes/${ordenId}/entregar`)
      .set('Authorization', `Bearer ${tokenJefe}`)
      .expect(200);

    await request(app.getHttpServer())
      .patch(`/trabajos/${trabajoId}/estado`)
      .set('Authorization', `Bearer ${tokenMecanico}`)
      .send({ estado: 'EN_PROCESO' })
      .expect(409);
  });
});
```

**Desvío respecto del spec.** El spec §9 pedía además un `trabajos.service.spec.ts`
unitario para la regla de asignación (`403`) y el rechazo sobre orden terminal
(`409`). Esas dos reglas quedan cubiertas aquí, en el e2e, y no como test
unitario: probarlas con mocks obligaría a simular `DataSource.transaction`, el
`QueryBuilder` con `setLock` y el `EntityManager`, y lo que se verificaría sería
el andamiaje del mock, no el comportamiento. El e2e las ejerce contra la base
real, que es donde el bloqueo y la cascada existen de verdad.

- [ ] **Step 3: Correr el e2e**

Requiere PostgreSQL levantado y el seed cargado.

```bash
docker compose up -d
cd backend && npm run test:e2e
```

Esperado: PASS, 8 tests. Si falla el login, la base está vacía: arrancar `npm run start:dev` una vez para que corran migraciones y seed, y volver a intentar.

- [ ] **Step 4: Correr toda la suite unitaria**

```bash
cd backend && npm test
```

Esperado: PASS. Deben aparecer `numero-orden`, `estado-orden` y `almacenamiento-disco`.

- [ ] **Step 5: Añadir las variables de almacenamiento a `render.yaml`**

En el servicio `tallerpro-api`, dentro de `envVars`:

```yaml
      - key: API_URL
        value: https://tallerpro-api-19r0.onrender.com
      - key: STORAGE_DRIVER
        value: s3
      - key: S3_ENDPOINT
        sync: false
      - key: S3_REGION
        value: auto
      - key: S3_BUCKET
        sync: false
      - key: S3_ACCESS_KEY_ID
        sync: false
      - key: S3_SECRET_ACCESS_KEY
        sync: false
```

`sync: false` significa que el valor se carga a mano en el panel de Render y no se versiona. Las credenciales nunca van al repositorio.

- [ ] **Step 6: Recrear la base de Render y desplegar**

La base desplegada tiene un esquema creado por `synchronize` y **no tiene la
tabla `migrations`**, así que el primer arranque con `migrationsRun: true`
intentaría crear tablas que ya existen y fallaría. Como quedó definido en el
spec, la base es desechable.

1. En el panel de Render, eliminar la base `tallerpro-db` y crearla de nuevo con
   el mismo nombre, para que `fromDatabase` del `render.yaml` siga resolviendo.
2. Cargar en el servicio `tallerpro-api` los cinco valores `sync: false` del
   bucket de producción.
3. Desplegar la rama.

Esperado en el log de Render: TypeORM aplica las tres migraciones en orden
(`EsquemaInicial`, `CrearSecuenciaNumeroOrden`, `AdjuntosClaveDeObjeto`), y
después `SeedService` carga roles y usuarios. Comprobar que se puede iniciar
sesión en https://tallerpro-web-36wx.onrender.com y que un adjunto subido
sobrevive a un reinicio manual del servicio, que es justamente lo que antes no
pasaba.

- [ ] **Step 7: Actualizar la documentación**

En `README.md`, sección "Notas técnicas", reemplazar el párrafo que empieza
"TypeORM está con `synchronize: true`" por estos tres:

> El esquema se administra con migraciones. `synchronize` está apagado en las dos
> configuraciones y `migrationsRun: true` aplica lo pendiente al arrancar, así que
> ni en local ni en Render hay que ejecutar nada a mano. Para crear una migración
> nueva después de cambiar una entidad: `npm run migration:generate -- src/migrations/NombreDelCambio`.
>
> El número de orden sale de una secuencia de PostgreSQL, no de contar filas. Las
> secuencias son atómicas, así que dos altas simultáneas nunca chocan, pero no se
> revierten con la transacción: si una alta falla, ese número queda saltado. Para
> órdenes de taller es aceptable; para facturación electrónica no lo sería.
>
> El estado de la orden no se edita: se deriva de sus trabajos. Con todos
> pendientes queda `RECIBIDA`, con todos completados pasa a `FINALIZADA`, y
> cualquier mezcla es `EN_PROCESO`. La derivación corre dentro de una transacción
> que bloquea la fila de la orden, porque sin ese bloqueo dos mecánicos
> completando a la vez los dos últimos trabajos dejan la orden mal. `ENTREGADA` y
> `CANCELADA` son las dos excepciones: son decisiones humanas, tienen su propio
> endpoint, y una vez ahí la orden ya no admite cambios en sus trabajos.

En la sección "Despliegue", reemplazar la frase sobre el disco efímero ("La
segunda es que el disco es efímero...") por:

> La segunda es que el disco es efímero, y por eso los adjuntos no se guardan
> ahí: en producción van a un bucket compatible con S3 y se sirven con URL
> firmada de cinco minutos. Las credenciales se cargan a mano en el panel de
> Render, no se versionan.

En la sección "Instalación", paso 2 (Backend), añadir después del `cp`:

> Completar en el `.env` las variables de almacenamiento. Para desarrollo basta
> con `STORAGE_DRIVER=disco` y `API_URL=http://localhost:3001`; las `S3_*` solo
> hacen falta con el driver `s3`.

En `docs/modelo-er.md`:
- En la sección "Estados", documentar cuáles se derivan y cuáles son manuales.
- En el script SQL de referencia, cambiar `ruta VARCHAR(500)` por `clave VARCHAR(500)` y borrar la línea de `nombre_archivo`.
- Reemplazar la nota "Las tablas las crea TypeORM con `synchronize: true`" por la mención a las migraciones.

En `docs/endpoints.http`, añadir las dos peticiones nuevas al final de la sección de órdenes:

```
### Entregar una orden (solo desde FINALIZADA)
PATCH {{api}}/ordenes/ID_DE_LA_ORDEN/entregar
Authorization: Bearer {{token}}

### Cancelar una orden
PATCH {{api}}/ordenes/ID_DE_LA_ORDEN/cancelar
Authorization: Bearer {{token}}
```

- [ ] **Step 8: Actualizar el contexto exportado**

`docs/contexto-core.md` describe el estado del sistema y quedó desactualizado en cuatro puntos: la sección 7 sigue diciendo que `synchronize: true` y que el correlativo usa `COUNT(*)+1`; la sección 3 dice que los estados van sueltos; la sección 5 no tiene los endpoints nuevos; y la sección 8 lista como huecos abiertos varios que esta fase cierra.

Actualizar esas cuatro secciones y mover a "resuelto" los huecos cerrados: correlativo, transición de estados, `orden_id` en el DTO, archivos huérfanos, `/uploads` público y `synchronize` en producción.

- [ ] **Step 9: Commit**

```bash
git add -A
git commit -m "test: cubrir el flujo de la orden y actualizar la documentación"
```

- [ ] **Step 10: Verificación final de punta a punta**

Base desde cero y recorrido completo con los cuatro usuarios:

```bash
docker compose down -v && docker compose up -d
cd backend && npm run start:dev     # aplica migraciones y seed
cd frontend && npm start
```

Comprobar:
1. `asesor@taller.com` registra una orden. Estado `RECIBIDA`, número `ORD-000001`.
2. `jefe@taller.com` la parte en dos trabajos y los asigna a `mecanico@taller.com`.
3. `mecanico@taller.com` ve sus trabajos en el dashboard, mueve uno a `EN_PROCESO` y el badge de la orden pasa a `EN_PROCESO`.
4. El mecánico sube una foto a un trabajo y deja un comentario.
5. El mecánico completa ambos trabajos; la orden pasa a `FINALIZADA`.
6. `asesor@taller.com` entrega la orden. El Kanban ya no deja mover trabajos.
7. Reiniciar la API y comprobar que el adjunto sigue accesible.
8. `admin@taller.com` elimina la orden y el archivo desaparece de `backend/uploads/`.

- [ ] **Step 11: Merge a master**

Con todo verificado:

```bash
git checkout master && git merge --no-ff fase-0 -m "merge: fase 0 — base lista para producción"
```
