# Fase 2 — Plan de implementación

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Convertir el `presupuesto` suelto de la orden en una cotización con desglose por trabajo, y agregar la respuesta del cliente: un trabajo sin aprobar no se puede mover.

**Architecture:** El trabajo gana precio de mano de obra y una tabla hija de repuestos, así que la cotización es la suma de los trabajos y no una lista aparte. El dinero se calcula en un único módulo puro; el estado de la orden gana `COTIZADA` y su derivación pasa a mirar también precio y aprobación. La aprobación se registra en bloque, en una transacción, porque así ocurre en el mostrador.

**Tech Stack:** NestJS 11, TypeORM 1.1.0, PostgreSQL 16, Jest 30 + ts-jest, Angular 20 (signals), Bootstrap 5.

**Spec:** `docs/superpowers/specs/2026-08-08-fase-2-design.md`

## Global Constraints

- Rama de trabajo: `fase-2`. No commitear a `master`.
- Mensajes de commit en español, conventional commits en minúscula. Todo commit termina con el trailer `Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>`.
- Tests unitarios: `backend/src/**/*.spec.ts`, con `npm test`. e2e: `backend/test/*.e2e-spec.ts`, con `npm run test:e2e`; **requieren PostgreSQL y el seed cargado**.
- **Nunca correr `npm run lint`**: es `eslint --fix` sobre todo `src` y reformatea archivos ajenos. Para revisar, `npx eslint <ruta>` sin `--fix`.
- Migraciones a mano en `backend/src/migrations/`, timestamp de `date +%s000`, el mismo número en el nombre de la clase. `synchronize` está apagado: cambiar una entidad **no** cambia la base.
- Un tipo usado en una firma decorada se importa con `import type` (TS1272).
- `noImplicitAny: false`, `strictNullChecks: true`.
- Nombres de dominio en español.
- **`0` es un precio válido** de mano de obra: una revisión de cortesía. `null` significa "sin cotizar".
- **Un trabajo está cotizado cuando `precio_mano_obra` no es nulo.** Los repuestos no lo declaran cotizado.
- El dinero se calcula **en un solo sitio**: `backend/src/ordenes/totales.ts`. No duplicar la fórmula en SQL.

---

## Estructura de archivos

**Se crean**

| Archivo | Responsabilidad |
|---|---|
| `backend/src/common/transformers/numerico.ts` | El `transformer` de `numeric` a número, hoy duplicado |
| `backend/src/ordenes/totales.ts` | `subtotalTrabajo` y `calcularTotales`, puras |
| `backend/src/ordenes/totales.spec.ts` | Sus tests |
| `backend/src/repuestos/entities/repuesto.entity.ts` | La entidad |
| `backend/src/repuestos/repuestos.service.ts` | Alta y baja, con la validación de trabajo cotizado |
| `backend/src/repuestos/repuestos.controller.ts` | Los dos endpoints |
| `backend/src/repuestos/repuestos.module.ts` | Módulo |
| `backend/src/repuestos/dto/crear-repuesto.dto.ts` | Validación de la línea |
| `backend/src/ordenes/dto/registrar-aprobacion.dto.ts` | Las decisiones en bloque |
| `backend/src/migrations/*-CotizacionEnTrabajos.ts` | Columnas, tabla y baja de `presupuesto` |
| `backend/test/cotizacion.e2e-spec.ts` | e2e del ciclo completo |
| `frontend/src/app/core/models/repuesto.model.ts` | Modelos de repuesto y totales |
| `frontend/src/app/core/services/repuesto.ts` | Servicio HTTP |
| `frontend/src/app/features/ordenes/panel-cotizacion/panel-cotizacion.ts` | Desglose, totales y editor de repuestos |
| `frontend/src/app/features/ordenes/panel-cotizacion/panel-cotizacion.html` | Su plantilla |
| `frontend/src/app/features/ordenes/panel-aprobacion/panel-aprobacion.ts` | Respuesta del cliente |
| `frontend/src/app/features/ordenes/panel-aprobacion/panel-aprobacion.html` | Su plantilla |

**Se modifican**

| Archivo | Cambio |
|---|---|
| `backend/src/common/enums/estados.enum.ts` | `COTIZADA` |
| `backend/src/ordenes/estado-orden.ts` y `.spec.ts` | Nueva firma y tabla; **12 tests reescritos** |
| `backend/src/trabajos/entities/trabajo.entity.ts` | `precio_mano_obra`, `aprobado`, `repuestos` |
| `backend/src/trabajos/trabajos.service.ts` | Derivación nueva, bloqueo por aprobación, `registrarAprobacion` |
| `backend/src/trabajos/dto/crear-trabajo.dto.ts` | `precio_mano_obra` |
| `backend/src/ordenes/entities/orden.entity.ts` | Sale `presupuesto` |
| `backend/src/ordenes/dto/crear-orden.dto.ts` | Sale `presupuesto` |
| `backend/src/ordenes/ordenes.service.ts` | Sale `presupuesto`, entran `totales` |
| `backend/src/ordenes/ordenes.controller.ts` y `.module.ts` | Endpoint de aprobación |
| `backend/src/vehiculos/vehiculos.service.ts` | `totales` en el historial |
| `backend/src/app.module.ts` | `RepuestosModule` |
| `frontend/src/app/core/models/*`, `services/orden.ts` | `totales`, sin `presupuesto` |
| `frontend/src/app/features/ordenes/*`, `trabajos/*`, `dashboard`, `vehiculos/*` | Vistas |
| `README.md`, `docs/modelo-er.md`, `docs/endpoints.http`, `docs/contexto-core.md` | Documentación |

---

### Task 1: Cálculo del dinero

**Files:**
- Create: `backend/src/ordenes/totales.ts`
- Create: `backend/src/ordenes/totales.spec.ts`

**Interfaces:**
- Consumes: nada.
- Produces:
  - `interface LineaRepuesto { cantidad: number; precio_unitario: number }`
  - `interface TrabajoCotizado { precio_mano_obra?: number | null; aprobado?: boolean | null; repuestos?: LineaRepuesto[] }`
  - `interface Totales { aprobado: number; pendiente: number; rechazado: number }`
  - `subtotalTrabajo(trabajo: TrabajoCotizado): number`
  - `calcularTotales(trabajos: TrabajoCotizado[]): Totales`

- [ ] **Step 1: Escribir los tests**

Crear `backend/src/ordenes/totales.spec.ts`:

```ts
import { calcularTotales, subtotalTrabajo, TrabajoCotizado } from './totales';

describe('subtotalTrabajo', () => {
  it('sin repuestos es la mano de obra', () => {
    expect(subtotalTrabajo({ precio_mano_obra: 120.5 })).toBe(120.5);
  });

  it('suma los repuestos por cantidad', () => {
    expect(
      subtotalTrabajo({
        precio_mano_obra: 100,
        repuestos: [
          { cantidad: 2, precio_unitario: 45.5 },
          { cantidad: 1, precio_unitario: 30 },
        ],
      }),
    ).toBe(221);
  });

  it('acepta mano de obra en cero: una revisión de cortesía con repuestos', () => {
    expect(
      subtotalTrabajo({ precio_mano_obra: 0, repuestos: [{ cantidad: 3, precio_unitario: 12.5 }] }),
    ).toBe(37.5);
  });

  it('un trabajo sin cotizar vale cero aunque tenga repuestos', () => {
    expect(
      subtotalTrabajo({ precio_mano_obra: null, repuestos: [{ cantidad: 1, precio_unitario: 99 }] }),
    ).toBe(0);
  });

  it('redondea a dos decimales', () => {
    expect(subtotalTrabajo({ precio_mano_obra: 0.1, repuestos: [{ cantidad: 1, precio_unitario: 0.2 }] })).toBe(0.3);
  });
});

describe('calcularTotales', () => {
  const trabajos: TrabajoCotizado[] = [
    { precio_mano_obra: 100, aprobado: true, repuestos: [{ cantidad: 1, precio_unitario: 50 }] },
    { precio_mano_obra: 200, aprobado: false },
    { precio_mano_obra: 80, aprobado: null },
    { precio_mano_obra: null, aprobado: null },
  ];

  it('reparte en los tres buckets', () => {
    expect(calcularTotales(trabajos)).toEqual({ aprobado: 150, pendiente: 80, rechazado: 200 });
  });

  it('sin trabajos todo es cero', () => {
    expect(calcularTotales([])).toEqual({ aprobado: 0, pendiente: 0, rechazado: 0 });
  });

  it('los trabajos sin cotizar no suman en ningún bucket', () => {
    expect(calcularTotales([{ precio_mano_obra: null, aprobado: true }])).toEqual({
      aprobado: 0,
      pendiente: 0,
      rechazado: 0,
    });
  });

  it('acumula varios trabajos en el mismo bucket', () => {
    expect(
      calcularTotales([
        { precio_mano_obra: 10.1, aprobado: true },
        { precio_mano_obra: 20.2, aprobado: true },
      ]),
    ).toEqual({ aprobado: 30.3, pendiente: 0, rechazado: 0 });
  });
});
```

- [ ] **Step 2: Correr los tests para verificar que fallan**

```bash
cd backend && npm test -- totales
```

Esperado: FAIL, `Cannot find module './totales'`.

- [ ] **Step 3: Escribir la implementación**

Crear `backend/src/ordenes/totales.ts`:

```ts
export interface LineaRepuesto {
  cantidad: number;
  precio_unitario: number;
}

export interface TrabajoCotizado {
  precio_mano_obra?: number | null;
  aprobado?: boolean | null;
  repuestos?: LineaRepuesto[];
}

export interface Totales {
  aprobado: number;
  pendiente: number;
  rechazado: number;
}

/** Dos decimales, para que sumar dinero no arrastre el error del punto flotante. */
function redondear(valor: number): number {
  return Math.round(valor * 100) / 100;
}

/**
 * Mano de obra más repuestos. Un trabajo sin cotizar vale cero: sus repuestos
 * no cuentan hasta que alguien le ponga precio de mano de obra, aunque sea 0.
 */
export function subtotalTrabajo(trabajo: TrabajoCotizado): number {
  if (trabajo.precio_mano_obra == null) return 0;

  const repuestos = (trabajo.repuestos ?? []).reduce(
    (suma, repuesto) => suma + repuesto.cantidad * repuesto.precio_unitario,
    0,
  );

  return redondear(trabajo.precio_mano_obra + repuestos);
}

/**
 * Tres cifras en vez de un total ambiguo: `aprobado` es lo que el cliente va a
 * pagar, `pendiente` lo que todavía no responde, `rechazado` lo que dijo que no.
 */
export function calcularTotales(trabajos: TrabajoCotizado[]): Totales {
  const totales: Totales = { aprobado: 0, pendiente: 0, rechazado: 0 };

  for (const trabajo of trabajos) {
    if (trabajo.precio_mano_obra == null) continue;

    const subtotal = subtotalTrabajo(trabajo);

    if (trabajo.aprobado === true) totales.aprobado += subtotal;
    else if (trabajo.aprobado === false) totales.rechazado += subtotal;
    else totales.pendiente += subtotal;
  }

  return {
    aprobado: redondear(totales.aprobado),
    pendiente: redondear(totales.pendiente),
    rechazado: redondear(totales.rechazado),
  };
}
```

- [ ] **Step 4: Correr los tests para verificar que pasan**

```bash
cd backend && npm test -- totales
```

Esperado: PASS, 9 tests.

- [ ] **Step 5: Commit**

```bash
git add backend/src/ordenes/totales.ts backend/src/ordenes/totales.spec.ts
git commit -m "feat: cálculo de los totales de la cotización"
```

---

### Task 2: Esquema de la cotización

**Files:**
- Create: `backend/src/common/transformers/numerico.ts`
- Create: `backend/src/repuestos/entities/repuesto.entity.ts`
- Create: `backend/src/migrations/<timestamp>-CotizacionEnTrabajos.ts`
- Modify: `backend/src/trabajos/entities/trabajo.entity.ts`
- Modify: `backend/src/ordenes/entities/orden.entity.ts` (sale `presupuesto`)
- Modify: `backend/src/ordenes/dto/crear-orden.dto.ts` (sale `presupuesto`)
- Modify: `backend/src/ordenes/ordenes.service.ts` (sale `presupuesto`)
- Modify: `backend/src/vehiculos/vehiculos.service.ts` (sale `presupuesto` del select)
- Modify: `frontend/src/app/core/models/orden.model.ts`, `vehiculo.model.ts`
- Modify: `frontend/src/app/features/ordenes/formulario-orden/*`, `lista-ordenes.html`, `detalle-orden.html`, `vehiculos/ficha-vehiculo/ficha-vehiculo.html`

**Interfaces:**
- Consumes: nada de las tareas anteriores.
- Produces:
  - `numerico` — el objeto `transformer` para columnas `numeric`.
  - Entidad `Repuesto` con `id`, `descripcion`, `cantidad`, `precio_unitario`, `trabajo`, `created_at`.
  - `Trabajo.precio_mano_obra?: number`, `Trabajo.aprobado?: boolean`, `Trabajo.repuestos: Repuesto[]`.
  - `Orden` ya no tiene `presupuesto`.
- **Esta tarea deja backend y frontend compilando.** Quitar `presupuesto` rompe las dos mitades, así que las dos se arreglan aquí.

- [ ] **Step 1: Extraer el transformer de columnas numéricas**

Hoy vive en línea dentro de `Orden.presupuesto` y va a hacer falta en dos columnas nuevas. Crear `backend/src/common/transformers/numerico.ts`:

```ts
/**
 * Las columnas `numeric` de PostgreSQL vuelven del driver como texto, para no
 * perder precisión. Sin esto la API devuelve "680.50" donde la entidad declara
 * un número, y la validación del formulario lo rechaza al reenviarlo.
 */
export const numerico = {
  to: (valor?: number | null) => valor,
  from: (valor?: string | null) => (valor == null ? valor : Number(valor)),
};
```

- [ ] **Step 2: Escribir la entidad `Repuesto`**

Crear `backend/src/repuestos/entities/repuesto.entity.ts`:

```ts
import {
  Column,
  CreateDateColumn,
  Entity,
  JoinColumn,
  ManyToOne,
  PrimaryGeneratedColumn,
} from 'typeorm';
import { Trabajo } from '../../trabajos/entities/trabajo.entity';
import { numerico } from '../../common/transformers/numerico';

@Entity('repuestos')
export class Repuesto {
  @PrimaryGeneratedColumn('uuid')
  id!: string;

  @Column({ type: 'varchar', length: 200 })
  descripcion!: string;

  @Column({ type: 'int' })
  cantidad!: number;

  @Column({ type: 'numeric', precision: 10, scale: 2, transformer: numerico })
  precio_unitario!: number;

  @ManyToOne(() => Trabajo, (trabajo) => trabajo.repuestos, {
    onDelete: 'CASCADE',
    nullable: false,
  })
  @JoinColumn({ name: 'trabajo_id' })
  trabajo!: Trabajo;

  @CreateDateColumn()
  created_at!: Date;
}
```

- [ ] **Step 3: Ampliar la entidad `Trabajo`**

En `backend/src/trabajos/entities/trabajo.entity.ts`, agregar los imports de `Repuesto` y `numerico`, y estas tres propiedades: las dos columnas después de `estado`, y la relación junto a `comentarios` y `adjuntos`:

```ts
  /** `null` = sin cotizar. `0` es un precio válido: una revisión de cortesía. */
  @Column({ type: 'numeric', precision: 10, scale: 2, nullable: true, transformer: numerico })
  precio_mano_obra?: number;

  /** `null` = esperando respuesta · `true` = aprobado · `false` = rechazado. */
  @Column({ type: 'boolean', nullable: true })
  aprobado?: boolean;
```

```ts
  @OneToMany(() => Repuesto, (repuesto) => repuesto.trabajo)
  repuestos!: Repuesto[];
```

- [ ] **Step 4: Quitar `presupuesto` de la entidad `Orden`**

En `backend/src/ordenes/entities/orden.entity.ts`, borrar el bloque completo de `presupuesto` con su `transformer` en línea.

- [ ] **Step 5: Escribir la migración**

Obtener el timestamp con `date +%s000` y usarlo en el archivo y en la clase.

Crear `backend/src/migrations/<timestamp>-CotizacionEnTrabajos.ts`:

```ts
import { MigrationInterface, QueryRunner } from 'typeorm';

/**
 * El presupuesto deja de ser un número suelto en la orden y pasa a ser la suma
 * de los trabajos. `ordenes.presupuesto` se elimina sin repartirlo: no hay forma
 * honesta de partirlo entre trabajos que no existen.
 */
export class CotizacionEnTrabajos<TIMESTAMP> implements MigrationInterface {
  public async up(queryRunner: QueryRunner): Promise<void> {
    await queryRunner.query(
      `ALTER TABLE "trabajos" ADD COLUMN "precio_mano_obra" numeric(10,2)`,
    );
    await queryRunner.query(`ALTER TABLE "trabajos" ADD COLUMN "aprobado" boolean`);

    await queryRunner.query(`
      CREATE TABLE "repuestos" (
        "id" uuid NOT NULL DEFAULT gen_random_uuid(),
        "descripcion" character varying(200) NOT NULL,
        "cantidad" integer NOT NULL,
        "precio_unitario" numeric(10,2) NOT NULL,
        "trabajo_id" uuid NOT NULL,
        "created_at" TIMESTAMP NOT NULL DEFAULT now(),
        CONSTRAINT "PK_repuestos" PRIMARY KEY ("id"),
        CONSTRAINT "FK_repuestos_trabajo" FOREIGN KEY ("trabajo_id")
          REFERENCES "trabajos"("id") ON DELETE CASCADE ON UPDATE NO ACTION
      )
    `);

    await queryRunner.query(`ALTER TABLE "ordenes" DROP COLUMN "presupuesto"`);
  }

  public async down(queryRunner: QueryRunner): Promise<void> {
    await queryRunner.query(`ALTER TABLE "ordenes" ADD COLUMN "presupuesto" numeric(10,2)`);
    await queryRunner.query(`DROP TABLE "repuestos"`);
    await queryRunner.query(`ALTER TABLE "trabajos" DROP COLUMN "aprobado"`);
    await queryRunner.query(`ALTER TABLE "trabajos" DROP COLUMN "precio_mano_obra"`);
  }
}
```

- [ ] **Step 6: Sacar `presupuesto` del backend**

- `backend/src/ordenes/dto/crear-orden.dto.ts`: borrar el bloque de `presupuesto` con sus tres decoradores.
- `backend/src/ordenes/ordenes.service.ts`: quitar `presupuesto: true` del `select` de `obtenerTodas`, la línea `presupuesto: dto.presupuesto,` de `crear`, y la línea `if (dto.presupuesto !== undefined) ...` de `actualizar`.
- `backend/src/vehiculos/vehiculos.service.ts`: quitar `presupuesto: true` del `select` de `obtenerConHistorial`.

- [ ] **Step 7: Sacar `presupuesto` del frontend**

- `core/models/orden.model.ts`: quitar `presupuesto` de `Orden` y de `OrdenRequest`.
- `core/models/vehiculo.model.ts`: quitar `presupuesto` de `OrdenDelHistorial`.
- `formulario-orden.ts`: quitar `presupuesto` del `FormGroup`, del `patchValue` de `cargarOrden`, y la línea `if (valores.presupuesto > 0) ...` de `enviar`.
- `formulario-orden.html`: borrar el bloque del campo presupuesto.
- `lista-ordenes.html`, `detalle-orden.html`, `ficha-vehiculo.html`: quitar por ahora las celdas que muestran el presupuesto. La Task 6 pone los totales en su lugar.

Encontrarlos con:

```bash
grep -rn "presupuesto" backend/src frontend/src
```

Esperado tras el cambio: solo aparece en `totales.ts`, en la migración y en los comentarios.

`Repuesto` hay que registrarla ya: `autoLoadEntities` solo conoce las entidades
declaradas en algún `forFeature`, y referenciarla desde `Trabajo.repuestos` no
alcanza —TypeORM falla con `Entity metadata for Trabajo#repuestos was not found`—.
Agregarla al `forFeature` de `backend/src/trabajos/trabajos.module.ts`:

```ts
  imports: [TypeOrmModule.forFeature([Trabajo, Repuesto]), AdjuntosModule],
```

El `RepuestosModule` completo llega en la Task 4; declarar la entidad en dos
módulos es válido.

- [ ] **Step 8: Aplicar la migración y verificar el esquema**

```bash
cd backend && npm run start:dev
```

En otra terminal:

```bash
docker compose exec -T postgres psql -U postgres -d db_taller -c "\d trabajos" -c "\d repuestos" \
  -c "SELECT column_name FROM information_schema.columns WHERE table_name='ordenes';"
```

Esperado: `trabajos` con `precio_mano_obra numeric(10,2)` y `aprobado boolean`, la tabla `repuestos` con su FK en cascada, y `ordenes` **sin** `presupuesto`.

- [ ] **Step 9: Compilar el frontend**

```bash
cd frontend && npm run build
```

Esperado: compila. Un `Property 'presupuesto' does not exist` señala una referencia sin quitar.

- [ ] **Step 10: Commit**

```bash
git add backend/src frontend/src
git commit -m "feat: precio y repuestos en el trabajo, y baja del presupuesto suelto"
```

---

### Task 3: El estado sabe de cotización y aprobación

**Files:**
- Modify: `backend/src/common/enums/estados.enum.ts`
- Modify: `backend/src/ordenes/estado-orden.ts`
- Modify: `backend/src/ordenes/estado-orden.spec.ts` (**reescrito**)
- Modify: `backend/src/trabajos/trabajos.service.ts`
- Modify: `frontend/src/app/core/models/estados.ts`

**Interfaces:**
- Consumes: las columnas de la Task 2.
- Produces:
  - `EstadoOrden.COTIZADA`
  - `interface TrabajoParaDerivar { estado: string; precio_mano_obra?: number | null; aprobado?: boolean | null }`
  - `derivarEstado(trabajos: TrabajoParaDerivar[]): EstadoOrden` — **firma nueva**
  - `TrabajosService.actualizarEstado` responde `409` si el trabajo no está aprobado.

- [ ] **Step 1: Agregar el estado al enum**

En `backend/src/common/enums/estados.enum.ts`, dentro de `EstadoOrden`, entre `RECIBIDA` y `EN_PROCESO`:

```ts
  COTIZADA = 'COTIZADA',
```

- [ ] **Step 2: Reescribir los tests de `derivarEstado`**

Los 12 tests actuales le pasan `string[]`. La función pasa a recibir objetos, así
que **el archivo se reescribe entero**. Reemplazar el bloque `describe('derivarEstado', ...)`
de `backend/src/ordenes/estado-orden.spec.ts` por:

```ts
describe('derivarEstado', () => {
  const pendiente = (extra = {}) => ({ estado: EstadoTrabajo.PENDIENTE, ...extra });
  const enProceso = (extra = {}) => ({ estado: EstadoTrabajo.EN_PROCESO, ...extra });
  const completado = (extra = {}) => ({ estado: EstadoTrabajo.COMPLETADO, ...extra });
  const aprobado = { precio_mano_obra: 100, aprobado: true };
  const rechazado = { precio_mano_obra: 100, aprobado: false };
  const sinResponder = { precio_mano_obra: 100, aprobado: null };
  const sinCotizar = { precio_mano_obra: null, aprobado: null };

  it('una orden sin trabajos queda RECIBIDA', () => {
    expect(derivarEstado([])).toBe(EstadoOrden.RECIBIDA);
  });

  it('con trabajos sin cotizar sigue RECIBIDA: el jefe está armando la cotización', () => {
    expect(derivarEstado([pendiente(sinCotizar), pendiente(sinCotizar)])).toBe(
      EstadoOrden.RECIBIDA,
    );
  });

  it('con un trabajo cotizado esperando respuesta pasa a COTIZADA', () => {
    expect(derivarEstado([pendiente(sinResponder)])).toBe(EstadoOrden.COTIZADA);
  });

  it('COTIZADA gana aunque ya haya trabajos aprobados avanzando', () => {
    expect(derivarEstado([enProceso(aprobado), pendiente(sinResponder)])).toBe(
      EstadoOrden.COTIZADA,
    );
  });

  it('con todo aprobado y nada empezado vuelve a RECIBIDA', () => {
    expect(derivarEstado([pendiente(aprobado), pendiente(aprobado)])).toBe(EstadoOrden.RECIBIDA);
  });

  it('con un aprobado en proceso pasa a EN_PROCESO', () => {
    expect(derivarEstado([enProceso(aprobado), pendiente(aprobado)])).toBe(EstadoOrden.EN_PROCESO);
  });

  it('con parte aprobada completada y parte pendiente sigue EN_PROCESO', () => {
    expect(derivarEstado([completado(aprobado), pendiente(aprobado)])).toBe(
      EstadoOrden.EN_PROCESO,
    );
  });

  it('con todos los aprobados completados pasa a FINALIZADA', () => {
    expect(derivarEstado([completado(aprobado), completado(aprobado)])).toBe(
      EstadoOrden.FINALIZADA,
    );
  });

  it('un trabajo rechazado no impide finalizar', () => {
    expect(derivarEstado([completado(aprobado), pendiente(rechazado)])).toBe(
      EstadoOrden.FINALIZADA,
    );
  });

  it('si el cliente rechaza todo, vuelve a RECIBIDA', () => {
    expect(derivarEstado([pendiente(rechazado), pendiente(rechazado)])).toBe(EstadoOrden.RECIBIDA);
  });

  it('un trabajo sin cotizar no impide finalizar los aprobados', () => {
    expect(derivarEstado([completado(aprobado), pendiente(sinCotizar)])).toBe(
      EstadoOrden.FINALIZADA,
    );
  });
});
```

Los bloques `describe` de `esTerminal`, `puedeEntregar` y `puedeCancelar` se quedan como están, más un caso nuevo en `puedeCancelar`:

```ts
  it('una orden cotizada se puede cancelar', () => {
    expect(puedeCancelar(EstadoOrden.COTIZADA)).toBe(true);
  });
```

- [ ] **Step 3: Correr los tests para verificar que fallan**

```bash
cd backend && npm test -- estado-orden
```

Esperado: FAIL. Los tests le pasan objetos a una función que espera `string[]`.

- [ ] **Step 4: Reescribir `derivarEstado`**

En `backend/src/ordenes/estado-orden.ts`, reemplazar la función y agregar el tipo:

```ts
export interface TrabajoParaDerivar {
  estado: string;
  precio_mano_obra?: number | null;
  aprobado?: boolean | null;
}

/**
 * El estado de la orden dentro del taller es función de sus trabajos, y desde
 * la fase 2 también de si el cliente respondió.
 *
 * Solo los trabajos aprobados participan del avance: uno rechazado no impide
 * finalizar, y uno sin cotizar tampoco, porque el jefe todavía está armando la
 * cotización.
 */
export function derivarEstado(trabajos: TrabajoParaDerivar[]): EstadoOrden {
  if (trabajos.length === 0) return EstadoOrden.RECIBIDA;

  const esperandoRespuesta = trabajos.some(
    (trabajo) => trabajo.precio_mano_obra != null && trabajo.aprobado == null,
  );
  if (esperandoRespuesta) return EstadoOrden.COTIZADA;

  const aprobados = trabajos.filter((trabajo) => trabajo.aprobado === true);
  if (aprobados.length === 0) return EstadoOrden.RECIBIDA;

  if (aprobados.every((trabajo) => trabajo.estado === EstadoTrabajo.PENDIENTE)) {
    return EstadoOrden.RECIBIDA;
  }
  if (aprobados.every((trabajo) => trabajo.estado === EstadoTrabajo.COMPLETADO)) {
    return EstadoOrden.FINALIZADA;
  }
  return EstadoOrden.EN_PROCESO;
}
```

- [ ] **Step 5: Correr los tests para verificar que pasan**

```bash
cd backend && npm test -- estado-orden
```

Esperado: PASS, 18 tests.

- [ ] **Step 6: Adaptar la sincronización y agregar el bloqueo**

En `backend/src/trabajos/trabajos.service.ts`, `sincronizarEstadoOrden` pasa a
leer las tres columnas en lugar de solo el estado:

```ts
  private async sincronizarEstadoOrden(manager: EntityManager, orden: Orden): Promise<void> {
    const trabajos = await manager.find(Trabajo, {
      where: { orden: { id: orden.id } },
      select: { id: true, estado: true, precio_mano_obra: true, aprobado: true },
    });

    const derivado = derivarEstado(trabajos);

    if (derivado !== orden.estado) {
      await manager.update(Orden, { id: orden.id }, { estado: derivado });
    }
  }
```

Y en `actualizarEstado`, después de la comprobación de asignación y antes de escribir:

```ts
      // La regla que de verdad impide trabajar sin autorización del cliente.
      if (actual.aprobado !== true) {
        throw new ConflictException(
          `El trabajo "${actual.titulo}" no está aprobado por el cliente`,
        );
      }
```

- [ ] **Step 7: Agregar el estado al frontend**

En `frontend/src/app/core/models/estados.ts`:

- `ESTADOS_ORDEN`: agregar `'COTIZADA'` entre `'RECIBIDA'` y `'EN_PROCESO'`.
- `ETIQUETA_ESTADO_ORDEN`: `COTIZADA: 'Cotizada'`.
- `COLOR_ESTADO`: `COTIZADA: 'warning'`.

- [ ] **Step 8: Verificar el bloqueo contra la API**

Levantar la API. Con un token de `jefe@taller.com`, crear una orden y un trabajo
(que nace sin cotizar), e intentar moverlo:

```bash
curl -s -o /dev/null -w '%{http_code}\n' -X PATCH \
  http://localhost:3001/trabajos/$TRABAJO_ID/estado \
  -H "Authorization: Bearer $TOKEN" -H 'Content-Type: application/json' \
  -d '{"estado":"EN_PROCESO"}'
```

Esperado: `409`, con el mensaje de que no está aprobado.

- [ ] **Step 9: Commit**

```bash
git add backend/src frontend/src/app/core/models/estados.ts
git commit -m "feat: estado COTIZADA y bloqueo de los trabajos sin aprobar"
```

---

### Task 4: Repuestos, aprobación y totales en la API

**Files:**
- Create: `backend/src/repuestos/dto/crear-repuesto.dto.ts`
- Create: `backend/src/repuestos/repuestos.service.ts`
- Create: `backend/src/repuestos/repuestos.controller.ts`
- Create: `backend/src/repuestos/repuestos.module.ts`
- Create: `backend/src/ordenes/dto/registrar-aprobacion.dto.ts`
- Create: `backend/test/cotizacion.e2e-spec.ts`
- Modify: `backend/src/app.module.ts`
- Modify: `backend/src/trabajos/trabajos.service.ts`, `.module.ts`, `dto/crear-trabajo.dto.ts`
- Modify: `backend/src/ordenes/ordenes.service.ts`, `.controller.ts`, `.module.ts`
- Modify: `backend/src/vehiculos/vehiculos.service.ts`

**Interfaces:**
- Consumes: `calcularTotales` de la Task 1; las entidades de la Task 2; `derivarEstado` y el bloqueo de la Task 3.
- Produces:
  - `POST /repuestos/trabajo/:trabajoId` y `DELETE /repuestos/:id`
  - `PATCH /ordenes/:id/aprobacion`
  - `TrabajosService.registrarAprobacion(ordenId: string, decisiones: DecisionDto[]): Promise<Trabajo[]>`
  - `GET /ordenes`, `GET /ordenes/:id` y el historial de `GET /vehiculos/:id` traen `totales`.
  - `GET /trabajos/orden/:ordenId` trae `precio_mano_obra`, `aprobado` y `repuestos`.

**Desvío del spec, deliberado:** el spec escribía las rutas como
`POST /trabajos/:trabajoId/repuestos`. Se usan
`POST /repuestos/trabajo/:trabajoId` y `DELETE /repuestos/:id` para seguir el
patrón que ya tiene el proyecto en `adjuntos` y `comentarios`, que son las dos
hijas equivalentes.

- [ ] **Step 1: Escribir el e2e primero**

Crear `backend/test/cotizacion.e2e-spec.ts`:

```ts
import { INestApplication, ValidationPipe } from '@nestjs/common';
import { Test, TestingModule } from '@nestjs/testing';
import request from 'supertest';
import { App } from 'supertest/types';
import { AppModule } from './../src/app.module';

describe('Cotización y aprobación (e2e)', () => {
  let app: INestApplication<App>;
  let tokenJefe: string;
  let tokenMecanico: string;
  let idMecanico: string;
  let ordenId: string;
  let trabajoA: string;
  let trabajoB: string;
  let trabajoSinCotizar: string;

  const iniciarSesion = async (email: string) => {
    const respuesta = await request(app.getHttpServer())
      .post('/auth/login')
      .send({ email, password: '123456' })
      .expect(201);
    return respuesta.body;
  };

  const estadoDeLaOrden = async () => {
    const respuesta = await request(app.getHttpServer())
      .get(`/ordenes/${ordenId}`)
      .set('Authorization', `Bearer ${tokenJefe}`)
      .expect(200);
    return respuesta.body;
  };

  const crearTrabajo = async (titulo: string, precio?: number) => {
    const respuesta = await request(app.getHttpServer())
      .post('/trabajos')
      .set('Authorization', `Bearer ${tokenJefe}`)
      .send({ titulo, orden_id: ordenId, asignado_a_id: idMecanico, precio_mano_obra: precio })
      .expect(201);
    return respuesta.body.id;
  };

  beforeAll(async () => {
    const moduleFixture: TestingModule = await Test.createTestingModule({
      imports: [AppModule],
    }).compile();

    app = moduleFixture.createNestApplication();
    app.useGlobalPipes(
      new ValidationPipe({ whitelist: true, forbidNonWhitelisted: true, transform: true }),
    );
    await app.init();

    tokenJefe = (await iniciarSesion('jefe@taller.com')).access_token;
    const mecanico = await iniciarSesion('mecanico@taller.com');
    tokenMecanico = mecanico.access_token;
    idMecanico = mecanico.usuario.id;

    const orden = await request(app.getHttpServer())
      .post('/ordenes')
      .set('Authorization', `Bearer ${tokenJefe}`)
      .send({
        descripcion: 'Mantenimiento con cotización',
        fecha_ingreso: '2026-08-08',
        placa: `COT${Date.now().toString().slice(-6)}`,
        marca: 'Toyota',
        modelo: 'Yaris',
        propietario_nombre: 'Cliente de prueba',
        propietario_telefono: '999888777',
      })
      .expect(201);
    ordenId = orden.body.id;
  });

  afterAll(async () => {
    const admin = await iniciarSesion('admin@taller.com');
    await request(app.getHttpServer())
      .delete(`/ordenes/${ordenId}`)
      .set('Authorization', `Bearer ${admin.access_token}`);
    await app.close();
  });

  it('un trabajo sin cotizar no mueve el estado de la orden', async () => {
    trabajoSinCotizar = await crearTrabajo('Diagnóstico');
    expect((await estadoDeLaOrden()).estado).toBe('RECIBIDA');
  });

  it('rechaza cargar un repuesto en un trabajo sin cotizar', async () => {
    await request(app.getHttpServer())
      .post(`/repuestos/trabajo/${trabajoSinCotizar}`)
      .set('Authorization', `Bearer ${tokenJefe}`)
      .send({ descripcion: 'Filtro', cantidad: 1, precio_unitario: 30 })
      .expect(409);
  });

  it('cotizar un trabajo deja la orden COTIZADA', async () => {
    trabajoA = await crearTrabajo('Cambio de aceite', 100);
    expect((await estadoDeLaOrden()).estado).toBe('COTIZADA');
  });

  it('los repuestos suman al total pendiente', async () => {
    await request(app.getHttpServer())
      .post(`/repuestos/trabajo/${trabajoA}`)
      .set('Authorization', `Bearer ${tokenJefe}`)
      .send({ descripcion: 'Aceite sintético', cantidad: 4, precio_unitario: 25.5 })
      .expect(201);

    expect((await estadoDeLaOrden()).totales).toEqual({
      aprobado: 0,
      pendiente: 202,
      rechazado: 0,
    });
  });

  it('un trabajo sin aprobar no se puede mover', async () => {
    await request(app.getHttpServer())
      .patch(`/trabajos/${trabajoA}/estado`)
      .set('Authorization', `Bearer ${tokenMecanico}`)
      .send({ estado: 'EN_PROCESO' })
      .expect(409);
  });

  it('no se puede aprobar un trabajo sin cotizar', async () => {
    await request(app.getHttpServer())
      .patch(`/ordenes/${ordenId}/aprobacion`)
      .set('Authorization', `Bearer ${tokenJefe}`)
      .send({ decisiones: [{ trabajo_id: trabajoSinCotizar, aprobado: true }] })
      .expect(409);
  });

  it('la respuesta parcial reparte los totales y deja la orden RECIBIDA', async () => {
    trabajoB = await crearTrabajo('Cambio de bujías', 80);

    await request(app.getHttpServer())
      .patch(`/ordenes/${ordenId}/aprobacion`)
      .set('Authorization', `Bearer ${tokenJefe}`)
      .send({
        decisiones: [
          { trabajo_id: trabajoA, aprobado: true },
          { trabajo_id: trabajoB, aprobado: false },
        ],
      })
      .expect(200);

    const orden = await estadoDeLaOrden();
    expect(orden.totales).toEqual({ aprobado: 202, pendiente: 0, rechazado: 80 });
    // Aprobar no es empezar: los aprobados siguen PENDIENTE.
    expect(orden.estado).toBe('RECIBIDA');
  });

  it('mover un trabajo aprobado sí avanza la orden', async () => {
    await request(app.getHttpServer())
      .patch(`/trabajos/${trabajoA}/estado`)
      .set('Authorization', `Bearer ${tokenMecanico}`)
      .send({ estado: 'EN_PROCESO' })
      .expect(200);

    expect((await estadoDeLaOrden()).estado).toBe('EN_PROCESO');
  });

  it('completar los aprobados finaliza la orden pese al rechazado', async () => {
    await request(app.getHttpServer())
      .patch(`/trabajos/${trabajoA}/estado`)
      .set('Authorization', `Bearer ${tokenMecanico}`)
      .send({ estado: 'COMPLETADO' })
      .expect(200);

    expect((await estadoDeLaOrden()).estado).toBe('FINALIZADA');
  });
});
```

- [ ] **Step 2: Correr el e2e para verificar que falla**

```bash
cd backend && npm run test:e2e -- cotizacion
```

Esperado: FAIL. `precio_mano_obra` todavía no se acepta en el DTO y los endpoints
de repuestos y aprobación no existen.

- [ ] **Step 3: Aceptar el precio al crear y editar un trabajo**

En `backend/src/trabajos/dto/crear-trabajo.dto.ts`, agregar:

```ts
  @IsOptional()
  @IsNumber({}, { message: 'El precio de mano de obra debe ser un número' })
  @Min(0, { message: 'El precio de mano de obra no puede ser negativo' })
  precio_mano_obra?: number;
```

Con `IsNumber` y `Min` añadidos al import de `class-validator`.

En `TrabajosService.crear`, añadir `precio_mano_obra: dto.precio_mano_obra,` al
objeto que se pasa a `manager.create`. En `actualizar`, añadir:

```ts
    if (dto.precio_mano_obra !== undefined) trabajo.precio_mano_obra = dto.precio_mano_obra;
```

`actualizar` no pasa por `conOrdenBloqueada`, así que cambiar un precio no
redispara la derivación. Para que cotizar mueva la orden a `COTIZADA`, envolver
también `actualizar`:

```ts
  async actualizar(id: string, dto: ActualizarTrabajoDto) {
    const trabajo = await this.obtenerPorId(id);

    return await this.conOrdenBloqueada(trabajo.orden.id, async (manager) => {
      const actual = await manager.findOne(Trabajo, { where: { id } });
      if (!actual) {
        throw new NotFoundException(`No existe el trabajo con id ${id}`);
      }

      if (dto.titulo !== undefined) actual.titulo = dto.titulo;
      if (dto.descripcion !== undefined) actual.descripcion = dto.descripcion;
      if (dto.prioridad !== undefined) actual.prioridad = dto.prioridad;
      if (dto.fecha_limite !== undefined) actual.fecha_limite = dto.fecha_limite;
      if (dto.precio_mano_obra !== undefined) actual.precio_mano_obra = dto.precio_mano_obra;
      if (dto.asignado_a_id !== undefined) {
        actual.asignado_a = { id: dto.asignado_a_id } as any;
      }

      return await manager.save(actual);
    });
  }
```

- [ ] **Step 4: Devolver la cotización con los trabajos**

El frontend **no debe recalcular el subtotal**: el spec exige que el dinero se
calcule en un solo sitio. Así que la API lo manda ya calculado.

En `TrabajosService.obtenerPorOrden`, agregar `repuestos: true` a `relations` y
estos campos al `select`:

```ts
        precio_mano_obra: true,
        aprobado: true,
        repuestos: {
          id: true,
          descripcion: true,
          cantidad: true,
          precio_unitario: true,
        },
```

Y mapear el resultado para agregar el subtotal de cada trabajo, con la misma
función que usa el cálculo de los totales:

```ts
    return trabajos.map((trabajo) => ({ ...trabajo, subtotal: subtotalTrabajo(trabajo) }));
```

con `import { subtotalTrabajo } from '../ordenes/totales';`.

- [ ] **Step 5: Escribir el módulo de repuestos**

Crear `backend/src/repuestos/dto/crear-repuesto.dto.ts`:

```ts
import { IsInt, IsNotEmpty, IsNumber, IsString, MaxLength, Min } from 'class-validator';

export class CrearRepuestoDto {
  @IsString()
  @IsNotEmpty({ message: 'La descripción del repuesto es obligatoria' })
  @MaxLength(200)
  descripcion!: string;

  @IsInt({ message: 'La cantidad debe ser un número entero' })
  @Min(1, { message: 'La cantidad mínima es 1' })
  cantidad!: number;

  @IsNumber({}, { message: 'El precio unitario debe ser un número' })
  @Min(0, { message: 'El precio unitario no puede ser negativo' })
  precio_unitario!: number;
}
```

Crear `backend/src/repuestos/repuestos.service.ts`:

```ts
import { ConflictException, Injectable, NotFoundException } from '@nestjs/common';
import { InjectRepository } from '@nestjs/typeorm';
import { Repository } from 'typeorm';

import { Repuesto } from './entities/repuesto.entity';
import { Trabajo } from '../trabajos/entities/trabajo.entity';
import { CrearRepuestoDto } from './dto/crear-repuesto.dto';

@Injectable()
export class RepuestosService {
  constructor(
    @InjectRepository(Repuesto)
    private readonly repuestoRepository: Repository<Repuesto>,
    @InjectRepository(Trabajo)
    private readonly trabajoRepository: Repository<Trabajo>,
  ) {}

  async crear(trabajoId: string, dto: CrearRepuestoDto) {
    const trabajo = await this.trabajoRepository.findOne({ where: { id: trabajoId } });
    if (!trabajo) {
      throw new NotFoundException(`No existe el trabajo con id ${trabajoId}`);
    }

    // Sin precio de mano de obra el trabajo no está cotizado, y sus repuestos
    // no sumarían en ningún total: dinero invisible.
    if (trabajo.precio_mano_obra == null) {
      throw new ConflictException(
        `El trabajo "${trabajo.titulo}" todavía no tiene precio de mano de obra: ponlo primero, aunque sea 0`,
      );
    }

    const nuevo = this.repuestoRepository.create({
      descripcion: dto.descripcion,
      cantidad: dto.cantidad,
      precio_unitario: dto.precio_unitario,
      trabajo: { id: trabajoId } as any,
    });

    return await this.repuestoRepository.save(nuevo);
  }

  async eliminar(id: string) {
    const repuesto = await this.repuestoRepository.findOne({ where: { id } });
    if (!repuesto) {
      throw new NotFoundException(`No existe el repuesto con id ${id}`);
    }

    await this.repuestoRepository.remove(repuesto);
    return { mensaje: 'Repuesto eliminado correctamente' };
  }
}
```

Crear `backend/src/repuestos/repuestos.controller.ts`:

```ts
import { Body, Controller, Delete, Param, Post, UseGuards } from '@nestjs/common';
import { RepuestosService } from './repuestos.service';
import { CrearRepuestoDto } from './dto/crear-repuesto.dto';
import { JwtAuthGuard } from '../auth/guards/jwt-auth.guard';
import { RolesGuard } from '../auth/guards/roles.guard';
import { Roles } from '../common/decorators/roles.decorator';
import { NombreRol } from '../common/enums/estados.enum';

@Controller('repuestos')
@UseGuards(JwtAuthGuard, RolesGuard)
export class RepuestosController {
  constructor(private readonly repuestosService: RepuestosService) {}

  @Post('trabajo/:trabajoId')
  @Roles(NombreRol.ADMINISTRADOR, NombreRol.JEFE_TALLER)
  async crear(@Param('trabajoId') trabajoId: string, @Body() dto: CrearRepuestoDto) {
    return await this.repuestosService.crear(trabajoId, dto);
  }

  @Delete(':id')
  @Roles(NombreRol.ADMINISTRADOR, NombreRol.JEFE_TALLER)
  async eliminar(@Param('id') id: string) {
    return await this.repuestosService.eliminar(id);
  }
}
```

Crear `backend/src/repuestos/repuestos.module.ts`:

```ts
import { Module } from '@nestjs/common';
import { TypeOrmModule } from '@nestjs/typeorm';
import { Repuesto } from './entities/repuesto.entity';
import { Trabajo } from '../trabajos/entities/trabajo.entity';
import { RepuestosService } from './repuestos.service';
import { RepuestosController } from './repuestos.controller';

@Module({
  imports: [TypeOrmModule.forFeature([Repuesto, Trabajo])],
  providers: [RepuestosService],
  controllers: [RepuestosController],
  exports: [TypeOrmModule],
})
export class RepuestosModule {}
```

Y agregarlo a `imports` en `backend/src/app.module.ts`, después de `TrabajosModule`.

- [ ] **Step 6: Escribir el DTO de aprobación**

Crear `backend/src/ordenes/dto/registrar-aprobacion.dto.ts`:

```ts
import { Type } from 'class-transformer';
import { ArrayNotEmpty, IsArray, IsBoolean, IsUUID, ValidateNested } from 'class-validator';

export class DecisionDto {
  @IsUUID('4', { message: 'El trabajo indicado no es válido' })
  trabajo_id!: string;

  @IsBoolean({ message: 'La decisión debe ser verdadero o falso' })
  aprobado!: boolean;
}

export class RegistrarAprobacionDto {
  @IsArray()
  @ArrayNotEmpty({ message: 'Hay que registrar al menos una decisión' })
  @ValidateNested({ each: true })
  @Type(() => DecisionDto)
  decisiones!: DecisionDto[];
}
```

- [ ] **Step 7: Registrar la aprobación en bloque**

En `backend/src/trabajos/trabajos.service.ts`, agregar el método. Va aquí y no en
`OrdenesService` porque es donde vive la transacción con bloqueo y la derivación:

```ts
  /**
   * Registra la respuesta del cliente sobre varios trabajos a la vez, que es
   * como ocurre: dice que sí a tres cosas y que no a una, en una conversación.
   * Una transacción y una sola derivación al final.
   */
  async registrarAprobacion(ordenId: string, decisiones: DecisionDto[]) {
    return await this.conOrdenBloqueada(ordenId, async (manager) => {
      const ids = decisiones.map((decision) => decision.trabajo_id);

      const trabajos = await manager.find(Trabajo, {
        where: { id: In(ids), orden: { id: ordenId } },
      });

      if (trabajos.length !== ids.length) {
        throw new BadRequestException(
          'Alguno de los trabajos indicados no pertenece a esta orden',
        );
      }

      const sinCotizar = trabajos.find((trabajo) => trabajo.precio_mano_obra == null);
      if (sinCotizar) {
        throw new ConflictException(
          `El trabajo "${sinCotizar.titulo}" no tiene precio: no se puede autorizar lo que no se cotizó`,
        );
      }

      for (const trabajo of trabajos) {
        const decision = decisiones.find((d) => d.trabajo_id === trabajo.id)!;
        trabajo.aprobado = decision.aprobado;
      }

      return await manager.save(trabajos);
    });
  }
```

Añadir `BadRequestException` al import de `@nestjs/common`, `In` al de `typeorm`,
y `import { DecisionDto } from '../ordenes/dto/registrar-aprobacion.dto';`.

- [ ] **Step 8: Exponer el endpoint en el controlador de órdenes**

En `backend/src/ordenes/ordenes.controller.ts`, inyectar `TrabajosService` y
agregar, junto a `entregar` y `cancelar`:

```ts
  @Patch(':id/aprobacion')
  @Roles(NombreRol.ADMINISTRADOR, NombreRol.JEFE_TALLER, NombreRol.ASESOR)
  async registrarAprobacion(
    @Param('id') id: string,
    @Body() dto: RegistrarAprobacionDto,
  ) {
    return await this.trabajosService.registrarAprobacion(id, dto.decisiones);
  }
```

Y en `backend/src/ordenes/ordenes.module.ts`, agregar `TrabajosModule` a
`imports`. No hay ciclo: `TrabajosModule` no importa `OrdenesModule`.

`TrabajosModule` debe exportar `TrabajosService` — añadirlo a su array `exports`.

- [ ] **Step 9: Incluir los totales en las respuestas**

En `backend/src/ordenes/ordenes.service.ts`, importar `calcularTotales` y, en
`obtenerTodas`, agregar a `relations` y `select`:

```ts
      relations: { creado_por: true, vehiculo: true, trabajos: { repuestos: true } },
```

```ts
        trabajos: {
          id: true,
          precio_mano_obra: true,
          aprobado: true,
          repuestos: { id: true, cantidad: true, precio_unitario: true },
        },
```

Y mapear el resultado, quitando los trabajos de la respuesta: se cargan para la
suma, no para viajar por la red.

```ts
    return ordenes.map(({ trabajos, ...orden }) => ({
      ...orden,
      totales: calcularTotales(trabajos ?? []),
    }));
```

En `obtenerPorId`, añadir lo mismo a `relations` y devolver:

```ts
    const { trabajos, ...resto } = orden;
    return { ...resto, totales: calcularTotales(trabajos ?? []) };
```

`obtenerPorId` la usan también `entregar` y `cancelar`, que necesitan la entidad
para guardarla. Para no romperlas, dejar `obtenerPorId` devolviendo la entidad y
crear un método aparte que agregue los totales:

```ts
  async obtenerDetalle(id: string) {
    const orden = await this.ordenRepository.findOne({
      where: { id },
      relations: { creado_por: true, vehiculo: true, trabajos: { repuestos: true } },
    });

    if (!orden) {
      throw new NotFoundException(`No existe la orden con id ${id}`);
    }

    const { trabajos, ...resto } = orden;
    return { ...resto, totales: calcularTotales(trabajos ?? []) };
  }
```

El controlador de `GET /ordenes/:id` pasa a llamar a `obtenerDetalle`.

En `backend/src/vehiculos/vehiculos.service.ts`, `obtenerConHistorial` agrega
`relations: { trabajos: { repuestos: true } }` a su consulta de órdenes y mapea
igual, reemplazando el `presupuesto` que quitó la Task 2 por `totales`.

- [ ] **Step 10: Correr el e2e para verificar que pasa**

```bash
cd backend && npm run test:e2e -- cotizacion
```

Esperado: PASS, 9 tests.

- [ ] **Step 11: Correr toda la suite**

```bash
cd backend && npm test && npm run test:e2e
```

Esperado: todo verde. Si `flujo-orden` falla al mover un trabajo, es porque sus
trabajos ahora nacen sin cotizar y el bloqueo los rechaza: actualizar ese spec
para cotizar y aprobar antes de mover.

- [ ] **Step 12: Commit**

```bash
git add backend/src backend/test
git commit -m "feat: repuestos, aprobación en bloque y totales en la API"
```

---

### Task 5: Panel de cotización

**Files:**
- Create: `frontend/src/app/core/models/repuesto.model.ts`
- Create: `frontend/src/app/core/services/repuesto.ts`
- Create: `frontend/src/app/features/ordenes/panel-cotizacion/panel-cotizacion.ts`
- Create: `frontend/src/app/features/ordenes/panel-cotizacion/panel-cotizacion.html`
- Modify: `frontend/src/app/core/models/trabajo.model.ts`, `orden.model.ts`
- Modify: `frontend/src/app/features/trabajos/formulario-trabajo/*`
- Modify: `frontend/src/app/features/ordenes/detalle-orden/*`

**Interfaces:**
- Consumes: `POST /repuestos/trabajo/:trabajoId`, `DELETE /repuestos/:id`, y los campos nuevos de `GET /trabajos/orden/:ordenId`.
- Produces:
  - `interface Repuesto { id, descripcion, cantidad, precio_unitario }`
  - `interface Totales { aprobado: number; pendiente: number; rechazado: number }`
  - `RepuestoService.crear(trabajoId, datos)` y `.eliminar(id)`
  - Componente `PanelCotizacion` con input `ordenId` y output `cotizacionCambiada`.

- [ ] **Step 1: Escribir los modelos**

Crear `frontend/src/app/core/models/repuesto.model.ts`:

```ts
export interface Repuesto {
  id: string;
  descripcion: string;
  cantidad: number;
  precio_unitario: number;
}

export interface RepuestoRequest {
  descripcion: string;
  cantidad: number;
  precio_unitario: number;
}

export interface Totales {
  aprobado: number;
  pendiente: number;
  rechazado: number;
}
```

En `core/models/trabajo.model.ts`, agregar a `Trabajo`:

```ts
  precio_mano_obra?: number;
  aprobado?: boolean | null;
  repuestos?: Repuesto[];
```

En `core/models/orden.model.ts`, agregar a `Orden` el campo `totales: Totales;`
con su import. En `core/models/trabajo.model.ts`, agregar `precio_mano_obra?: number;`
a `TrabajoRequest`, y `subtotal?: number;` a `Trabajo` — lo calcula la API.

- [ ] **Step 2: Escribir el servicio de repuestos**

Crear `frontend/src/app/core/services/repuesto.ts`:

```ts
import { HttpClient } from '@angular/common/http';
import { inject, Injectable } from '@angular/core';
import { Observable } from 'rxjs';

import { environment } from '../../../environments/environment';
import { Repuesto, RepuestoRequest } from '../models/repuesto.model';
import { MensajeResponse } from '../models/auth.model';

@Injectable({
  providedIn: 'root',
})
export class RepuestoService {
  private readonly http = inject(HttpClient);
  private readonly url = `${environment.apiUrl}/repuestos`;

  crear(trabajoId: string, datos: RepuestoRequest): Observable<Repuesto> {
    return this.http.post<Repuesto>(`${this.url}/trabajo/${trabajoId}`, datos);
  }

  eliminar(id: string): Observable<MensajeResponse> {
    return this.http.delete<MensajeResponse>(`${this.url}/${id}`);
  }
}
```

- [ ] **Step 3: Agregar el precio al formulario de trabajo**

En `formulario-trabajo.ts`, agregar al `FormGroup`:

```ts
    precio_mano_obra: [null as number | null, [Validators.min(0)]],
```

y en `enviar`, dentro del armado de `datos`:

```ts
    if (valores.precio_mano_obra !== null && valores.precio_mano_obra !== undefined) {
      datos.precio_mano_obra = valores.precio_mano_obra;
    }
```

En `formulario-trabajo.html`, junto a la prioridad:

```html
<div class="col-md-4 mb-3">
  <label class="form-label" for="precio_mano_obra">Mano de obra (S/)</label>
  <input id="precio_mano_obra" type="number" step="0.01" min="0" class="form-control"
         formControlName="precio_mano_obra" placeholder="Sin cotizar" />
  <small class="text-muted">Déjalo vacío si todavía no cotizas. Usa 0 si no cobras mano de obra.</small>
</div>
```

- [ ] **Step 4: Escribir el panel de cotización**

Crear `frontend/src/app/features/ordenes/panel-cotizacion/panel-cotizacion.ts`:

```ts
import { Component, inject, input, OnInit, output, signal } from '@angular/core';
import { FormBuilder, ReactiveFormsModule, Validators } from '@angular/forms';
import { CurrencyPipe } from '@angular/common';

import { TrabajoService } from '../../../core/services/trabajo';
import { RepuestoService } from '../../../core/services/repuesto';
import { TokenService } from '../../../core/services/token';
import { Trabajo } from '../../../core/models/trabajo.model';
import { Totales } from '../../../core/models/repuesto.model';
import { ROLES } from '../../../core/models/estados';

@Component({
  selector: 'app-panel-cotizacion',
  imports: [ReactiveFormsModule, CurrencyPipe],
  templateUrl: './panel-cotizacion.html',
  styles: ``,
})
export class PanelCotizacion implements OnInit {
  private readonly fb = inject(FormBuilder);
  private readonly trabajoService = inject(TrabajoService);
  private readonly repuestoService = inject(RepuestoService);
  private readonly tokenService = inject(TokenService);

  readonly ordenId = input.required<string>();
  readonly totales = input.required<Totales>();

  readonly cotizacionCambiada = output<void>();

  readonly trabajos = signal<Trabajo[]>([]);
  readonly mensajeError = signal<string | null>(null);
  readonly trabajoEnEdicion = signal<string | null>(null);

  readonly formularioRepuesto = this.fb.nonNullable.group({
    descripcion: ['', [Validators.required]],
    cantidad: [1, [Validators.required, Validators.min(1)]],
    precio_unitario: [0, [Validators.required, Validators.min(0)]],
  });

  ngOnInit(): void {
    this.cargar();
  }

  cargar(): void {
    this.trabajoService.obtenerPorOrden(this.ordenId()).subscribe({
      next: (datos) => this.trabajos.set(datos),
      error: () => this.mensajeError.set('No se pudieron cargar los trabajos'),
    });
  }

  puedeCotizar(): boolean {
    return this.tokenService.tieneRol(ROLES.ADMINISTRADOR, ROLES.JEFE_TALLER);
  }

  abrirEditor(trabajoId: string): void {
    this.trabajoEnEdicion.set(trabajoId);
    this.formularioRepuesto.reset({ descripcion: '', cantidad: 1, precio_unitario: 0 });
  }

  cerrarEditor(): void {
    this.trabajoEnEdicion.set(null);
  }

  agregarRepuesto(): void {
    const trabajoId = this.trabajoEnEdicion();
    if (!trabajoId || this.formularioRepuesto.invalid) {
      this.formularioRepuesto.markAllAsTouched();
      return;
    }

    this.mensajeError.set(null);

    this.repuestoService.crear(trabajoId, this.formularioRepuesto.getRawValue()).subscribe({
      next: () => {
        this.cerrarEditor();
        this.cargar();
        this.cotizacionCambiada.emit();
      },
      error: (error) => this.mensajeError.set(error.error?.message ?? 'No se pudo agregar'),
    });
  }

  eliminarRepuesto(id: string): void {
    this.repuestoService.eliminar(id).subscribe({
      next: () => {
        this.cargar();
        this.cotizacionCambiada.emit();
      },
      error: (error) => this.mensajeError.set(error.error?.message ?? 'No se pudo eliminar'),
    });
  }
}
```

- [ ] **Step 5: Escribir la plantilla del panel**

Crear `frontend/src/app/features/ordenes/panel-cotizacion/panel-cotizacion.html`:

```html
<div class="card shadow-sm mb-4">
  <div class="card-header fw-semibold">Cotización</div>

  <div class="card-body">
    @if (mensajeError()) {
      <div class="alert alert-danger">{{ mensajeError() }}</div>
    }

    @if (trabajos().length === 0) {
      <p class="text-muted mb-0">Esta orden todavía no tiene trabajos.</p>
    } @else {
      @for (trabajo of trabajos(); track trabajo.id) {
        <div class="border-bottom pb-3 mb-3">
          <div class="d-flex justify-content-between">
            <div>
              <strong>{{ trabajo.titulo }}</strong>
              @if (trabajo.precio_mano_obra === undefined || trabajo.precio_mano_obra === null) {
                <span class="badge bg-secondary ms-2">Sin cotizar</span>
              } @else if (trabajo.aprobado === true) {
                <span class="badge bg-success ms-2">Aprobado</span>
              } @else if (trabajo.aprobado === false) {
                <span class="badge bg-danger ms-2">Rechazado</span>
              } @else {
                <span class="badge bg-warning text-dark ms-2">Esperando respuesta</span>
              }
            </div>
            <strong>{{ trabajo.subtotal | currency: 'PEN' }}</strong>
          </div>

          @if (trabajo.precio_mano_obra !== undefined && trabajo.precio_mano_obra !== null) {
            <div class="small text-muted mt-1">
              Mano de obra: {{ trabajo.precio_mano_obra | currency: 'PEN' }}
            </div>

            @for (repuesto of trabajo.repuestos ?? []; track repuesto.id) {
              <div class="small d-flex justify-content-between mt-1">
                <span>
                  {{ repuesto.cantidad }} × {{ repuesto.descripcion }}
                  ({{ repuesto.precio_unitario | currency: 'PEN' }} c/u)
                </span>
                <span>
                  {{ repuesto.cantidad * repuesto.precio_unitario | currency: 'PEN' }}
                  @if (puedeCotizar()) {
                    <button class="btn btn-sm btn-link text-danger p-0 ms-2"
                            (click)="eliminarRepuesto(repuesto.id)">quitar</button>
                  }
                </span>
              </div>
            }

            @if (puedeCotizar()) {
              @if (trabajoEnEdicion() === trabajo.id) {
                <form class="row g-2 mt-2" [formGroup]="formularioRepuesto"
                      (ngSubmit)="agregarRepuesto()">
                  <div class="col-md-6">
                    <input class="form-control form-control-sm" placeholder="Repuesto"
                           formControlName="descripcion" />
                  </div>
                  <div class="col-md-2">
                    <input class="form-control form-control-sm" type="number" min="1"
                           formControlName="cantidad" />
                  </div>
                  <div class="col-md-2">
                    <input class="form-control form-control-sm" type="number" step="0.01" min="0"
                           formControlName="precio_unitario" />
                  </div>
                  <div class="col-md-2 d-flex gap-1">
                    <button class="btn btn-sm btn-primary" type="submit">Agregar</button>
                    <button class="btn btn-sm btn-outline-secondary" type="button"
                            (click)="cerrarEditor()">×</button>
                  </div>
                </form>
              } @else {
                <button class="btn btn-sm btn-outline-primary mt-2"
                        (click)="abrirEditor(trabajo.id)">
                  + Repuesto
                </button>
              }
            }
          }
        </div>
      }

      <div class="row text-end">
        <div class="col">
          <div class="small text-muted">Aprobado</div>
          <div class="fw-bold text-success">{{ totales().aprobado | currency: 'PEN' }}</div>
        </div>
        <div class="col">
          <div class="small text-muted">Esperando respuesta</div>
          <div class="fw-bold">{{ totales().pendiente | currency: 'PEN' }}</div>
        </div>
        <div class="col">
          <div class="small text-muted">Rechazado</div>
          <div class="fw-bold text-muted">{{ totales().rechazado | currency: 'PEN' }}</div>
        </div>
      </div>
    }
  </div>
</div>
```

- [ ] **Step 6: Enganchar el panel en el detalle de orden**

En `detalle-orden.html`, encima de `<h5 class="mb-3">Trabajos de la orden</h5>`:

```html
<app-panel-cotizacion
  [ordenId]="datosOrden.id"
  [totales]="datosOrden.totales"
  (cotizacionCambiada)="refrescarOrden()"
></app-panel-cotizacion>
```

Y añadir `PanelCotizacion` a los `imports` del componente `DetalleOrden`.

- [ ] **Step 7: Compilar y verificar en el navegador**

```bash
cd frontend && npm run build
```

Después, con la aplicación levantada y sesión de `jefe@taller.com`: crear una
orden, agregarle un trabajo con mano de obra `100`, cargarle un repuesto de
`4 × 25.50`, y comprobar que el subtotal dice `S/ 202.00`, que el total de
"esperando respuesta" coincide, y que el badge de la orden pasó a `Cotizada`.
Comprobar también que "+ Repuesto" no aparece sobre un trabajo sin cotizar.

- [ ] **Step 8: Commit**

```bash
git add frontend/src/app
git commit -m "feat: panel de cotización con desglose, repuestos y totales"
```

---

### Task 6: Respuesta del cliente y Kanban

**Files:**
- Create: `frontend/src/app/features/ordenes/panel-aprobacion/panel-aprobacion.ts`
- Create: `frontend/src/app/features/ordenes/panel-aprobacion/panel-aprobacion.html`
- Modify: `frontend/src/app/core/services/orden.ts`
- Modify: `frontend/src/app/features/ordenes/detalle-orden/*`
- Modify: `frontend/src/app/features/trabajos/tablero-kanban/*`
- Modify: `frontend/src/app/features/ordenes/lista-ordenes/lista-ordenes.html`
- Modify: `frontend/src/app/features/dashboard/dashboard.html`
- Modify: `frontend/src/app/features/vehiculos/ficha-vehiculo/ficha-vehiculo.html`

**Interfaces:**
- Consumes: `PATCH /ordenes/:id/aprobacion` de la Task 4; el panel de la Task 5.
- Produces: `OrdenService.registrarAprobacion(id, decisiones)`.

- [ ] **Step 1: Añadir el método al servicio de órdenes**

En `frontend/src/app/core/services/orden.ts`:

```ts
  registrarAprobacion(
    id: string,
    decisiones: { trabajo_id: string; aprobado: boolean }[],
  ): Observable<unknown> {
    return this.http.patch(`${this.url}/${id}/aprobacion`, { decisiones });
  }
```

- [ ] **Step 2: Escribir el panel de aprobación**

Crear `frontend/src/app/features/ordenes/panel-aprobacion/panel-aprobacion.ts`:

```ts
import { Component, inject, input, OnInit, output, signal } from '@angular/core';
import { CurrencyPipe } from '@angular/common';

import { OrdenService } from '../../../core/services/orden';
import { TrabajoService } from '../../../core/services/trabajo';
import { Trabajo } from '../../../core/models/trabajo.model';

@Component({
  selector: 'app-panel-aprobacion',
  imports: [CurrencyPipe],
  templateUrl: './panel-aprobacion.html',
  styles: ``,
})
export class PanelAprobacion implements OnInit {
  private readonly ordenService = inject(OrdenService);
  private readonly trabajoService = inject(TrabajoService);

  readonly ordenId = input.required<string>();

  readonly respuestaRegistrada = output<void>();

  readonly pendientes = signal<Trabajo[]>([]);
  readonly decisiones = signal<Record<string, boolean>>({});
  readonly guardando = signal<boolean>(false);
  readonly mensajeError = signal<string | null>(null);

  ngOnInit(): void {
    this.trabajoService.obtenerPorOrden(this.ordenId()).subscribe({
      next: (todos) => {
        const esperando = todos.filter(
          (trabajo) =>
            trabajo.precio_mano_obra !== undefined &&
            trabajo.precio_mano_obra !== null &&
            (trabajo.aprobado === undefined || trabajo.aprobado === null),
        );
        this.pendientes.set(esperando);
        // Por defecto, todo aprobado: es la respuesta más frecuente.
        this.decisiones.set(Object.fromEntries(esperando.map((t) => [t.id, true])));
      },
      error: () => this.mensajeError.set('No se pudieron cargar los trabajos'),
    });
  }

  marcar(trabajoId: string, aprobado: boolean): void {
    this.decisiones.update((actual) => ({ ...actual, [trabajoId]: aprobado }));
  }

  registrar(): void {
    this.guardando.set(true);
    this.mensajeError.set(null);

    const decisiones = Object.entries(this.decisiones()).map(([trabajo_id, aprobado]) => ({
      trabajo_id,
      aprobado,
    }));

    this.ordenService.registrarAprobacion(this.ordenId(), decisiones).subscribe({
      next: () => {
        this.guardando.set(false);
        this.respuestaRegistrada.emit();
      },
      error: (error) => {
        this.guardando.set(false);
        this.mensajeError.set(error.error?.message ?? 'No se pudo registrar la respuesta');
      },
    });
  }
}
```

- [ ] **Step 3: Escribir su plantilla**

Crear `frontend/src/app/features/ordenes/panel-aprobacion/panel-aprobacion.html`:

```html
<div class="card border-warning shadow-sm mb-4">
  <div class="card-header bg-warning-subtle fw-semibold">Respuesta del cliente</div>

  <div class="card-body">
    @if (mensajeError()) {
      <div class="alert alert-danger">{{ mensajeError() }}</div>
    }

    @if (pendientes().length === 0) {
      <p class="text-muted mb-0">No hay trabajos esperando respuesta.</p>
    } @else {
      @for (trabajo of pendientes(); track trabajo.id) {
        <div class="d-flex justify-content-between align-items-center mb-2">
          <span>
            {{ trabajo.titulo }}
            <span class="text-muted small ms-2">
              {{ trabajo.precio_mano_obra | currency: 'PEN' }} + repuestos
            </span>
          </span>
          <div class="btn-group btn-group-sm">
            <button class="btn"
                    [class.btn-success]="decisiones()[trabajo.id] === true"
                    [class.btn-outline-success]="decisiones()[trabajo.id] !== true"
                    (click)="marcar(trabajo.id, true)">Aprueba</button>
            <button class="btn"
                    [class.btn-danger]="decisiones()[trabajo.id] === false"
                    [class.btn-outline-danger]="decisiones()[trabajo.id] !== false"
                    (click)="marcar(trabajo.id, false)">Rechaza</button>
          </div>
        </div>
      }

      <button class="btn btn-warning mt-3" [disabled]="guardando()" (click)="registrar()">
        Registrar la respuesta
      </button>
    }
  </div>
</div>
```

- [ ] **Step 4: Mostrarlo cuando la orden está cotizada**

En `detalle-orden.html`, encima del panel de cotización:

```html
@if (datosOrden.estado === 'COTIZADA' && puedeAprobar()) {
  <app-panel-aprobacion
    [ordenId]="datosOrden.id"
    (respuestaRegistrada)="refrescarOrden()"
  ></app-panel-aprobacion>
}
```

En `detalle-orden.ts`, añadir `PanelAprobacion` a `imports` y el método:

```ts
  puedeAprobar(): boolean {
    return this.tokenService.tieneRol(ROLES.ADMINISTRADOR, ROLES.JEFE_TALLER, ROLES.ASESOR);
  }
```

`refrescarOrden` ya recarga la orden; para que el Kanban y la cotización también
se actualicen, llamar además a `this.tablero()?.cargarTrabajos()` dentro de él.

- [ ] **Step 5: Atenuar los trabajos sin aprobar en el Kanban**

En `tablero-kanban.ts`, añadir:

```ts
  estaAprobado(trabajo: Trabajo): boolean {
    return trabajo.aprobado === true;
  }
```

y en `puedeMover`, anteponer la comprobación:

```ts
  puedeMover(trabajo: Trabajo): boolean {
    if (!this.estaAprobado(trabajo)) return false;
    if (this.tokenService.tieneRol(ROLES.ADMINISTRADOR, ROLES.JEFE_TALLER)) {
      return true;
    }
    return trabajo.asignado_a?.id === this.tokenService.usuario()?.id;
  }
```

En `tablero-kanban.html`, en la tarjeta, añadir la clase de atenuado y la
etiqueta:

```html
[class.opacity-50]="!estaAprobado(trabajo)"
```

```html
@if (!estaAprobado(trabajo)) {
  <span class="badge bg-secondary">Sin aprobar</span>
}
```

- [ ] **Step 6: Poner los totales donde estaba el presupuesto**

- `lista-ordenes.html`: en la celda del presupuesto que la Task 2 vació,
  `{{ orden.totales.aprobado | currency: 'PEN' }}`.
- `detalle-orden.html`: en el bloque "Servicio",
  `Aprobado: {{ datosOrden.totales.aprobado | currency: 'PEN' }}`.
- `ficha-vehiculo.html`: la columna "Presupuesto" pasa a "Aprobado" con
  `{{ orden.totales.aprobado | currency: 'PEN' }}`, y en `vehiculo.model.ts`,
  `OrdenDelHistorial` gana `totales: Totales;`.

- [ ] **Step 7: Compilar y verificar el ciclo en el navegador**

```bash
cd frontend && npm run build
```

Con la aplicación levantada, como `jefe@taller.com`:

1. Cotizar dos trabajos de una orden; el badge pasa a `Cotizada`.
2. Comprobar que las tarjetas del Kanban están atenuadas y sus flechas deshabilitadas.
3. En el panel de respuesta, aprobar uno y rechazar el otro, y registrar.
4. La orden vuelve a `Recibida`, el total aprobado refleja solo el aprobado, y el rechazado queda en su columna.
5. Mover el trabajo aprobado: la orden pasa a `En proceso`. La tarjeta del rechazado sigue atenuada.
6. Completar el aprobado: la orden pasa a `Finalizada` pese al rechazado.

- [ ] **Step 8: Commit**

```bash
git add frontend/src/app
git commit -m "feat: registrar la respuesta del cliente y bloquear el Kanban sin aprobación"
```

---

### Task 7: Documentación y cierre

**Files:**
- Modify: `README.md`, `docs/modelo-er.md`, `docs/endpoints.http`, `docs/contexto-core.md`

**Interfaces:**
- Consumes: todo lo anterior.
- Produces: nada.

- [ ] **Step 1: Actualizar el README**

En "De qué va", después de la viñeta del jefe de taller:

> - El cliente aprueba o rechaza cada trabajo cotizado, y hasta que responde
>   nadie puede empezar.

En "Notas técnicas", al final:

> El presupuesto no es un número que se escriba a mano: cada trabajo lleva su
> precio de mano de obra y sus repuestos, y el total de la orden es la suma.
> `null` en el precio significa "sin cotizar" y `0` es un precio válido, así que
> poner el precio —aunque sea cero— es lo que declara el trabajo cotizado. Por
> eso cargar un repuesto en un trabajo sin precio responde `409`: si no, ese
> dinero no aparecería en ningún total.
>
> Una orden con trabajos cotizados esperando respuesta está `COTIZADA`, y un
> trabajo sin aprobar no se puede mover en el Kanban. Aprobar no es empezar: si
> nadie ha movido nada, la orden vuelve a `RECIBIDA` hasta que el mecánico
> arranque.

- [ ] **Step 2: Actualizar el modelo E-R**

- Diagrama: agregar `REPUESTOS` colgando de `TRABAJOS`.
- Tabla de relaciones: `| Repuesto | Trabajo | N:1 | @ManyToOne con onDelete: CASCADE |`.
- Sección "Estados": agregar `COTIZADA` y las reglas de derivación nuevas.
- Script SQL: agregar `CREATE TABLE repuestos (...)`, añadir `precio_mano_obra NUMERIC(10,2)` y `aprobado BOOLEAN` a `trabajos`, y quitar `presupuesto` de `ordenes`.

- [ ] **Step 3: Actualizar la colección de endpoints**

Quitar `"presupuesto"` del cuerpo de "Crear una orden" y agregar al final:

```
### ─────────────── COTIZACIÓN ───────────────

### Cotizar un trabajo (0 es válido; vacío = sin cotizar)
PATCH {{api}}/trabajos/ID_DEL_TRABAJO
Authorization: Bearer {{token}}
Content-Type: application/json

{ "precio_mano_obra": 120.50 }

### Agregar un repuesto (409 si el trabajo no tiene precio de mano de obra)
POST {{api}}/repuestos/trabajo/ID_DEL_TRABAJO
Authorization: Bearer {{token}}
Content-Type: application/json

{ "descripcion": "Filtro de aceite", "cantidad": 1, "precio_unitario": 35 }

### Quitar un repuesto
DELETE {{api}}/repuestos/ID_DEL_REPUESTO
Authorization: Bearer {{token}}

### Registrar la respuesta del cliente, en bloque
PATCH {{api}}/ordenes/ID_DE_LA_ORDEN/aprobacion
Authorization: Bearer {{token}}
Content-Type: application/json

{
  "decisiones": [
    { "trabajo_id": "ID_DEL_TRABAJO_A", "aprobado": true },
    { "trabajo_id": "ID_DEL_TRABAJO_B", "aprobado": false }
  ]
}
```

- [ ] **Step 4: Actualizar el contexto exportado**

En `docs/contexto-core.md`:

- Sección 1: el flujo incorpora la cotización y la respuesta del cliente.
- Sección 3: `repuestos` en el diagrama y en la lista de entidades; `trabajos`
  gana las dos columnas; `ordenes` pierde `presupuesto`.
- Sección 3, "Máquinas de estado": la tabla de derivación nueva con `COTIZADA`.
- Sección 5: las cuatro rutas nuevas.
- Sección 7: la regla de qué significa "cotizado" y por qué `0` no es `null`.
- Sección 8: mover a "Cerrados" el hueco del presupuesto sin ítems ni aprobación,
  y dejar abiertos los que el spec lista como límites conocidos (sin historial de
  quién aprobó, sin costo real, sin inventario).

- [ ] **Step 5: Verificación final de punta a punta**

```bash
docker compose down -v && docker compose up -d
cd backend && npm run start:dev     # aplica las 7 migraciones y el seed
cd frontend && npm start
```

Recorrido con los cuatro usuarios:

1. `asesor@taller.com` registra una orden. Estado `Recibida`, sin totales.
2. `jefe@taller.com` crea dos trabajos con precio y le carga repuestos a uno. La orden pasa a `Cotizada` y el total de "esperando respuesta" cuadra con la suma a mano.
3. `mecanico@taller.com` no puede mover ninguna tarjeta: están atenuadas.
4. `asesor@taller.com` registra la respuesta: aprueba uno, rechaza el otro. La orden vuelve a `Recibida` y los totales se reparten.
5. El mecánico mueve el aprobado: `En proceso`. Lo completa: `Finalizada`, pese al rechazado.
6. `asesor@taller.com` entrega la orden.

- [ ] **Step 6: Commit**

```bash
git add -A
git commit -m "docs: documentar la cotización y la aprobación del cliente"
```

- [ ] **Step 7: Merge**

```bash
git checkout master && git merge --no-ff fase-2 -m "merge: fase 2 — cotización con desglose y aprobación del cliente"
```
