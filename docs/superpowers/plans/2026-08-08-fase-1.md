# Fase 1 — Plan de implementación

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Extraer el vehículo de la tabla `ordenes` como entidad propia, para que escribir una placa conocida autocomplete los datos y dé acceso al historial de ese auto.

**Architecture:** Una entidad `Vehiculo` con la placa como clave natural y el propietario adentro; `ordenes` cuelga de ella. La lógica delicada —normalizar la placa y detectar diferencias— vive en dos funciones puras testeables sin base de datos. El vehículo se resuelve con find-or-create dentro de la transacción que escribe la orden, y ante datos distintos la API responde `409` con el detalle en vez de pisar el histórico.

**Tech Stack:** NestJS 11, TypeORM 1.1.0, PostgreSQL 16, Jest 30 + ts-jest, Angular 20 (signals, rxjs), Bootstrap 5.

**Spec:** `docs/superpowers/specs/2026-08-08-fase-1-design.md`

## Global Constraints

- Rama de trabajo: `fase-1`. No commitear a `master`.
- Mensajes de commit en español, conventional commits en minúscula (`feat:`, `fix:`, `docs:`, `chore:`, `test:`).
- Todo commit termina con el trailer `Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>`.
- Tests unitarios del backend: `backend/src/**/*.spec.ts`, se corren con `npm test` desde `backend/`.
- Tests e2e: `backend/test/*.e2e-spec.ts`, con `npm run test:e2e`. **Requieren PostgreSQL** (`docker compose up -d` desde la raíz) y el seed cargado.
- **Nunca correr `npm run lint`**: en este proyecto es `eslint --fix` sobre todo `src` y reformatea archivos ajenos. Para revisar, `npx eslint <ruta>` sin `--fix`.
- El esquema se administra con migraciones. `synchronize` está apagado; cambiar una entidad **no** cambia la base. Migraciones a mano en `backend/src/migrations/`, con timestamp de `date +%s000` y el mismo número en el nombre de la clase.
- `backend/tsconfig.json` tiene `noImplicitAny: false` y `strictNullChecks: true`.
- Un tipo usado en una firma decorada debe importarse con `import type` (TS1272, por `isolatedModules` + `emitDecoratorMetadata`).
- Nombres de dominio en español (`vehiculo`, `placa`, `propietario`).
- **Normalización de la placa:** mayúsculas y solo letras y dígitos. `ABC-123` se guarda y se muestra como `ABC123`.
- La vigencia de las URL firmadas y el límite de subida no cambian en esta fase.

---

## Estructura de archivos

**Se crean**

| Archivo | Responsabilidad |
|---|---|
| `backend/src/vehiculos/placa.ts` | `normalizarPlaca`, función pura |
| `backend/src/vehiculos/placa.spec.ts` | Sus tests |
| `backend/src/vehiculos/comparar-vehiculo.ts` | `compararVehiculo` y sus tipos, función pura |
| `backend/src/vehiculos/comparar-vehiculo.spec.ts` | Sus tests |
| `backend/src/vehiculos/entities/vehiculo.entity.ts` | La entidad |
| `backend/src/vehiculos/vehiculos.service.ts` | Lectura y `resolverParaOrden` |
| `backend/src/vehiculos/vehiculos.controller.ts` | Los dos endpoints de lectura |
| `backend/src/vehiculos/vehiculos.module.ts` | Módulo |
| `backend/src/migrations/*-CrearVehiculos.ts` | Tabla, backfill y `vehiculo_id` nullable |
| `backend/src/migrations/*-SoltarDatosDeVehiculoEnOrdenes.ts` | `NOT NULL`, FK y `DROP COLUMN` |
| `backend/test/vehiculo.e2e-spec.ts` | e2e del flujo completo |
| `frontend/src/app/core/models/vehiculo.model.ts` | Modelo |
| `frontend/src/app/core/services/vehiculo.ts` | Servicio HTTP |
| `frontend/src/app/features/vehiculos/ficha-vehiculo/ficha-vehiculo.ts` | Ficha e historial |
| `frontend/src/app/features/vehiculos/ficha-vehiculo/ficha-vehiculo.html` | Su plantilla |

**Se modifican**

| Archivo | Cambio |
|---|---|
| `backend/src/ordenes/entities/orden.entity.ts` | Pierde 6 campos, gana la relación `vehiculo` |
| `backend/src/ordenes/dto/crear-orden.dto.ts` | `cliente_*` → `propietario_*`, más `actualizar_vehiculo` |
| `backend/src/ordenes/ordenes.service.ts` | `crear` y `actualizar` resuelven el vehículo en transacción |
| `backend/src/ordenes/ordenes.module.ts` | Importa `VehiculosModule` |
| `backend/src/app.module.ts` | Registra `VehiculosModule` |
| `frontend/src/app/core/models/orden.model.ts` | `Orden` y `OrdenRequest` |
| `frontend/src/app/app.routes.ts` | Ruta `/vehiculos/:id` |
| `frontend/src/app/features/ordenes/formulario-orden/*` | Autocompletado y diálogo de conflicto |
| `frontend/src/app/features/ordenes/detalle-orden/*` | Lee `orden.vehiculo.*`, enlace al historial |
| `frontend/src/app/features/ordenes/lista-ordenes/lista-ordenes.html` | Lee `orden.vehiculo.placa` |
| `frontend/src/app/features/dashboard/dashboard.html` | Lee `orden.vehiculo.placa` |
| `README.md`, `docs/modelo-er.md`, `docs/endpoints.http`, `docs/contexto-core.md` | Documentación |

---

### Task 1: Lógica pura del módulo de vehículos

**Files:**
- Create: `backend/src/vehiculos/placa.ts`
- Create: `backend/src/vehiculos/placa.spec.ts`
- Create: `backend/src/vehiculos/comparar-vehiculo.ts`
- Create: `backend/src/vehiculos/comparar-vehiculo.spec.ts`

**Interfaces:**
- Consumes: nada.
- Produces:
  - `normalizarPlaca(valor: string): string`
  - `interface DatosVehiculo { marca: string; modelo: string; anio?: number | null; propietario_nombre: string; propietario_telefono: string }`
  - `interface Diferencia { campo: string; guardado: unknown; enviado: unknown }`
  - `compararVehiculo(guardado: DatosVehiculo, enviado: Partial<DatosVehiculo>): Diferencia[]`

- [ ] **Step 1: Escribir los tests de `normalizarPlaca`**

Crear `backend/src/vehiculos/placa.spec.ts`:

```ts
import { normalizarPlaca } from './placa';

describe('normalizarPlaca', () => {
  it('pasa a mayúsculas', () => {
    expect(normalizarPlaca('abc123')).toBe('ABC123');
  });

  it('descarta el guion de la placa peruana', () => {
    expect(normalizarPlaca('ABC-123')).toBe('ABC123');
  });

  it('descarta espacios internos y externos', () => {
    expect(normalizarPlaca('  abc 123  ')).toBe('ABC123');
  });

  it('descarta cualquier separador, no solo guiones y espacios', () => {
    expect(normalizarPlaca('a.b/c-1_2 3')).toBe('ABC123');
  });

  it('deja igual una placa que ya está normalizada', () => {
    expect(normalizarPlaca('ABC123')).toBe('ABC123');
  });

  it('con una cadena vacía devuelve vacío', () => {
    expect(normalizarPlaca('')).toBe('');
  });
});
```

- [ ] **Step 2: Correr los tests para verificar que fallan**

```bash
cd backend && npm test -- placa
```

Esperado: FAIL, `Cannot find module './placa'`.

- [ ] **Step 3: Escribir `normalizarPlaca`**

Crear `backend/src/vehiculos/placa.ts`:

```ts
/**
 * Una placa se guarda en una sola forma canónica: mayúsculas y solo letras y
 * dígitos. Así `abc-123`, `ABC 123` y `abc123` son el mismo auto. El costo es
 * que se pierde el guion que trae impresa la placa peruana.
 */
export function normalizarPlaca(valor: string): string {
  return valor.replace(/[^A-Za-z0-9]/g, '').toUpperCase();
}
```

- [ ] **Step 4: Correr los tests para verificar que pasan**

```bash
cd backend && npm test -- placa
```

Esperado: PASS, 6 tests.

- [ ] **Step 5: Escribir los tests de `compararVehiculo`**

Crear `backend/src/vehiculos/comparar-vehiculo.spec.ts`:

```ts
import { compararVehiculo, DatosVehiculo } from './comparar-vehiculo';

const guardado: DatosVehiculo = {
  marca: 'Toyota',
  modelo: 'Yaris',
  anio: 2021,
  propietario_nombre: 'Rosa Delgado',
  propietario_telefono: '987654321',
};

describe('compararVehiculo', () => {
  it('sin cambios no devuelve diferencias', () => {
    expect(compararVehiculo(guardado, { ...guardado })).toEqual([]);
  });

  it('detecta un campo distinto', () => {
    expect(compararVehiculo(guardado, { ...guardado, modelo: 'Corolla' })).toEqual([
      { campo: 'modelo', guardado: 'Yaris', enviado: 'Corolla' },
    ]);
  });

  it('detecta varios campos distintos', () => {
    const diferencias = compararVehiculo(guardado, {
      ...guardado,
      modelo: 'Corolla',
      propietario_nombre: 'Juan Pérez',
    });

    expect(diferencias).toHaveLength(2);
    expect(diferencias.map((d) => d.campo)).toEqual(['modelo', 'propietario_nombre']);
  });

  it('un campo ausente no cuenta como diferencia', () => {
    // El asesor dejó el año vacío: eso no significa "bórrale el año al vehículo".
    expect(compararVehiculo(guardado, { marca: 'Toyota' })).toEqual([]);
  });

  it('un año nuevo sobre un vehículo sin año sí es diferencia', () => {
    expect(compararVehiculo({ ...guardado, anio: null }, { anio: 2021 })).toEqual([
      { campo: 'anio', guardado: null, enviado: 2021 },
    ]);
  });

  it('nunca compara la placa: es la identidad', () => {
    const enviado = { ...guardado, placa: 'OTRA999' } as Partial<DatosVehiculo>;
    expect(compararVehiculo(guardado, enviado)).toEqual([]);
  });
});
```

- [ ] **Step 6: Correr los tests para verificar que fallan**

```bash
cd backend && npm test -- comparar-vehiculo
```

Esperado: FAIL, `Cannot find module './comparar-vehiculo'`.

- [ ] **Step 7: Escribir `compararVehiculo`**

Crear `backend/src/vehiculos/comparar-vehiculo.ts`:

```ts
export interface DatosVehiculo {
  marca: string;
  modelo: string;
  anio?: number | null;
  propietario_nombre: string;
  propietario_telefono: string;
}

export interface Diferencia {
  campo: string;
  guardado: unknown;
  enviado: unknown;
}

// `placa` queda fuera a propósito: es la identidad con la que se encontró el
// registro, no un dato que pueda estar en conflicto consigo mismo.
const CAMPOS_COMPARABLES: (keyof DatosVehiculo)[] = [
  'marca',
  'modelo',
  'anio',
  'propietario_nombre',
  'propietario_telefono',
];

/**
 * Devuelve qué campos del vehículo guardado difieren de los enviados.
 * Un campo ausente en `enviado` no se compara: significa "no opino", no
 * "bórralo".
 */
export function compararVehiculo(
  guardado: DatosVehiculo,
  enviado: Partial<DatosVehiculo>,
): Diferencia[] {
  return CAMPOS_COMPARABLES.filter((campo) => enviado[campo] !== undefined)
    .filter((campo) => guardado[campo] !== enviado[campo])
    .map((campo) => ({ campo, guardado: guardado[campo], enviado: enviado[campo] }));
}
```

- [ ] **Step 8: Correr los tests para verificar que pasan**

```bash
cd backend && npm test -- comparar-vehiculo
```

Esperado: PASS, 6 tests.

- [ ] **Step 9: Commit**

```bash
git add backend/src/vehiculos
git commit -m "feat: lógica pura de normalización de placa y comparación de vehículo"
```

---

### Task 2: Entidad, migración de creación y endpoints de lectura

**Files:**
- Create: `backend/src/vehiculos/entities/vehiculo.entity.ts`
- Create: `backend/src/migrations/<timestamp>-CrearVehiculos.ts`
- Create: `backend/src/vehiculos/vehiculos.service.ts`
- Create: `backend/src/vehiculos/vehiculos.controller.ts`
- Create: `backend/src/vehiculos/vehiculos.module.ts`
- Modify: `backend/src/app.module.ts` (registrar `VehiculosModule`)

**Interfaces:**
- Consumes: `normalizarPlaca` de la Task 1.
- Produces:
  - Entidad `Vehiculo` con `id`, `placa`, `marca`, `modelo`, `anio`, `propietario_nombre`, `propietario_telefono`, `ordenes`, timestamps.
  - `VehiculosService.buscarPorPlaca(placa: string): Promise<Vehiculo>` (lanza `NotFoundException`).
  - `VehiculosService.obtenerConHistorial(id: string): Promise<Vehiculo & { ordenes: ... }>`.
  - `GET /vehiculos/placa/:placa` y `GET /vehiculos/:id`.
- **Este corte es aditivo:** crea la tabla y la columna `vehiculo_id` nullable, pero no toca todavía las columnas viejas de `ordenes` ni su entidad. La aplicación sigue funcionando igual.

- [ ] **Step 1: Escribir la entidad**

Crear `backend/src/vehiculos/entities/vehiculo.entity.ts`:

```ts
import {
  Column,
  CreateDateColumn,
  Entity,
  PrimaryGeneratedColumn,
  UpdateDateColumn,
} from 'typeorm';

@Entity('vehiculos')
export class Vehiculo {
  @PrimaryGeneratedColumn('uuid')
  id!: string;

  /** Siempre normalizada: mayúsculas y solo letras y dígitos. */
  @Column({ type: 'varchar', length: 10, unique: true })
  placa!: string;

  @Column({ type: 'varchar', length: 50 })
  marca!: string;

  @Column({ type: 'varchar', length: 50 })
  modelo!: string;

  @Column({ type: 'int', nullable: true })
  anio?: number;

  @Column({ type: 'varchar', length: 150 })
  propietario_nombre!: string;

  @Column({ type: 'varchar', length: 20 })
  propietario_telefono!: string;

  @CreateDateColumn()
  created_at!: Date;

  @UpdateDateColumn()
  updated_at!: Date;
}
```

Sin relación inversa `ordenes` todavía: apunta a `orden.vehiculo`, que no existe
hasta la Task 3. Se agrega allí.

- [ ] **Step 2: Escribir la migración de creación con backfill**

Obtener el timestamp con `date +%s000` y usarlo en el nombre del archivo y de la clase.

Crear `backend/src/migrations/<timestamp>-CrearVehiculos.ts`:

```ts
import { MigrationInterface, QueryRunner } from 'typeorm';

/**
 * Crea la tabla de vehículos y la puebla desde las órdenes existentes, una
 * fila por placa normalizada. Cuando una placa aparece en varias órdenes con
 * datos distintos, gana la más reciente.
 *
 * Deja `ordenes.vehiculo_id` nullable a propósito: soltar las columnas viejas
 * es otra migración, para que el código pueda cambiar entre una y otra.
 */
export class CrearVehiculos<TIMESTAMP> implements MigrationInterface {
  public async up(queryRunner: QueryRunner): Promise<void> {
    await queryRunner.query(`
      CREATE TABLE "vehiculos" (
        "id" uuid NOT NULL DEFAULT gen_random_uuid(),
        "placa" character varying(10) NOT NULL,
        "marca" character varying(50) NOT NULL,
        "modelo" character varying(50) NOT NULL,
        "anio" integer,
        "propietario_nombre" character varying(150) NOT NULL,
        "propietario_telefono" character varying(20) NOT NULL,
        "created_at" TIMESTAMP NOT NULL DEFAULT now(),
        "updated_at" TIMESTAMP NOT NULL DEFAULT now(),
        CONSTRAINT "PK_vehiculos" PRIMARY KEY ("id"),
        CONSTRAINT "UQ_vehiculos_placa" UNIQUE ("placa")
      )
    `);

    await queryRunner.query(`
      INSERT INTO "vehiculos" ("placa", "marca", "modelo", "anio",
                               "propietario_nombre", "propietario_telefono")
      SELECT DISTINCT ON (upper(regexp_replace("placa", '[^A-Za-z0-9]', '', 'g')))
             upper(regexp_replace("placa", '[^A-Za-z0-9]', '', 'g')),
             "marca", "modelo", "anio", "cliente_nombre", "cliente_telefono"
      FROM "ordenes"
      ORDER BY upper(regexp_replace("placa", '[^A-Za-z0-9]', '', 'g')), "created_at" DESC
    `);

    await queryRunner.query(`ALTER TABLE "ordenes" ADD COLUMN "vehiculo_id" uuid`);

    await queryRunner.query(`
      UPDATE "ordenes" o SET "vehiculo_id" = v."id"
      FROM "vehiculos" v
      WHERE v."placa" = upper(regexp_replace(o."placa", '[^A-Za-z0-9]', '', 'g'))
    `);
  }

  public async down(queryRunner: QueryRunner): Promise<void> {
    await queryRunner.query(`ALTER TABLE "ordenes" DROP COLUMN "vehiculo_id"`);
    await queryRunner.query(`DROP TABLE "vehiculos"`);
  }
}
```

- [ ] **Step 3: Aplicar la migración y comprobar el backfill**

```bash
cd backend && npm run migration:run
```

Con la base vacía la tabla queda vacía, que es correcto. Para comprobar que el backfill funciona de verdad, revertir, insertar una orden de prueba y volver a aplicar:

```bash
npm run migration:revert
docker compose exec -T postgres psql -U postgres -d db_taller -c \
  "INSERT INTO ordenes (id, numero_orden, descripcion, fecha_ingreso, estado, placa, marca, modelo, cliente_nombre, cliente_telefono, creado_por) \
   SELECT gen_random_uuid(), 'ORD-TEST01', 'prueba backfill', '2026-08-08', 'RECIBIDA', 'abc-123', 'Toyota', 'Yaris', 'Rosa', '987654321', id FROM usuarios LIMIT 1;"
npm run migration:run
docker compose exec -T postgres psql -U postgres -d db_taller -c \
  "SELECT v.placa, v.marca, v.propietario_nombre, o.numero_orden FROM vehiculos v JOIN ordenes o ON o.vehiculo_id = v.id;"
```

Esperado: una fila con `placa = ABC123` (normalizada, sin guion), `Toyota`, `Rosa` y `ORD-TEST01`. Borrar la orden de prueba después:

```bash
docker compose exec -T postgres psql -U postgres -d db_taller -c \
  "DELETE FROM ordenes WHERE numero_orden = 'ORD-TEST01'; DELETE FROM vehiculos WHERE placa = 'ABC123';"
```

- [ ] **Step 4: Escribir el servicio**

Crear `backend/src/vehiculos/vehiculos.service.ts`. `obtenerConHistorial` consulta las órdenes por separado en lugar de con `relations`, para poder elegir columnas y orden sin depender del ordenamiento de relaciones de TypeORM:

```ts
import { Injectable, NotFoundException } from '@nestjs/common';
import { InjectRepository } from '@nestjs/typeorm';
import { Repository } from 'typeorm';

import { Vehiculo } from './entities/vehiculo.entity';
import { Orden } from '../ordenes/entities/orden.entity';
import { normalizarPlaca } from './placa';

@Injectable()
export class VehiculosService {
  constructor(
    @InjectRepository(Vehiculo)
    private readonly vehiculoRepository: Repository<Vehiculo>,
    @InjectRepository(Orden)
    private readonly ordenRepository: Repository<Orden>,
  ) {}

  async buscarPorPlaca(placa: string) {
    const normalizada = normalizarPlaca(placa);

    const vehiculo = await this.vehiculoRepository.findOne({
      where: { placa: normalizada },
    });

    if (!vehiculo) {
      throw new NotFoundException(`No hay ningún vehículo con la placa ${normalizada}`);
    }
    return vehiculo;
  }

  async obtenerConHistorial(id: string) {
    const vehiculo = await this.vehiculoRepository.findOne({ where: { id } });
    if (!vehiculo) {
      throw new NotFoundException(`No existe el vehículo con id ${id}`);
    }

    const ordenes = await this.ordenRepository.find({
      where: { vehiculo: { id } },
      select: {
        id: true,
        numero_orden: true,
        descripcion: true,
        estado: true,
        presupuesto: true,
        fecha_ingreso: true,
        fecha_entrega: true,
      },
      order: { created_at: 'DESC' },
    });

    return { ...vehiculo, ordenes };
  }
}
```

- [ ] **Step 5: Escribir el controlador**

Crear `backend/src/vehiculos/vehiculos.controller.ts`. Los dos endpoints son de lectura para cualquier autenticado, así que llevan `JwtAuthGuard` y ningún `@Roles`:

```ts
import { Controller, Get, Param, UseGuards } from '@nestjs/common';
import { VehiculosService } from './vehiculos.service';
import { JwtAuthGuard } from '../auth/guards/jwt-auth.guard';

@Controller('vehiculos')
@UseGuards(JwtAuthGuard)
export class VehiculosController {
  constructor(private readonly vehiculosService: VehiculosService) {}

  @Get('placa/:placa')
  async buscarPorPlaca(@Param('placa') placa: string) {
    return await this.vehiculosService.buscarPorPlaca(placa);
  }

  @Get(':id')
  async obtenerConHistorial(@Param('id') id: string) {
    return await this.vehiculosService.obtenerConHistorial(id);
  }
}
```

- [ ] **Step 6: Escribir el módulo y registrarlo**

Crear `backend/src/vehiculos/vehiculos.module.ts`:

```ts
import { Module } from '@nestjs/common';
import { TypeOrmModule } from '@nestjs/typeorm';
import { Vehiculo } from './entities/vehiculo.entity';
import { Orden } from '../ordenes/entities/orden.entity';
import { VehiculosService } from './vehiculos.service';
import { VehiculosController } from './vehiculos.controller';

@Module({
  imports: [TypeOrmModule.forFeature([Vehiculo, Orden])],
  providers: [VehiculosService],
  controllers: [VehiculosController],
  exports: [TypeOrmModule, VehiculosService],
})
export class VehiculosModule {}
```

En `backend/src/app.module.ts`, agregar `VehiculosModule` al array `imports`, antes de `OrdenesModule`, y su import arriba.

- [ ] **Step 7: Verificar los endpoints contra la API**

Arrancar la API (`npm run start:dev`) y, con un token de `jefe@taller.com`:

```bash
TOKEN=$(curl -s -X POST http://localhost:3001/auth/login \
  -H 'Content-Type: application/json' \
  -d '{"email":"jefe@taller.com","password":"123456"}' \
  | node -pe 'JSON.parse(require("fs").readFileSync(0)).access_token')

curl -s -o /dev/null -w 'placa inexistente: %{http_code}\n' \
  http://localhost:3001/vehiculos/placa/NOEXISTE -H "Authorization: Bearer $TOKEN"

curl -s -o /dev/null -w 'sin token: %{http_code}\n' \
  http://localhost:3001/vehiculos/placa/NOEXISTE
```

Esperado: `404` con token y `401` sin token.

- [ ] **Step 8: Commit**

```bash
git add backend/src/vehiculos backend/src/migrations backend/src/app.module.ts
git commit -m "feat: entidad Vehiculo, backfill desde las órdenes y consultas de lectura"
```

---

### Task 3: Las órdenes cuelgan del vehículo

**Files:**
- Create: `backend/test/vehiculo.e2e-spec.ts`
- Create: `backend/src/migrations/<timestamp>-SoltarDatosDeVehiculoEnOrdenes.ts`
- Modify: `backend/src/vehiculos/entities/vehiculo.entity.ts` (agregar `@OneToMany`)
- Modify: `backend/src/vehiculos/vehiculos.service.ts` (agregar `resolverParaOrden`)
- Modify: `backend/src/ordenes/entities/orden.entity.ts`
- Modify: `backend/src/ordenes/dto/crear-orden.dto.ts`
- Modify: `backend/src/ordenes/ordenes.service.ts`
- Modify: `backend/src/ordenes/ordenes.module.ts`

**Interfaces:**
- Consumes: `normalizarPlaca`, `compararVehiculo`, `Diferencia` de la Task 1; la entidad `Vehiculo` y `VehiculosService` de la Task 2.
- Produces:
  - `VehiculosService.resolverParaOrden(datos: DatosVehiculoEntrantes, actualizar: boolean, manager: EntityManager): Promise<Vehiculo>`
  - `interface DatosVehiculoEntrantes { placa: string; marca: string; modelo: string; anio?: number; propietario_nombre: string; propietario_telefono: string }`
  - `Orden.vehiculo: Vehiculo` en lugar de los seis campos planos.
  - `CrearOrdenDto` con `propietario_nombre`, `propietario_telefono` y `actualizar_vehiculo?: boolean`.

- [ ] **Step 1: Escribir el e2e primero**

Es una funcionalidad de integración: la prueba que importa es el flujo contra la base real. Crear `backend/test/vehiculo.e2e-spec.ts`:

```ts
import { INestApplication, ValidationPipe } from '@nestjs/common';
import { Test, TestingModule } from '@nestjs/testing';
import request from 'supertest';
import { App } from 'supertest/types';
import { AppModule } from './../src/app.module';

describe('Vehículo y historial (e2e)', () => {
  let app: INestApplication<App>;
  let token: string;
  let vehiculoId: string;
  const ordenesCreadas: string[] = [];

  const placa = `E2E${Date.now().toString().slice(-6)}`;

  const ordenBase = {
    descripcion: 'Mantenimiento preventivo',
    fecha_ingreso: '2026-08-08',
    marca: 'Toyota',
    modelo: 'Yaris',
    anio: 2021,
    propietario_nombre: 'Rosa Delgado',
    propietario_telefono: '987654321',
  };

  const crearOrden = (cuerpo: Record<string, unknown>) =>
    request(app.getHttpServer())
      .post('/ordenes')
      .set('Authorization', `Bearer ${token}`)
      .send(cuerpo);

  beforeAll(async () => {
    const moduleFixture: TestingModule = await Test.createTestingModule({
      imports: [AppModule],
    }).compile();

    app = moduleFixture.createNestApplication();
    app.useGlobalPipes(
      new ValidationPipe({ whitelist: true, forbidNonWhitelisted: true, transform: true }),
    );
    await app.init();

    const sesion = await request(app.getHttpServer())
      .post('/auth/login')
      .send({ email: 'jefe@taller.com', password: '123456' })
      .expect(201);
    token = sesion.body.access_token;
  });

  afterAll(async () => {
    const admin = await request(app.getHttpServer())
      .post('/auth/login')
      .send({ email: 'admin@taller.com', password: '123456' });

    for (const id of ordenesCreadas) {
      await request(app.getHttpServer())
        .delete(`/ordenes/${id}`)
        .set('Authorization', `Bearer ${admin.body.access_token}`);
    }
    await app.close();
  });

  it('una placa nueva crea el vehículo y lo normaliza', async () => {
    const respuesta = await crearOrden({ ...ordenBase, placa: `${placa}-x` }).expect(201);

    ordenesCreadas.push(respuesta.body.id);
    vehiculoId = respuesta.body.vehiculo.id;

    expect(respuesta.body.vehiculo.placa).toBe(`${placa}X`);
    expect(respuesta.body.vehiculo.propietario_nombre).toBe('Rosa Delgado');
  });

  it('la busca por placa sin importar el formato que se escriba', async () => {
    const respuesta = await request(app.getHttpServer())
      .get(`/vehiculos/placa/${placa.toLowerCase()}-x`)
      .set('Authorization', `Bearer ${token}`)
      .expect(200);

    expect(respuesta.body.id).toBe(vehiculoId);
  });

  it('una segunda orden con la misma placa reutiliza el vehículo', async () => {
    const respuesta = await crearOrden({ ...ordenBase, placa: `${placa}X` }).expect(201);

    ordenesCreadas.push(respuesta.body.id);
    expect(respuesta.body.vehiculo.id).toBe(vehiculoId);
  });

  it('datos distintos responden 409 con el detalle y no pisan nada', async () => {
    const respuesta = await crearOrden({
      ...ordenBase,
      placa: `${placa}X`,
      modelo: 'Corolla',
      propietario_nombre: 'Juan Pérez',
    }).expect(409);

    expect(respuesta.body.diferencias).toEqual(
      expect.arrayContaining([
        { campo: 'modelo', guardado: 'Yaris', enviado: 'Corolla' },
        { campo: 'propietario_nombre', guardado: 'Rosa Delgado', enviado: 'Juan Pérez' },
      ]),
    );

    const sinCambios = await request(app.getHttpServer())
      .get(`/vehiculos/${vehiculoId}`)
      .set('Authorization', `Bearer ${token}`)
      .expect(200);

    expect(sinCambios.body.modelo).toBe('Yaris');
  });

  it('con actualizar_vehiculo el cambio sí se aplica', async () => {
    const respuesta = await crearOrden({
      ...ordenBase,
      placa: `${placa}X`,
      modelo: 'Corolla',
      actualizar_vehiculo: true,
    }).expect(201);

    ordenesCreadas.push(respuesta.body.id);
    expect(respuesta.body.vehiculo.modelo).toBe('Corolla');
  });

  it('la ficha del vehículo trae su historial completo', async () => {
    const respuesta = await request(app.getHttpServer())
      .get(`/vehiculos/${vehiculoId}`)
      .set('Authorization', `Bearer ${token}`)
      .expect(200);

    expect(respuesta.body.ordenes).toHaveLength(3);
    expect(respuesta.body.ordenes[0].numero_orden).toMatch(/^ORD-\d{6,}$/);
  });
});
```

- [ ] **Step 2: Correr el e2e para verificar que falla**

```bash
cd backend && npm run test:e2e -- vehiculo
```

Esperado: FAIL. La orden todavía no acepta `propietario_nombre` (responde `400` por `forbidNonWhitelisted`) ni devuelve `vehiculo`.

- [ ] **Step 3: Agregar la relación inversa a la entidad `Vehiculo`**

En `backend/src/vehiculos/entities/vehiculo.entity.ts`, agregar el import y el bloque, antes de los timestamps:

```ts
import { OneToMany } from 'typeorm';
import { Orden } from '../../ordenes/entities/orden.entity';
```

```ts
  @OneToMany(() => Orden, (orden) => orden.vehiculo)
  ordenes!: Orden[];
```

- [ ] **Step 4: Cambiar la entidad `Orden`**

En `backend/src/ordenes/entities/orden.entity.ts`, **borrar** los seis bloques de `placa`, `marca`, `modelo`, `anio`, `cliente_nombre` y `cliente_telefono`, y agregar en su lugar, junto a las otras relaciones:

```ts
  // RESTRICT y no CASCADE a propósito: borrar un vehículo con historial debe
  // fallar, no llevarse sus órdenes por delante.
  @ManyToOne(() => Vehiculo, (vehiculo) => vehiculo.ordenes, {
    nullable: false,
    onDelete: 'RESTRICT',
  })
  @JoinColumn({ name: 'vehiculo_id' })
  vehiculo!: Vehiculo;
```

Con el import `import { Vehiculo } from '../../vehiculos/entities/vehiculo.entity';`.

- [ ] **Step 5: Cambiar el DTO de creación**

En `backend/src/ordenes/dto/crear-orden.dto.ts`, renombrar `cliente_nombre` a `propietario_nombre` y `cliente_telefono` a `propietario_telefono` (conservando sus decoradores y mensajes, cambiando "cliente" por "propietario" en los textos), y agregar al final:

```ts
  @IsOptional()
  @IsBoolean({ message: 'actualizar_vehiculo debe ser verdadero o falso' })
  actualizar_vehiculo?: boolean;
```

Añadiendo `IsBoolean` al import de `class-validator`.

`ActualizarOrdenDto` no se toca: hereda de `PartialType(CrearOrdenDto)` y recibe los cambios solo.

- [ ] **Step 6: Agregar `resolverParaOrden` al servicio de vehículos**

En `backend/src/vehiculos/vehiculos.service.ts`, ampliar los imports:

```ts
import { ConflictException, Injectable, NotFoundException } from '@nestjs/common';
import { EntityManager, Repository } from 'typeorm';
import { compararVehiculo } from './comparar-vehiculo';
```

Y agregar el tipo y el método:

```ts
export interface DatosVehiculoEntrantes {
  placa: string;
  marca: string;
  modelo: string;
  anio?: number;
  propietario_nombre: string;
  propietario_telefono: string;
}
```

```ts
  /**
   * Find-or-create del vehículo de una orden. Recibe el `manager` de la
   * transacción que está escribiendo la orden, para que una orden que falla no
   * deje un vehículo huérfano.
   */
  async resolverParaOrden(
    datos: DatosVehiculoEntrantes,
    actualizar: boolean,
    manager: EntityManager,
  ): Promise<Vehiculo> {
    const placa = normalizarPlaca(datos.placa);
    const existente = await manager.findOne(Vehiculo, { where: { placa } });

    if (!existente) {
      // Campo por campo y no `{ ...datos }`: cuando esto se llama desde
      // `actualizar`, `datos` viene mezclado con el vehículo actual de la orden
      // y arrastraría su `id`, con lo que TypeORM haría un UPDATE del vehículo
      // viejo —cambiándole la placa— en vez de crear uno nuevo.
      return await manager.save(
        manager.create(Vehiculo, {
          placa,
          marca: datos.marca,
          modelo: datos.modelo,
          anio: datos.anio,
          propietario_nombre: datos.propietario_nombre,
          propietario_telefono: datos.propietario_telefono,
        }),
      );
    }

    const diferencias = compararVehiculo(existente, datos);
    if (diferencias.length === 0) {
      return existente;
    }

    if (!actualizar) {
      throw new ConflictException({
        statusCode: 409,
        message: `La placa ${placa} ya está registrada con datos distintos`,
        diferencias,
      });
    }

    for (const { campo } of diferencias) {
      existente[campo] = datos[campo];
    }
    return await manager.save(existente);
  }
```

- [ ] **Step 7: Resolver el vehículo al crear y al actualizar la orden**

En `backend/src/ordenes/ordenes.service.ts`, agregar el import de `VehiculosService` y de su tipo, inyectarlo, y reescribir `crear` y `actualizar`.

`generarNumeroOrden` pasa a recibir el `manager` para viajar por la misma transacción:

```ts
  private async generarNumeroOrden(manager: EntityManager): Promise<string> {
    const filas: Array<{ nextval: string }> = await manager.query(
      `SELECT nextval('${SECUENCIA_NUMERO_ORDEN}') AS nextval`,
    );
    return formatearNumeroOrden(Number(filas[0].nextval));
  }
```

```ts
  async crear(dto: CrearOrdenDto, usuarioId: string) {
    return await this.dataSource.transaction(async (manager) => {
      const vehiculo = await this.vehiculosService.resolverParaOrden(
        dto,
        dto.actualizar_vehiculo === true,
        manager,
      );

      const nuevaOrden = manager.create(Orden, {
        descripcion: dto.descripcion,
        presupuesto: dto.presupuesto,
        fecha_ingreso: dto.fecha_ingreso,
        fecha_entrega: dto.fecha_entrega,
        numero_orden: await this.generarNumeroOrden(manager),
        vehiculo,
        creado_por: { id: usuarioId } as any,
      });

      return await manager.save(nuevaOrden);
    });
  }

  async actualizar(id: string, dto: ActualizarOrdenDto) {
    return await this.dataSource.transaction(async (manager) => {
      const orden = await manager.findOne(Orden, {
        where: { id },
        relations: { vehiculo: true },
      });

      if (!orden) {
        throw new NotFoundException(`No existe la orden con id ${id}`);
      }

      if (dto.placa) {
        orden.vehiculo = await this.vehiculosService.resolverParaOrden(
          { ...orden.vehiculo, ...dto, placa: dto.placa },
          dto.actualizar_vehiculo === true,
          manager,
        );
      }

      if (dto.descripcion !== undefined) orden.descripcion = dto.descripcion;
      if (dto.presupuesto !== undefined) orden.presupuesto = dto.presupuesto;
      if (dto.fecha_ingreso !== undefined) orden.fecha_ingreso = dto.fecha_ingreso;
      if (dto.fecha_entrega !== undefined) orden.fecha_entrega = dto.fecha_entrega;

      return await manager.save(orden);
    });
  }
```

Se pasa a asignación campo por campo en lugar de `Object.assign(orden, dto)` porque el DTO ahora trae campos del vehículo que no son columnas de la orden.

- [ ] **Step 8: Incorporar la relación a las consultas**

En el mismo archivo, en `obtenerTodas` agregar `vehiculo: true` a `relations` y reemplazar en `select` los seis campos planos por:

```ts
        vehiculo: {
          id: true,
          placa: true,
          marca: true,
          modelo: true,
          anio: true,
          propietario_nombre: true,
          propietario_telefono: true,
        },
```

Y en `obtenerPorId`, agregar `vehiculo: true` a `relations`.

- [ ] **Step 9: Conectar los módulos**

En `backend/src/ordenes/ordenes.module.ts`, agregar `VehiculosModule` a `imports` y su import arriba.

- [ ] **Step 10: Escribir la migración que suelta las columnas viejas**

Obtener un timestamp nuevo con `date +%s000`, posterior al de la Task 2.

Crear `backend/src/migrations/<timestamp>-SoltarDatosDeVehiculoEnOrdenes.ts`:

```ts
import { MigrationInterface, QueryRunner } from 'typeorm';

/**
 * Segunda mitad de la extracción: ahora que el código usa la relación, se
 * puede exigir `vehiculo_id` y soltar los seis campos desnormalizados.
 */
export class SoltarDatosDeVehiculoEnOrdenes<TIMESTAMP> implements MigrationInterface {
  public async up(queryRunner: QueryRunner): Promise<void> {
    await queryRunner.query(`ALTER TABLE "ordenes" ALTER COLUMN "vehiculo_id" SET NOT NULL`);
    await queryRunner.query(`
      ALTER TABLE "ordenes" ADD CONSTRAINT "FK_ordenes_vehiculo"
      FOREIGN KEY ("vehiculo_id") REFERENCES "vehiculos"("id")
      ON DELETE RESTRICT ON UPDATE NO ACTION
    `);
    await queryRunner.query(`
      ALTER TABLE "ordenes"
        DROP COLUMN "placa",
        DROP COLUMN "marca",
        DROP COLUMN "modelo",
        DROP COLUMN "anio",
        DROP COLUMN "cliente_nombre",
        DROP COLUMN "cliente_telefono"
    `);
  }

  public async down(queryRunner: QueryRunner): Promise<void> {
    await queryRunner.query(`
      ALTER TABLE "ordenes"
        ADD COLUMN "placa" character varying(10),
        ADD COLUMN "marca" character varying(50),
        ADD COLUMN "modelo" character varying(50),
        ADD COLUMN "anio" integer,
        ADD COLUMN "cliente_nombre" character varying(150),
        ADD COLUMN "cliente_telefono" character varying(20)
    `);
    await queryRunner.query(`
      UPDATE "ordenes" o
      SET "placa" = v."placa", "marca" = v."marca", "modelo" = v."modelo",
          "anio" = v."anio", "cliente_nombre" = v."propietario_nombre",
          "cliente_telefono" = v."propietario_telefono"
      FROM "vehiculos" v WHERE v."id" = o."vehiculo_id"
    `);
    await queryRunner.query(`ALTER TABLE "ordenes" DROP CONSTRAINT "FK_ordenes_vehiculo"`);
    await queryRunner.query(`ALTER TABLE "ordenes" ALTER COLUMN "vehiculo_id" DROP NOT NULL`);
  }
}
```

- [ ] **Step 11: Correr el e2e para verificar que pasa**

```bash
cd backend && npm run test:e2e -- vehiculo
```

Esperado: PASS, 6 tests. La API aplica la migración al arrancar, así que basta con que el runner levante la aplicación.

- [ ] **Step 12: Correr toda la suite**

```bash
cd backend && npm test && npm run test:e2e
```

Esperado: los unitarios pasan enteros, y los dos e2e —`flujo-orden` y `vehiculo`— también. Si `flujo-orden` falla, es porque su cuerpo de orden todavía manda `cliente_nombre` y `cliente_telefono`: actualizarlo a `propietario_nombre`, `propietario_telefono` y `placa`.

- [ ] **Step 13: Commit**

```bash
git add backend/src backend/test
git commit -m "feat: las órdenes cuelgan del vehículo, con find-or-create y aviso de conflicto"
```

---

### Task 4: Autocompletado por placa en el formulario de orden

**Files:**
- Create: `frontend/src/app/core/models/vehiculo.model.ts`
- Create: `frontend/src/app/core/services/vehiculo.ts`
- Modify: `frontend/src/app/core/models/orden.model.ts`
- Modify: `frontend/src/app/features/ordenes/formulario-orden/formulario-orden.ts`
- Modify: `frontend/src/app/features/ordenes/formulario-orden/formulario-orden.html`

**Interfaces:**
- Consumes: `GET /vehiculos/placa/:placa` de la Task 2; el `409` con `diferencias` de la Task 3.
- Produces:
  - `interface Vehiculo { id, placa, marca, modelo, anio?, propietario_nombre, propietario_telefono }`
  - `interface VehiculoConHistorial extends Vehiculo { ordenes: OrdenResumen[] }`
  - `VehiculoService.buscarPorPlaca(placa: string): Observable<Vehiculo>`
  - `VehiculoService.obtenerPorId(id: string): Observable<VehiculoConHistorial>`

- [ ] **Step 1: Escribir el modelo**

Crear `frontend/src/app/core/models/vehiculo.model.ts`:

```ts
export interface Vehiculo {
  id: string;
  placa: string;
  marca: string;
  modelo: string;
  anio?: number;
  propietario_nombre: string;
  propietario_telefono: string;
}

export interface OrdenResumen {
  id: string;
  numero_orden: string;
  descripcion: string;
  estado: string;
  presupuesto?: number;
  fecha_ingreso: string;
  fecha_entrega?: string;
}

export interface VehiculoConHistorial extends Vehiculo {
  ordenes: OrdenResumen[];
}

export interface Diferencia {
  campo: string;
  guardado: unknown;
  enviado: unknown;
}
```

- [ ] **Step 2: Escribir el servicio**

Crear `frontend/src/app/core/services/vehiculo.ts`:

```ts
import { HttpClient } from '@angular/common/http';
import { inject, Injectable } from '@angular/core';
import { Observable } from 'rxjs';

import { environment } from '../../../environments/environment';
import { Vehiculo, VehiculoConHistorial } from '../models/vehiculo.model';

@Injectable({
  providedIn: 'root',
})
export class VehiculoService {
  private readonly http = inject(HttpClient);
  private readonly url = `${environment.apiUrl}/vehiculos`;

  buscarPorPlaca(placa: string): Observable<Vehiculo> {
    return this.http.get<Vehiculo>(`${this.url}/placa/${encodeURIComponent(placa)}`);
  }

  obtenerPorId(id: string): Observable<VehiculoConHistorial> {
    return this.http.get<VehiculoConHistorial>(`${this.url}/${id}`);
  }
}
```

- [ ] **Step 3: Actualizar el modelo de orden**

En `frontend/src/app/core/models/orden.model.ts`, en `Orden` reemplazar los seis campos por `vehiculo: Vehiculo;` con su import, y en `OrdenRequest` renombrar `cliente_nombre` y `cliente_telefono` a `propietario_nombre` y `propietario_telefono`, y agregar `actualizar_vehiculo?: boolean;`.

- [ ] **Step 4: Consultar la placa mientras se escribe**

En `formulario-orden.ts`, renombrar los dos campos del `FormGroup` y agregar el estado del autocompletado y del conflicto:

```ts
  readonly vehiculoConocido = signal<Vehiculo | null>(null);
  readonly diferencias = signal<Diferencia[]>([]);
```

En el constructor, suscribirse al campo de placa con 400 ms de espera. `takeUntilDestroyed` necesita ejecutarse en contexto de inyección, y el constructor lo es:

```ts
  constructor() {
    this.formulario.controls.placa.valueChanges
      .pipe(
        debounceTime(400),
        map((placa) => placa.replace(/[^A-Za-z0-9]/g, '').toUpperCase()),
        distinctUntilChanged(),
        filter((placa) => placa.length >= 6),
        switchMap((placa) =>
          this.vehiculoService.buscarPorPlaca(placa).pipe(catchError(() => of(null))),
        ),
        takeUntilDestroyed(),
      )
      .subscribe((vehiculo) => {
        this.vehiculoConocido.set(vehiculo);
        if (vehiculo && !this.esEdicion) {
          this.formulario.patchValue(
            {
              marca: vehiculo.marca,
              modelo: vehiculo.modelo,
              anio: vehiculo.anio ?? new Date().getFullYear(),
              propietario_nombre: vehiculo.propietario_nombre,
              propietario_telefono: vehiculo.propietario_telefono,
            },
            { emitEvent: false },
          );
        }
      });
  }
```

Con los imports `import { debounceTime, distinctUntilChanged, filter, map, switchMap, catchError, of } from 'rxjs';` y `import { takeUntilDestroyed } from '@angular/core/rxjs-interop';`.

El `catchError` que devuelve `of(null)` es lo que convierte el `404` de "placa desconocida" en el caso normal: no es un error que mostrar, es que el auto es nuevo.

- [ ] **Step 5: Manejar el conflicto al guardar**

En `manejarError`, antes de armar el mensaje, capturar el `409`:

```ts
  private manejarError(error: any): void {
    this.guardando.set(false);

    if (error.status === 409 && Array.isArray(error.error?.diferencias)) {
      this.diferencias.set(error.error.diferencias);
      return;
    }

    const mensaje = error.error?.message;
    this.mensajeError.set(
      Array.isArray(mensaje) ? mensaje.join('. ') : (mensaje ?? 'No se pudo guardar la orden'),
    );
  }
```

Y agregar los dos métodos que resuelven el diálogo:

```ts
  confirmarActualizacionDelVehiculo(): void {
    this.diferencias.set([]);
    this.enviar(true);
  }

  descartarActualizacion(): void {
    this.diferencias.set([]);
  }
```

`enviar` pasa a aceptar el flag y a incluirlo en el cuerpo:

```ts
  enviar(actualizarVehiculo = false): void {
    // ...validación y armado de `datos` como hasta ahora...
    if (actualizarVehiculo) datos.actualizar_vehiculo = true;
    // ...
  }
```

En la plantilla, el `(ngSubmit)` pasa a `(ngSubmit)="enviar()"`.

- [ ] **Step 6: Mostrar el aviso y el diálogo en la plantilla**

En `formulario-orden.html`, debajo del campo de placa:

```html
@if (vehiculoConocido(); as vehiculo) {
  <small class="text-success d-block mt-1">
    Vehículo conocido ·
    <a [routerLink]="['/vehiculos', vehiculo.id]">ver historial</a>
  </small>
}
```

Y encima de los botones de acción, el diálogo de conflicto como bloque en la
página, sin depender del JavaScript de Bootstrap:

```html
@if (diferencias().length > 0) {
  <div class="alert alert-warning">
    <p class="fw-semibold mb-2">Esta placa ya está registrada con otros datos</p>
    <ul class="mb-3">
      @for (diferencia of diferencias(); track diferencia.campo) {
        <li>
          <strong>{{ diferencia.campo }}</strong>:
          guardado <em>{{ diferencia.guardado }}</em>,
          escrito <em>{{ diferencia.enviado }}</em>
        </li>
      }
    </ul>
    <button type="button" class="btn btn-sm btn-warning me-2"
            (click)="confirmarActualizacionDelVehiculo()">
      Actualizar el vehículo
    </button>
    <button type="button" class="btn btn-sm btn-outline-secondary"
            (click)="descartarActualizacion()">
      Corregir
    </button>
  </div>
}
```

Añadir `RouterLink` a los `imports` del componente. Renombrar en la plantilla los dos campos de cliente a `propietario_nombre` y `propietario_telefono`, incluidos sus `for`, `id` y `formControlName`.

- [ ] **Step 7: Verificar en el navegador**

Levantar API y frontend, entrar como `jefe@taller.com` y comprobar:

1. Crear una orden con placa nueva `ABC-123`. Se guarda y el detalle muestra `ABC123`.
2. Crear otra orden y escribir `abc123`: a los 400 ms se autocompletan marca, modelo y propietario, y aparece "Vehículo conocido · ver historial".
3. Cambiar el modelo a otra cosa y guardar: aparece el bloque amarillo con la diferencia.
4. Pulsar **Corregir**: el bloque desaparece y no se guardó nada.
5. Volver a guardar y pulsar **Actualizar el vehículo**: la orden se crea y el modelo del vehículo queda cambiado.

- [ ] **Step 8: Compilar**

```bash
cd frontend && npm run build
```

Esperado: compila sin errores. Un `Property 'cliente_nombre' does not exist` significa que quedó una referencia sin renombrar.

- [ ] **Step 9: Commit**

```bash
git add frontend/src/app
git commit -m "feat: autocompletar el vehículo por placa y avisar del conflicto"
```

---

### Task 5: Ficha del vehículo y adaptación de las vistas

**Files:**
- Create: `frontend/src/app/features/vehiculos/ficha-vehiculo/ficha-vehiculo.ts`
- Create: `frontend/src/app/features/vehiculos/ficha-vehiculo/ficha-vehiculo.html`
- Modify: `frontend/src/app/app.routes.ts`
- Modify: `frontend/src/app/features/ordenes/detalle-orden/detalle-orden.html`
- Modify: `frontend/src/app/features/ordenes/lista-ordenes/lista-ordenes.html`
- Modify: `frontend/src/app/features/dashboard/dashboard.html`

**Interfaces:**
- Consumes: `VehiculoService.obtenerPorId` y `VehiculoConHistorial` de la Task 4.
- Produces: la ruta `/vehiculos/:id`.

- [ ] **Step 1: Escribir el componente**

Crear `frontend/src/app/features/vehiculos/ficha-vehiculo/ficha-vehiculo.ts`:

```ts
import { Component, inject, OnInit, signal } from '@angular/core';
import { ActivatedRoute, RouterLink } from '@angular/router';
import { CurrencyPipe, DatePipe } from '@angular/common';

import { VehiculoService } from '../../../core/services/vehiculo';
import { VehiculoConHistorial } from '../../../core/models/vehiculo.model';
import { Spinner } from '../../../shared/components/spinner/spinner';
import { BadgeEstado } from '../../../shared/components/badge-estado/badge-estado';

@Component({
  selector: 'app-ficha-vehiculo',
  imports: [RouterLink, CurrencyPipe, DatePipe, Spinner, BadgeEstado],
  templateUrl: './ficha-vehiculo.html',
  styles: ``,
})
export class FichaVehiculo implements OnInit {
  private readonly vehiculoService = inject(VehiculoService);
  private readonly route = inject(ActivatedRoute);

  readonly cargando = signal<boolean>(true);
  readonly mensajeError = signal<string | null>(null);
  readonly vehiculo = signal<VehiculoConHistorial | null>(null);

  ngOnInit(): void {
    const id = this.route.snapshot.paramMap.get('id');
    if (!id) return;

    this.vehiculoService.obtenerPorId(id).subscribe({
      next: (datos) => {
        this.vehiculo.set(datos);
        this.cargando.set(false);
      },
      error: () => {
        this.mensajeError.set('No se pudo cargar el vehículo');
        this.cargando.set(false);
      },
    });
  }
}
```

- [ ] **Step 2: Escribir la plantilla**

Crear `frontend/src/app/features/vehiculos/ficha-vehiculo/ficha-vehiculo.html`:

```html
@if (mensajeError()) {
  <div class="alert alert-danger">{{ mensajeError() }}</div>
  <a class="btn btn-outline-secondary" routerLink="/ordenes">Volver al listado</a>
}

@if (cargando()) {
  <app-spinner></app-spinner>
} @else if (vehiculo(); as datos) {
  <div class="card shadow-sm mb-4">
    <div class="card-header d-flex justify-content-between align-items-center">
      <span class="fw-bold fs-5">{{ datos.placa }}</span>
      <a class="btn btn-sm btn-outline-secondary" routerLink="/ordenes">Volver</a>
    </div>

    <div class="card-body row">
      <div class="col-md-6">
        <h6 class="text-muted">Vehículo</h6>
        <p class="mb-1"><strong>{{ datos.marca }} {{ datos.modelo }}</strong></p>
        @if (datos.anio) {
          <p class="mb-1 small">Año: {{ datos.anio }}</p>
        }
      </div>
      <div class="col-md-6">
        <h6 class="text-muted">Propietario</h6>
        <p class="mb-1"><strong>{{ datos.propietario_nombre }}</strong></p>
        <p class="mb-1 small">Teléfono: {{ datos.propietario_telefono }}</p>
      </div>
    </div>
  </div>

  <h5 class="mb-3">Historial ({{ datos.ordenes.length }})</h5>

  @if (datos.ordenes.length === 0) {
    <p class="text-muted">Este vehículo todavía no tiene órdenes.</p>
  } @else {
    <div class="table-responsive">
      <table class="table table-hover align-middle">
        <thead>
          <tr>
            <th>Orden</th>
            <th>Ingreso</th>
            <th>Servicio</th>
            <th>Presupuesto</th>
            <th>Estado</th>
          </tr>
        </thead>
        <tbody>
          @for (orden of datos.ordenes; track orden.id) {
            <tr>
              <td><a [routerLink]="['/ordenes', orden.id]">{{ orden.numero_orden }}</a></td>
              <td>{{ orden.fecha_ingreso | date: 'dd/MM/yyyy' }}</td>
              <td>{{ orden.descripcion }}</td>
              <td>
                @if (orden.presupuesto) {
                  {{ orden.presupuesto | currency: 'PEN' }}
                } @else {
                  <span class="text-muted">—</span>
                }
              </td>
              <td><app-badge-estado [estado]="orden.estado"></app-badge-estado></td>
            </tr>
          }
        </tbody>
      </table>
    </div>
  }
}
```

- [ ] **Step 3: Registrar la ruta**

En `frontend/src/app/app.routes.ts`, antes de la ruta comodín `**`:

```ts
  {
    path: 'vehiculos/:id',
    canActivate: [authGuard],
    loadComponent: () =>
      import('./features/vehiculos/ficha-vehiculo/ficha-vehiculo').then((c) => c.FichaVehiculo),
  },
```

- [ ] **Step 4: Adaptar el detalle de orden**

En `detalle-orden.html`, el bloque "Vehículo" pasa a leer la relación y gana el enlace:

```html
<h6 class="text-muted">Vehículo</h6>
<p class="mb-1">
  <strong>{{ datosOrden.vehiculo.marca }} {{ datosOrden.vehiculo.modelo }}</strong>
</p>
<p class="mb-1 small">
  Placa:
  <a [routerLink]="['/vehiculos', datosOrden.vehiculo.id]">
    {{ datosOrden.vehiculo.placa }}
  </a>
</p>
@if (datosOrden.vehiculo.anio) {
  <p class="mb-1 small">Año: {{ datosOrden.vehiculo.anio }}</p>
}
```

Y el bloque "Cliente" pasa a leer `datosOrden.vehiculo.propietario_nombre` y `datosOrden.vehiculo.propietario_telefono`, con el encabezado "Propietario".

- [ ] **Step 5: Adaptar el listado y el dashboard**

En `lista-ordenes.html` y `dashboard.html`, reemplazar cada `orden.placa` por `orden.vehiculo.placa`, y cualquier `orden.marca` / `orden.modelo` / `orden.cliente_nombre` por su equivalente bajo `orden.vehiculo`. Buscarlos con:

```bash
grep -rn "\.placa\|\.marca\|\.modelo\|cliente_nombre\|cliente_telefono" frontend/src/app
```

Esperado tras el cambio: ninguna referencia sin `vehiculo.` delante, salvo dentro de `vehiculo.model.ts` y de la ficha.

- [ ] **Step 6: Compilar y verificar en el navegador**

```bash
cd frontend && npm run build
```

Después, con la aplicación levantada: abrir una orden, pulsar la placa, comprobar que la ficha muestra los datos y el historial, y que desde una fila del historial se vuelve a la orden.

- [ ] **Step 7: Commit**

```bash
git add frontend/src/app
git commit -m "feat: ficha de vehículo con su historial de órdenes"
```

---

### Task 6: Documentación

**Files:**
- Modify: `README.md`
- Modify: `docs/modelo-er.md`
- Modify: `docs/endpoints.http`
- Modify: `docs/contexto-core.md`

**Interfaces:**
- Consumes: todo lo anterior.
- Produces: nada que consuman otras tareas.

- [ ] **Step 1: Actualizar el README**

En la sección "De qué va", cambiar la primera viñeta por:

> - El asesor de servicio registra la orden: escribe la placa y, si el auto ya
>   estuvo en el taller, se autocompletan sus datos y los del propietario.

En "Notas técnicas", agregar al final:

> La placa es la identidad del vehículo y se guarda normalizada: mayúsculas y
> solo letras y dígitos, así que `ABC-123` queda como `ABC123`. Es una forma
> canónica única a costa del guion impreso. Al registrar una orden con una placa
> conocida pero datos distintos, la API responde `409` con la lista de
> diferencias en lugar de reescribir el histórico del auto; el formulario las
> muestra y deja elegir.

En "Estructura", agregar `vehiculos/` bajo `backend/src/` con la descripción
`placa, ficha e historial`.

- [ ] **Step 2: Actualizar el modelo E-R**

En `docs/modelo-er.md`:

- En el diagrama, agregar `VEHICULOS ──< ORDENES`.
- En la tabla de relaciones, agregar la fila
  `| Orden | Vehiculo | N:1 | @ManyToOne con onDelete: RESTRICT |`.
- En el script SQL, agregar el `CREATE TABLE vehiculos` y, en `ordenes`,
  reemplazar `placa`, `marca`, `modelo`, `anio`, `cliente_nombre` y
  `cliente_telefono` por
  `vehiculo_id UUID NOT NULL REFERENCES vehiculos(id) ON DELETE RESTRICT`.

- [ ] **Step 3: Actualizar la colección de endpoints**

En `docs/endpoints.http`, en el cuerpo de "Crear una orden" renombrar
`cliente_nombre` y `cliente_telefono` a `propietario_nombre` y
`propietario_telefono`, y agregar al final:

```
### ─────────────── VEHÍCULOS ───────────────

### Buscar por placa (para autocompletar; acepta cualquier formato)
GET {{api}}/vehiculos/placa/abc-123
Authorization: Bearer {{token}}

### Ficha con el historial de órdenes
GET {{api}}/vehiculos/ID_DEL_VEHICULO
Authorization: Bearer {{token}}
```

- [ ] **Step 4: Actualizar el contexto exportado**

En `docs/contexto-core.md`:

- Sección 1: la primera línea del flujo pasa a mencionar la búsqueda por placa.
- Sección 3: agregar `VEHICULOS` al diagrama y la entidad `vehiculos` a la lista;
  quitar de `ordenes` los seis campos y describir `vehiculo_id`.
- Sección 5: agregar las dos rutas `GET /vehiculos/...`.
- Sección 7: agregar la nota de normalización de la placa.
- Sección 8: mover a "Cerrados" el hueco de cliente y vehículo desnormalizados,
  ajustándolo para decir que el vehículo ya es entidad y el cliente todavía no.

- [ ] **Step 5: Verificación final de punta a punta**

Base desde cero y recorrido completo:

```bash
docker compose down -v && docker compose up -d
cd backend && npm run start:dev     # aplica las 6 migraciones y el seed
cd frontend && npm start
```

Comprobar:

1. `asesor@taller.com` registra una orden con placa `ABC-123`. Queda `ORD-000001` y el vehículo aparece como `ABC123`.
2. Registra una segunda orden escribiendo `abc 123`: autocompleta y avisa "Vehículo conocido".
3. Cambia el modelo y guarda: aparece el aviso de conflicto. "Corregir" no cambia nada; "Actualizar el vehículo" sí.
4. Desde el detalle de la orden, la placa lleva a la ficha, que muestra las dos órdenes.
5. `admin@taller.com` elimina una de las órdenes y el vehículo sigue existiendo con la otra.

- [ ] **Step 6: Commit**

```bash
git add -A
git commit -m "docs: documentar la extracción del vehículo"
```

- [ ] **Step 7: Merge**

Con todo verificado:

```bash
git checkout master && git merge --no-ff fase-1 -m "merge: fase 1 — el vehículo como entidad con historial por placa"
```
